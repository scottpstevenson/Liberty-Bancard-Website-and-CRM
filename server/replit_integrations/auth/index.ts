export { setupAuth, isAuthenticated, isAdmin, isAffiliate, isPartnerAuthenticated, isDashboardUser, requireRole, getSession } from "./replitAuth";
export { authStorage, type IAuthStorage } from "./storage";
export { registerAuthRoutes } from "./routes";
