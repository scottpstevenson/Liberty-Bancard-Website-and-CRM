Approved with guardrails. This is a safe cleanup task only if Replit verifies imports are truly unused after hub consolidation.

The key risk: #789 required old routes to still work. So App.tsx lazy imports can only be removed if those old routes now redirect to hubs or no longer reference those components directly.

Final Addendum to #802 — Remove Unused Hub Consolidation Imports

Approved to build as a cleanup-only task.

Core rule

Remove only imports that TypeScript/lint confirms are unused.

Do not remove any lazy import that is still referenced by an active Route in App.tsx.

App.tsx guard

Before removing top-level lazy imports, check whether each old route:

1. still renders the original component directly
2. redirects to a hub route
3. was removed accidentally

Allowed:

old route redirects to hub tab → remove old component lazy import from App.tsx
hub imports child component internally → keep import only in hub file

Not allowed:

old route still renders component → remove its App.tsx lazy import
old route returns 404
old route silently disappears

DashboardLayout.tsx guard

Remove unused lucide-react icons only if they are not referenced anywhere in the file after the sidebar consolidation.

Do not replace icons or change nav labels in this task.

Out of scope

Do not:

change routes
change hub behavior
change sidebar IA
change role guards
delete page files
change child page imports inside hub files
change bundle-splitting strategy beyond removing unused top-level imports

Kill lines

STOP if any old route breaks after removing lazy imports.
STOP if an App.tsx lazy import is removed while still referenced by a Route.
STOP if a hub page loses access to one of its child tabs.
STOP if DashboardLayout nav labels/groups change.
STOP if this task deletes page files.
STOP if role-guard smoke tests regress.

Validation

Run:

npx tsc --noEmit --skipLibCheck
npx tsx scripts/smoke-role-guards.ts

Also run grep checks:

grep -n "OnboardingBoard\|GhlSettings\|GhlWorkflowManager\|GhlSequenceGuide\|MarketingPlaybook\|GrowthPlaybook\|GrowthKPI\|ContentEditor\|BlogGenerator\|SocialComposer\|BlazeIntegration\|WinLoss\|OutreachAnalytics\|ReviewRequests\|TestimonialSubmissions\|NpsDashboard\|RetentionCampaigns\|Outreach\|OutreachCommand\|ColdLeads\|Reporting" client/src/App.tsx
grep -n "Linkedin\|Wand2\|Pencil\|BarChart2\|Trophy\|ThumbsUp\|MessageSquare\|Star\|RefreshCw" client/src/pages/DashboardLayout.tsx

Expected: only legitimate remaining references appear.

Manual checks

Open or verify redirects for old hubbed routes:

/dashboard/onboarding-board
/dashboard/ghl-settings
/dashboard/ghl-workflow-ids
/dashboard/ghl-sequence-guide
/dashboard/marketing-playbook
/dashboard/growth-playbook
/dashboard/blog-generator
/dashboard/social-composer
/dashboard/blaze-integration
/dashboard/growth-kpi
/dashboard/win-loss
/dashboard/outreach-analytics
/dashboard/review-requests
/dashboard/testimonial-submissions
/dashboard/nps
/dashboard/retention-campaigns
/dashboard/outreach
/dashboard/outreach-command
/dashboard/cold-leads

Each must still render or redirect cleanly.

Final report required

Return:

App.tsx imports removed:
DashboardLayout icons removed:
Old routes verified:
Hub child tabs verified:
Validation commands run:
Route-guard smoke result:
Files changed:

Final verdict: build #802. It’s a good cleanup task, but it must stay purely import cleanup — no route or hub behavior changes.