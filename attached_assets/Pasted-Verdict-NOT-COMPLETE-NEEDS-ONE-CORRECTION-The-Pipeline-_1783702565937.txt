Verdict: NOT COMPLETE / NEEDS ONE CORRECTION

The Pipeline helper is correct for stacked card labels, but it is being misused in the Deal Detail panel.

identity.secondary is not a company value. It may be an email or phone number. Rendering it in a field labeled Company creates incorrect UI semantics.

Correction to send back

Fix the Deal Detail panel before closing Task 923.
Do not render identity.secondary under the label “Company.”
getDealCardIdentity() is appropriate for:
- Kanban card primary/secondary display
- DragOverlay primary/secondary display
It is not appropriate for explicitly labeled detail fields.
Deal Detail must resolve fields independently:
Contact:
full name
→ companyName
→ email
→ phone
→ Deal #${deal.id}
Company:
contact?.companyName || "N/A"
Email:
existing canonical email value
Phone:
existing canonical phone value
Do not display an email address or phone number inside the Company row merely because it is the visual secondary identifier.
Also confirm:
1. /dashboard/outreach-hub?tab=command is an actual registered route and tab value.
2. useAuth role values are exactly "admin" and "manager" in this surface.
3. The vertical query’s isError state cannot retain stale vertical data and render populated options at the same time.
4. Add focused tests for the identity helper and four vertical states; typecheck and role guards alone do not verify rendering behavior.

Kill lines

STOP if identity.secondary can appear under a field labeled Company.
STOP if the Outreach Hub link or tab parameter is not verified from route code.
STOP if an error response can show stale populated vertical options.
STOP if the no-duplicate helper has no targeted tests for company-only, email-only, phone-only, and no-contact cases.

Validation

- Company-only contact:
  card = company / next distinct identifier
  detail Company = company
- Email-only contact:
  card = email / null
  detail Company = N/A, not email
- Phone-only contact:
  card = phone / null
  detail Company = N/A, not phone
- No contact:
  card and detail Contact = Deal #N
  detail Company = N/A
- Loading/error/empty/populated vertical states are tested independently.
- Error state never shows enrichment guidance or stale options.
- Outreach Hub link opens the intended command tab for admin/manager.

Final status: NOT COMPLETE

Once the Deal Detail field semantics are corrected and the route/tab is verified, the task can be marked COMPLETE / VERIFIED.