Final Corrections to Task #693 Before Build

Do not build #693 until these corrections are applied.

1. Do not assume the statement_proposals row already exists when the analyzer runs

The preflight confirms:

Step 10 creates statement_proposals row
Step 5 queues/runs analysis

That means the analyzer can run before Step 10 creates the proposal shell.

Required behavior:

analyzeStatement(dealId)
→ look for existing statement_proposals row by dealId
→ if found, update it
→ if not found, create a draft row safely
→ do not create duplicates

If there is no unique constraint on statement_proposals.dealId, do not rely on ON CONFLICT.

Use select-before-insert and re-check if needed.

Kill line:

STOP if analyzeStatement assumes a statement_proposals row already exists and silently updates 0 rows.

Kill line:

STOP if repeated analysis runs can create duplicate statement_proposals rows for the same deal.

⸻

2. Fix analysisStatus ownership so complete cannot overwrite failed

The plan currently says:

If extraction fails: analysisStatus = failed, write audit log, do NOT throw.

But if the existing queue worker writes:

analysisStatus = complete

after generateDealBlueprint, it may overwrite the analyzer failure.

Required:

Make status ordering explicit.

Acceptable options:

Option A — deal.analysisStatus represents full analysis

pending → processing
generateDealBlueprint succeeds
analyzeStatement succeeds
→ complete

If structured statement analysis fails:

→ failed
audit log
worker continues without crashing
do not overwrite failed with complete

Option B — deal.analysisStatus remains blueprint-owned

Then structured analyzer status must live on:

statement_proposals.status

with values like:

draft / analyzing / complete / failed

But do not claim deal.analysisStatus='failed' if the worker later overwrites it.

Kill line:

STOP if structured statement analysis fails but final deal.analysisStatus is still written as complete.

⸻

3. Do not default to 185 bps in production

The design currently shows:

Number(process.env.LIBERTY_TARGET_EFFECTIVE_RATE_BPS) || 185

That contradicts the requirement:

Default 185 only when TEST_MODE=true.

Correct behavior:

if LIBERTY_TARGET_EFFECTIVE_RATE_BPS is set:
  calculate Liberty estimated fees and savings
else if TEST_MODE=true or SKIP_AI=true:
  use fixture target bps
else:
  do not calculate Liberty savings
  store "target rate not configured"
  show "No estimate available — rep review required"

Kill line:

STOP if production analysis silently defaults to 185 bps when LIBERTY_TARGET_EFFECTIVE_RATE_BPS is unset.

⸻

4. Verify statement_proposals.plans shape before writing an object into it

The plan says to write:

plans: {
  extraction,
  computedMetrics
}

But plans may already be consumed elsewhere as an array.

Before changing the shape, grep all consumers of:

statementProposals.plans
plans
savingsProposal

Required:

* If consumers expect an array, preserve array compatibility.
* If no consumers rely on array shape, document that plans now stores structured proposal payload.
* Do not break existing proposal UI.

Kill line:

STOP if this task changes statement_proposals.plans from array to object and breaks existing proposal rendering.

⸻

5. Analyzer must select the correct statement document

The analyzer should not blindly use the first document for a deal.

Required file resolution:

dealId
→ documents for that deal
→ choose latest statement/payment-processing document
→ resolve storageKey/path
→ read from uploads/statements/

If multiple candidate documents exist, choose the most recent matching statement document and log the selected documentId.

If none exists:

analysisStatus/status = failed
audit log
no crash

Kill line:

STOP if analyzer can process the wrong document because it selects an arbitrary document row.

⸻

6. Existing autoGenerateProposal auto-send risk must be disclosed

The preflight confirms existing code can send a proposal email when:

proposal_auto_send = true

This task does not need to rewrite proposal-engine.ts, but the final report must explicitly confirm:

statement-analyzer.ts does not call autoGenerateProposal
statement-analyzer.ts does not call sendProposalEmail
statement-analyzer.ts only writes draft analysis/proposal data

Do not claim the entire statement pipeline is draft-only unless proposal_auto_send is confirmed disabled.

Kill line:

STOP if the new analyzer calls autoGenerateProposal, sendProposalEmail, sendEmail, SMS, GHL workflow enrollment, or sequence enrollment.

⸻

Corrected build scope

Build only after applying the above:

1. New server/services/statement-analyzer.ts
2. analyzeStatement(dealId) with document/file resolution
3. Zod-validated raw extraction schema
4. Deterministic server-side math
5. No production default target rate unless env is set
6. Safe update-or-create of statement_proposals
7. No duplicate proposal rows
8. Explicit analysis status ownership
9. Additive call after generateDealBlueprint
10. GET /api/deals/:id/analysis
11. ContactDetail analysis panel with draft/estimated/rep-review language
12. Fixture-only smoke tests

Do not:

* add deals.analysisData
* add a migration
* pass fake fileBuffer
* trust AI math
* hard-code 1.85 / 0.0185
* replace generateDealBlueprint
* touch proposal-engine.ts
* send proposals
* call live OpenAI in tests