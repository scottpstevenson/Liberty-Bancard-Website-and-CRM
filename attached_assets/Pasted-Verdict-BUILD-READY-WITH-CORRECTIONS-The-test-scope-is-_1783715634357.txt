Verdict: BUILD-READY WITH CORRECTIONS

The test scope is correct, but the worker-cycle tests are not isolated enough yet. Calling runSlaCheckDirect() against the normal development dataset may evaluate every qualifying deal, create unrelated tasks, and trigger audit or downstream behavior outside the fixtures.

Corrections to send back

Proceed after these corrections:
1. Make the SLA cycle fixture-scoped.
Do not call the unrestricted production-shaped runSlaCheckDirect() against the full dev dataset unless it supports injected scope/dependencies.
Preferred test-safe contract:
runSlaCheckDirect({
  dealIds: [fixtureDealId],
  suppressSideEffects: true
})
or export a dependency-injected single-cycle function used by both:
- runSlaCheckDirect()
- the integration test
The test must prove the actual generation logic without scanning or mutating unrelated deals.
Do not add test-only branching inside the core decision logic based on NODE_ENV.
2. Prove side-effect suppression covers the actual cycle path.
Setting environment flags is insufficient unless the called services read those flags.
Provide file:line evidence that the test prevents:
- GHL task creation/deletion
- audit noise outside test records
- BullMQ enqueueing
- email/SMS
- sequence enrollment
- recurring timers
Mock or inject side-effect boundaries where necessary.
3. Do not infer “non-production DB” from a fragile DATABASE_URL string check.
Use a positive test-database safety contract, such as:
- explicit TEST_DATABASE_URL
- required database name suffix/prefix
- dedicated environment flag plus DB identity verification
The script must refuse to run unless the database is explicitly approved for destructive integration tests.
4. Do not let the test create or mutate a persistent shared user without cleanup.
Prefer the existing reusable non-admin test user if it is already canonical.
If creating a user/session:
- use a unique test identifier
- track and clean it up
- avoid altering an existing user’s role
- never use production authentication cookies or credentials
5. Define how the real Express app starts without background services.
Importing server startup code may start:
- listeners
- BullMQ
- intervals
- migrations
- workers
Use the repository’s test app factory that registers real routes and middleware without invoking production startup side effects.
If no such factory exists, extract one before writing this test.
6. Fix test numbering and reporting.
The brief describes 10 route/worker cases plus the separate concurrency command, but later calls it “test 11.”
Report separately:
- route/worker integration assertions
- existing concurrency suite
- existing invariant suite
7. Test both provenance fields independently.
Verify POST and PUT behavior for:
- source only
- automationKey only
- both fields
A schema mistake could protect one while exposing the other.
8. Verify persisted business fields as well as provenance.
For accepted POST/PUT requests, confirm intended mutable fields still persist correctly. A test that only proves protected fields are stripped could pass even if the whole request body is accidentally ignored.
9. Worker assertions must use the canonical identity fields.
Count replacements using:
dealId
source = 'sla'
automationKey = 'stalling-deal-follow-up'
deletedAt IS NULL
completedAt IS NULL
Do not identify test-generated SLA tasks by title alone.
10. Cleanup must remove every side effect created by the cycle.
After each worker invocation, discover and track any newly created fixture tasks rather than cleaning only IDs known before the cycle.

Kill lines

STOP if runSlaCheckDirect() scans or mutates unrelated development records.
STOP if importing the app starts recurring workers, timers, or BullMQ.
STOP if outbound suppression is assumed from environment flags without code evidence.
STOP if the script can run against a database that is not explicitly identified as a test database.
STOP if session setup changes an existing user’s role or leaves test users behind.
STOP if cleanup tracks only pre-created tasks and misses tasks generated by the SLA cycle.
STOP if replacement-task assertions use title instead of canonical source/automationKey identity.
STOP if route tests bypass the real registered middleware or Zod schemas.

Validation

npx tsx scripts/test-sla-route-integration.ts
npx tsx scripts/test-sla-task-idempotency.ts
npx tsx scripts/test-task-invariants.ts
npx tsc --noEmit --skipLibCheck

Required proof:

- Real app routes run without production startup services.
- Only the fixture deal is evaluated by the SLA cycle.
- POST/PUT cannot persist either provenance field.
- Normal task fields still persist.
- Complete sets completedAt; reopen clears it.
- Existing active SLA task blocks replacement.
- Completion followed by one cycle creates exactly one canonical replacement.
- Second cycle creates no additional active replacement.
- All generated fixture records and sessions are cleaned in finally.

Final status: BUILD-READY WITH CORRECTIONS

The tests are valuable and correctly layered, but the SLA cycle must be constrained to deterministic fixtures before this is safe to run in CI or a shared development database.