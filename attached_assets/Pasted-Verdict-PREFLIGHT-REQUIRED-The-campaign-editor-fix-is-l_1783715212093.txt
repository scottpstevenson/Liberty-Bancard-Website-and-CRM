Verdict: PREFLIGHT REQUIRED

The campaign-editor fix is likely straightforward. The KPI plan is not build-ready because it assumes score fields and meanings that are probably wrong.

The biggest red flag is:

contacts WHERE score = 'hot'
prospects WHERE score = 'hot'

Liberty’s contact scoring has already been described as numeric lead_score thresholds, not a string score column.

Corrections to send back

Run a focused KPI source-of-truth preflight before implementation.
1. Verify the exact schema and canonical tier logic for each dataset.
Provide file:line evidence for:
- contacts score column name and type
- prospects score column name and type
- Hot/Warm thresholds
- archived/deleted eligibility rules
- the exact Prospects-tab count query
- the exact existing LCC count query
Do not implement contacts WHERE score = 'hot' unless that field actually exists.
The new endpoint must reuse the same canonical thresholds used by:
- contact lead scoring,
- Prospects tab,
- LCC eligibility displays.
2. Do not claim Hot/Warm must “match or exceed” the Prospects tab.
That is only true if:
- Prospects counts are included unchanged,
- filters and archived states match,
- there is no overlap/exclusion difference.
Instead require:
- prospectsHot in the new endpoint exactly reconciles to the Prospects tab;
- combinedHot = contactsHot + prospectsHot;
- combinedWarm = contactsWarm + prospectsWarm.
3. Reconsider the “Total Leads” label.
sunbiz_entities + contacts + prospects is a raw cross-table record count with acknowledged duplication.
Label it explicitly, such as:
“Total lead records”
Tooltip:
“Combined records across Sunbiz discovery, CRM contacts, and prospects. Records may overlap across sources.”
Do not present it as a unique-person or unique-business count.
4. Define Contactable once.
The brief currently says:
contacts with email/phone
+ Sunbiz entities with contact info
— or whichever is more useful
That is unresolved architecture.
Choose one metric before building.
Recommended operational definition:
“Contactable CRM”
= active contacts that pass the existing canonical contactability rules.
Do not replace canonical contactability with merely:
email IS NOT NULL OR phone IS NOT NULL
That can include:
- malformed values,
- suppressed contacts,
- DNC records,
- unsubscribed contacts,
- missing consent,
- unusable phone/email data.
If the intent is only field presence, label it:
“CRM records with contact info”
not “Contactable.”
5. Use one server-side aggregate query if practical.
Do not automatically issue eight sequential COUNT requests.
Use:
- parallel independent scalar queries, or
- one bounded SQL response with scalar subqueries if the query plan is safe.
Provide EXPLAIN/latency evidence for the 1.9M-row Sunbiz count and ensure the endpoint is not polled excessively.
6. Verify existing endpoint ownership before creating /api/leads/stats.
Search for current:
- LCC stats endpoint,
- analytics endpoint,
- prospect summary endpoint,
- contact score distribution endpoint.
Extend the canonical owner rather than creating a second KPI source of truth.
7. Campaign editor tab state must update per selected step.
Do not only initialize useState from step.body once.
Required behavior:
- opening an existing non-empty step → Preview
- opening a new empty step → Write
- switching between steps recalculates the appropriate initial tab
- manually switching tabs while editing the same step is not unexpectedly reset on every render
Use the step ID/change event as the reset boundary.
8. Confirm “body content” correctly handles whitespace and empty HTML.
Treat these deliberately:
- ""
- whitespace only
- "<p></p>"
- "<br>"
- real content
Do not default to Preview for effectively empty content unless that is intended.

Kill lines

STOP if contacts/prospects Hot and Warm counts use unverified string score fields.
STOP if new KPI thresholds differ from the Prospects tab or canonical scoring tier logic.
STOP if “Contactable” bypasses suppression, DNC, consent, or canonical contactability ownership.
STOP if duplicated cross-table records are labeled as unique Total Leads.
STOP if a second stats endpoint duplicates an existing analytics owner.
STOP if the editor tab default only works on first component mount and not when switching steps.
STOP if campaign body storage, sanitization, preview rendering, or send behavior changes.

Validation

- Prospects Hot/Warm values exactly reconcile with the current Prospects tab.
- Contact Hot/Warm use the canonical numeric thresholds and active-record filters.
- Combined totals equal their documented component counts.
- “Total lead records” clearly warns about cross-source overlap.
- Contactable metric matches its exact documented definition.
- Stats endpoint remains isDashboardUser protected.
- Existing populated step opens on Preview.
- Empty new step opens on Write.
- Switching between populated and empty steps resets correctly.
- Manual tab changes are preserved while remaining on the same step.

Final status: PREFLIGHT REQUIRED

The editor UX can likely proceed after a quick call-site check. The LCC KPI work needs exact schema, threshold, contactability, and endpoint-ownership evidence before implementation.