This is an **unfinished implementation**, not a legitimate reason to declare #1731 blocked. Items 1–5 are the core functionality #1731 was created to build.

`origin/main` remains at `773c50d…` / migration `0194`, so the reported `0195` work exists only in Replit’s task workspace and cannot yet be line-reviewed here.

Send Replit this:

---

# Task #1731 — Continue and finish CRO-03C in place

Continue Task #1731 in the current task. Do not create a remediation/follow-up task, do not merge only the authority foundation, and do not mark the task blocked merely because the required dispatcher and provider integration have not yet been implemented.

The architecture review identified unfinished task scope. Complete it now.

## 1. Preserve the completed foundation

Retain:

* Migration `0195_cro03c_live_activation_authority`
* CRO-03C generations, commands, runs and stage dispositions
* Runtime attestations
* Provider economics/reservation structures
* Validation authorizations
* Initial-rollout singleton
* Admin-only routes
* Pinned SafeEgress transport primitive
* Provider-manifest caller restrictions
* Denial-first certification
* `PAUSED / NOT AUTHORIZED`

Do not modify migrations `0188–0194` or historical CRO-03A/B evidence.

If `0195` has already been applied to any persistent development/shared database, do not rewrite it. Add `0196+` for missing constraints or schema.

## 2. Implement the queue-owned CRO-03C dispatcher

Add one canonical queue/job-registry owner for:

* micro-canary execution;
* initial-batch execution;
* cancellation;
* restart/recovery;
* reconciliation.

The worker must claim with:

* `FOR UPDATE SKIP LOCKED`;
* claim token;
* execution fence;
* lease expiry;
* exact command/run/generation/stage identity.

Immediately before reservation and again immediately before I/O, verify:

* active activation revision;
* command/run state;
* command expiration;
* exact release/runtime attestation;
* worker process/boot identity and heartbeat;
* migration and recipe hashes;
* frozen handoff and stage-plan hashes;
* stage eligibility;
* provider control revision;
* circuit and budget;
* current claim/fence;
* cancellation/revocation;
* readable paused-outbound authority.

A lost or unreadable prerequisite denies transport.

## 3. Enforce the dispatch uncertainty boundary

Before I/O:

* create the durable provider operation;
* reserve the exact maximum unit/cost authority;
* create attempt identity;
* record the immediate pre-I/O authorization checkpoint.

After transport begins, a crash or lease expiry must never produce blind replay.

Recovery must classify the operation as:

* completed and reconciled;
* confirmed not dispatched and safely releasable;
* ambiguous billing/result requiring manual reconciliation.

Do not reclaim an uncertain attempt into another provider call.

## 4. Integrate every canonical stage owner

### Internal/registry

* Zero-cost evidence/attempt receipts
* No paid or public transport
* Never represented as fake live provider success

### Public website

Connect the production DNS-pinned transport to one bounded crawl operation:

* robots retrieval;
* homepage plus approved pages;
* maximum five pages;
* redirect/DNS/IP/domain/content/size/time checks;
* durable domain limits;
* per-hop/request receipts;
* terminal operation receipt.

“One crawl operation” may contain multiple bounded HTTP requests. Do not reinterpret it as one HTTP request.

### Serper

* CRO-03C operation exists before the call.
* SerperGateway retains circuit and billing-window authority.
* Link its request/outcome into CRO-03C lineage.
* Do not reserve or charge the same request twice.

### Outscraper and Apollo

* Use the new CRO-03C generation/stage-bound context.
* Do not use the legacy contact-batch context.
* Preserve exact organization matching.
* Settle actual contract-defined units.
* Unknown or ambiguous billing is terminal and quarantined.
* Do not guess provider pricing or credit semantics.

### AI

* Require approved model, prompt version/hash and bounded evidence bundle.
* Reserve token and amount ceilings before invocation.
* Persist actual token/cost settlement and citation-bearing evidence.
* Reject malformed, uncited or over-cap output.

### ZeroBounce

Retain all three command-bound authorization checks:

1. claim;
2. reservation;
3. immediately before I/O.

Also require:

* exact winning-email intent;
* current contact generation and email hash;
* unexpired/unrevoked authorization;
* expected activation and provider-control revisions;
* exact command/run/release identity.

Complete the provider result, evidence, readiness and single scoring-hook flow without creating generic validation work.

## 5. Complete deterministic stage progression

Persist one terminal stage disposition for every stage:

* eligible and completed;
* skipped because evidence is sufficient;
* skipped as not applicable;
* blocked by authority/control/budget;
* failed;
* review required;
* ambiguous.

Skipped stages must create no provider reservation, operation or attempt.

The worker must advance sequentially through the frozen CRO-03B recipe without automatically calling every provider.

Micro-canaries must not project canonical contacts/businesses or create readiness/scoring work. The unique initial batch owns the full evidence → arbitration → projection → validation flow.

## 6. Build causal no-outbound enforcement

Zero-valued snapshots alone are insufficient.

Add a CRO-03C effect fence proving:

* `effect_authorized=false` for all campaign/outbound effects;
* no CR-04 cohort mutation;
* no CR-06 preparation/release;
* no campaign or sequence enrollment;
* no GHL mutation;
* no sender activation;
* no email, SMS or RVM attempt;
* no outbound pause mutation.

Persist:

* pre/post pause epochs;
* run-linked subject/cohort identities;
* correlated forbidden-effect counts;
* relevant global counter observations;
* reconciliation disposition.

If a global counter changes but cannot be causally attributed, stop and mark the run `INCONCLUSIVE_PENDING_RECONCILIATION`. Never falsely attribute unrelated activity or silently pass it.

## 7. Add mandatory disposable certification

Use fresh disposable PostgreSQL and isolated Redis with all external network transports denied.

Cover:

* zero-database migration;
* upgrade from `0194`;
* migration replay;
* schema parity;
* concurrent command creation;
* concurrent stage claims;
* reservation/cap races;
* cancellation and expiry immediately before I/O;
* lease loss;
* restart before dispatch;
* restart after possible dispatch;
* ambiguous reconciliation;
* skipped-stage zero-attempt behavior;
* exact provider settlement;
* current-generation ZeroBounce authorization and revocation;
* one global initial-batch singleton;
* maximum 100 membership;
* admin/manager/agent/public authorization and IDOR;
* CSRF;
* redaction;
* no external network;
* no canonical projection from micro-canaries;
* zero outbound/GHL/campaign/CR-04/CR-06 effects.

Register every new suite in both CI and pre-deploy.

## 8. Separate baseline failures correctly

For the unrelated pre-deploy failures:

* reproduce them against untouched `origin/main` at `773c50d…`;
* fix every failure introduced or affected by #1731;
* record genuinely unchanged baseline failures separately;
* do not use unrelated baseline failures to skip CRO-03C certification.

All mandatory #1731 suites must pass.

## 9. Finish the exact-release runtime path

Once the code, dispatcher and disposable suites pass:

1. Commit and merge the implementation.
2. Deploy that exact reviewed SHA normally.
3. Produce fresh runtime attestations.
4. Run only the applicable provider micro-canaries under persisted caps.
5. Stop at the first failed authority, billing, identity, output or outbound gate.
6. Run the unique initial batch of at most 100 fresh eligible handoffs.
7. Reconcile provider results, costs, projections, ZeroBounce and no-outbound evidence.
8. Schedule no successor batch.

The production command must reject arbitrary IDs, wildcards, `--all`, unbounded values, wrong release/environment and missing approval.

If credentials, pricing contracts or explicit owner approval are unavailable, finish and merge the complete executable code and report only the exact runtime evidence as pending. Do not substitute mocks for live proof.

## Final evidence

Return:

* task-workspace commit and merged commit;
* migration head;
* changed-file inventory;
* queue/job ownership;
* state-transition and stage-owner matrices;
* provider-specific reservation/settlement evidence;
* disposable test totals;
* baseline-failure classification;
* exact release/runtime attestation;
* provider-by-provider canary results;
* unique initial-batch receipt;
* ZeroBounce result;
* no-outbound causal reconciliation;
* confirmation that no successor batch was scheduled;
* outreach status `PAUSED / NOT AUTHORIZED`.

Do not mark #1731 complete until the queue-owned dispatcher and items 2–7 above are implemented. Code may be reported `SAFE TO MERGE — RUNTIME VERIFICATION PENDING`, but full CRO-03C completion requires the exact-release live receipts.

---

The key distinction: lack of production canary evidence can temporarily block **runtime completion**, but it does not block Replit from building the dispatcher, integrating the providers, adding the causal effect fence, or completing disposable certification. Those are unfinished #1731 work and should be completed in the same task.
