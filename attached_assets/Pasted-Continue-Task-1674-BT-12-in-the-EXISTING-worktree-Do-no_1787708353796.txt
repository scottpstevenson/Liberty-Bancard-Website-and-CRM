Continue Task #1674 / BT-12 in the EXISTING worktree.

Do not merge, abandon, reset, revert, split into another task, request another design approval, or merely document the remaining defects. Preserve all completed BT-12 changes and finish the five merge-blocking requirements identified by your final review.

LIVE GITHUB BASELINE
- main remains 93192a21ba4f7142c4194550d6f297274357a8e4.
- The current BT-12 implementation is not yet present on live GitHub.
- Record the current local branch, HEAD, status, and diff before continuing.
- Preserve unrelated files and existing task work.

MODE
PREFLIGHT CORRECTION + BUILD CONTINUATION.

Required sequence:
baseline → inspect existing BT-12 diff and migration → verify new schema contracts → complete dispatchers/authorities → focused concurrency and recovery tests → manifest registration → full gates → post-build searches → complete diff review → final verdict.

P0-1 — COMPLETE DEAL-STAGE EFFECT DISPATCH

The deal-stage effect intents are not complete merely because they are persisted.

Implement a registered durable dispatcher using existing job/lease/outbox conventions:

- Atomically claim pending or retryable stage-effect intents.
- Use immutable intent identity and effect-specific idempotency keys.
- Use lease owner, lease token, lease expiry, heartbeat where execution may be long, attempts, next-attempt time, last error, and terminal status.
- Fence completion and failure updates by the current lease token.
- Recover expired claims after process death.
- Apply bounded exponential retry and an explicit terminal-failure state.
- Register startup/scheduler ownership exactly once per replica-safe authority.
- Do not initialize workers lazily from HTTP routes.
- Do not directly execute the same material effect after persisting its intent.
- Classify each effect:
  - GHL/lifecycle/onboarding/material task creation: durable dispatch required.
  - Nonmaterial analytics/notifications: best-effort only if explicitly classified and truthfully audited.
- Remove or replace competing fire-and-forget material follow-ons from advanceDealStage().
- A successful stage commit must remain recoverable if the process exits immediately afterward.

Required tests:

1. Process dies after intent commit but before dispatch.
2. Lease expires and a second owner recovers it.
3. Stale owner cannot complete after lease loss.
4. Duplicate dispatcher invocation produces one logical external effect.
5. Permanent failure reaches a truthful terminal state.
6. Same-stage replay creates no duplicate material effects.

P0-2 — COMPLETE CHARGEBACK COMMAND EXECUTION

The HTTP route now creates a durable chargeback command, but an undispatched command is not a functioning authority.

Implement and register the canonical chargeback command worker:

- Atomic SKIP LOCKED or equivalent claim behavior.
- Lease token fencing, heartbeat where required, attempt tracking, expiry recovery, bounded retry, terminal failure, and observability.
- Register it through the existing startup/queue/scheduler authority.
- Pass the immutable command UUID as the provider idempotency key whenever the processor supports one.
- Persist request identity and provider response/reference before declaring success.
- Treat network timeout or ambiguous provider response as UNKNOWN/RECONCILE_REQUIRED—not automatically failed and not automatically safe to resend.
- Reconcile ambiguous outcomes using provider lookup/reference capability before retrying a submission.
- Never perform provider calls from the HTTP request.
- HTTP must return the truthful accepted/current-command lifecycle.
- Tests must use a fake provider adapter; do not call a live processor.

Required tests:

1. Duplicate HTTP request resolves to one logical command.
2. Two workers cannot execute the same command concurrently.
3. Crash after provider acceptance but before local completion does not create a second chargeback.
4. Expired lease is recoverable.
5. Stale lease owner is fenced.
6. Provider timeout becomes ambiguous/reconciliation-required.
7. Retry preserves the same provider idempotency key.
8. Permanent failure is visible and auditable.

STOP if the existing 0162 schema cannot represent these guarantees. If an additive correction is required, add the next migration and journal entry; do not rewrite 0162 after it may have been applied.

P0-3 — MAKE A/B TABLES AUTHORITATIVE

The immutable A/B tables cannot coexist with mutable email-log winner authority or competing route writers.

- Inventory every writer of:
  - experiment assignment;
  - cohort membership;
  - send exposure;
  - results;
  - statistical significance;
  - winner selection;
  - winner projection.
- Designate exactly one canonical writer for each state.
- Persist immutable eligibility/cohort/variant/experiment-version evidence before or transactionally with send admission.
- Count only production-classified, approved, immutable exposures.
- Do not infer historical cohort eligibility from mutable current contacts or mutable log fields.
- Route evaluation through one lease-protected canonical worker.
- Select/project a winner with CAS/version protection so simultaneous evaluators cannot choose competing winners.
- Re-evaluation must be idempotent.
- Retire or prohibit generic/manual writes to computed counts, significance, winnerSelected, winnerAt, or winner variant.
- Manager/admin trigger routes may request evaluation but may not fabricate results.
- Worker errors must propagate or persist FAILED/UNAVAILABLE; never return a false successful completion.
- Preserve the difference between “insufficient evidence,” “no winner,” “evaluation failed,” and “winner selected.”

Required tests:

1. Two evaluators race; one authoritative projection wins.
2. Mutable contact/log changes after send cannot alter cohort membership.
3. Manual API caller cannot write winner or computed metrics.
4. Non-production/test/demo recipients are excluded.
5. Failed evaluation cannot report complete.
6. Repeated evaluation produces identical authoritative results.
7. Reset/recompute, if supported, is privileged, audited, and versioned.

P0-4 — REMOVE ALL BINARY-FLOAT MONEY LOGIC IN BT-12 SCOPE

The payout aggregator fix is insufficient while residual import and commission paths still use floats.

Search all reachable residual, statement, commission, payout, adjustment, chargeback, and import paths for:

- parseFloat
- Number(...)
- unary numeric coercion
- multiplication/division of currency values as JS numbers
- toFixed used as a correctness mechanism
- floating SUM/reduction
- cents reconstructed through binary arithmetic

Replace monetary calculations with the existing exact decimal/minor-unit authority.

Define explicitly:

- input normalization;
- supported currency scale;
- rounding mode;
- when rounding occurs;
- negative adjustments;
- null/blank handling;
- overflow bounds;
- reconciliation between imported totals, stored totals, and payout totals.

Do not round every intermediate operand if the business formula requires rounding only at a defined boundary.

Required tests:

1. Fractional-cent inputs.
2. Negative adjustments and reversals.
3. Large batch accumulation.
4. Repeated import/recalculation.
5. Commission split totals reconcile exactly.
6. Import total equals persisted and payout total.
7. No binary-float money arithmetic remains in reachable BT-12 paths.

P0-5 — IMPLEMENT THE COMPLETE BT-12 INTEGRATION SUITE

Create or complete the promised end-to-end BT-12 integration suite. Do not rely only on a static authority guard.

It must exercise production functions with isolated database state and fake providers:

- Generic stage mutation rejection.
- Legal, illegal, stale, concurrent, and idempotent stage transitions.
- Stage-effect intent dispatch, crash recovery, retry, lease fencing, and deduplication.
- Underwriting decision/stage recovery.
- MID legal graph, linkage authorization, concurrency, and audit.
- Chargeback command acceptance, dispatch, ambiguity, recovery, and provider idempotency.
- A/B immutable assignment, exclusion, evaluator concurrency, and authoritative projection.
- Exact residual import, aggregation, adjustment, and payout arithmetic.
- Unresolved template variables fail closed before send.
- Test/demo classification cannot influence production totals or experiment winners.
- Every fixture and hold is correlation-scoped and removed in finally blocks.
- No broad DELETE, TRUNCATE, shared pause mutation, live provider call, or production database fallback.

Register this suite in every CURRENT CI/pre-deploy manifest. Do not create a second manifest or runner.

FULL PRE-DEPLOY FAILURES

Re-run the 14 reported failures and classify each with:

- exact suite;
- exact command;
- exit code;
- first actionable error;
- whether it also fails on untouched main at 93192a21;
- whether BT-12 changed the affected files or behavior;
- task-owned, regression, environment-blocked, or confirmed pre-existing.

Rules:

- Any BT-12-owned failure or regression is a merge blocker.
- Do not label a failure “unrelated” without a main-baseline comparison and file/behavior evidence.
- Do not weaken assertions, skip suites, delete gates, or modify unrelated production behavior merely to make the count green.
- Confirm whether GitHub Actions currently exists and runs the registered manifest; manifest registration alone is not proof of CI enforcement.

POST-BUILD SEARCHES

Prove:

- No persisted stage-effect intent lacks a registered recovery dispatcher.
- No material advanceDealStage follow-on remains fire-and-forget.
- No chargeback provider submission occurs from an HTTP route.
- No chargeback command lacks executable startup ownership.
- No competing A/B winner writer remains.
- No mutable email log is the authoritative cohort source.
- No raw monetary float arithmetic remains in BT-12 scope.
- No unresolved template placeholder can be blank-stripped and sent.
- No second runner, ledger, stage authority, A/B authority, MID authority, or API client was introduced.

REQUIRED GATES

Run and report actual commands and exit codes:

- focused BT-12 integration suite
- BT-12 authority/static guard
- chargeback command recovery suite
- stage-effect dispatcher recovery suite
- A/B concurrency/authority suite
- exact-money/residual suite
- template fail-closed suite
- npm run typecheck
- npm run build
- migration integrity/journal checks
- CI/pre-deploy manifest check
- complete pre-deploy command
- git diff --check

FINAL RESPONSE

Return:

1. Starting and ending branch/SHA.
2. Migration head before and after.
3. Complete changed-file list.
4. Each of the five former blockers with file:line evidence and test proof.
5. Dispatcher registration/startup evidence.
6. Lease, retry, fencing, ambiguity, and idempotency evidence.
7. Complete gate table with commands and exit codes.
8. The 14-failure baseline comparison.
9. Post-build grep proof.
10. Full git status, diff stat, and diff review.
11. Explicit secrets/PII/generated-junk review.
12. Final VFC table.
13. SAFE TO MERGE / SAFE TO MERGE — RUNTIME VERIFICATION PENDING / DO NOT MERGE.

Do not claim completion because ledgers or tables exist. Completion requires registered execution, recovery, single-writer authority, exact arithmetic, and end-to-end regression proof.