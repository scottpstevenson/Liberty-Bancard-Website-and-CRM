Verdict: BUILD-READY WITH CORRECTIONS

The UX fix is valid, but do not copy the Pipeline helper blindly into labeled detail fields.

Corrections to send back

Proceed with these corrections:
1. Reuse the existing Pipeline identity utility if it was extracted into a shared module.
If getDealCardIdentity() still lives inside Pipeline.tsx, move the pure identity logic into a shared client utility and import it from both Pipeline.tsx and Onboarding.tsx.
Do not maintain two independently drifting fallback implementations.
2. Use coordinated identity logic for compact visual surfaces only:
- Onboarding board cards
- drag preview, if one exists
- other stacked primary/secondary displays
Return:
{
  primary: string;
  secondary: string | null;
}
Primary:
full name
→ companyName
→ email
→ phone
→ Deal #${deal.id}
Secondary:
first available companyName/email/phone value that does not equal primary after trim + case-insensitive comparison.
3. Do not render identity.secondary under a field explicitly labeled “Company.”
For the detail panel, resolve labeled fields independently:
Contact:
full name
→ companyName
→ email
→ phone
→ Deal #${deal.id}
Company:
contact?.companyName || "N/A"
Email and phone:
retain their existing canonical values/fallbacks.
4. Use deal.id for the final fallback, never contactId.
5. Audit every Onboarding call site before replacing the helpers. Confirm whether the helpers also affect exports, search, sorting, accessibility labels, or non-card surfaces.
6. Remove “Unnamed contact” and “Company unavailable” only from the Onboarding presentation paths covered by this task. Do not broad-replace unrelated copy across the repository.
7. Add targeted tests for:
- full name + company
- company only
- email only
- phone only
- missing contact record
- duplicate primary/secondary suppression

Kill lines

STOP if email or phone can appear under a detail field labeled Company.
STOP if Onboarding and Pipeline keep separate copies of identical identity logic without a documented reason.
STOP if the final fallback uses contactId instead of deal.id.
STOP if primary and secondary can differ only by capitalization or whitespace.
STOP if changes affect Onboarding workflow behavior, stage movement, enrollment, or backend data.

Validation

- Full name + company → card shows name / company.
- Company only + email → card shows company / email.
- Email only → card shows email with no duplicate secondary.
- Phone only → card shows phone with no secondary.
- No contact data → card and detail Contact show Deal #N.
- Detail Company shows companyName or N/A only.
- No Onboarding surface shows “Unnamed contact” or “Company unavailable.”
- Pipeline identity behavior remains unchanged.

Final status: BUILD-READY WITH CORRECTIONS

Safe to build once the shared-helper ownership and detail-panel field semantics are handled correctly.