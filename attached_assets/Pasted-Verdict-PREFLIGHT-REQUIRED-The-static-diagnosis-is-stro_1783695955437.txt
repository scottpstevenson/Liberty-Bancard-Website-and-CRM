Verdict: PREFLIGHT REQUIRED

The static diagnosis is strong and correctly proves that the proposed stale background_jobs lock fix was based on a false assumption. But the task explicitly says runtime evidence is still missing, so implementation is not yet approved.

There are also four corrections needed in the build plan.

Corrections to send back

The static preflight is accepted. Before implementation, capture and attach the required runtime evidence:
1. Current values for:
   - bulk_classify_progress
   - contact_enrich_batch_progress
   - enrichment_progress, if it exists
2. Deployment logs around the failed run:
   - [BulkClassify]
   - [ContactEnrich]
   - [Re-Enrich]
   - OOM/restart/error events
3. Credential presence in the running production process:
   - SERPER_API_KEY
   - the exact OpenAI environment variable actually consumed by the enrichment code
Then make these plan corrections:
1. Do not clear both progress keys generically during startup without identifying which execution each key represents.
bulk_classify_progress and contact_enrich_batch_progress belong to separate flows. Each stale-state transition must preserve the existing progress payload and add:
{
  ...existingProgress,
  status: "interrupted",
  interruptedAt: <timestamp>,
  interruptionReason: "server_restart"
}
Do not overwrite the record with only status/interruptedAt and discard totals, counters, errors, or timestamps.
2. Put startup reconciliation in one canonical startup service/function.
Do not add ad hoc database mutation near an unrelated migration call. It must execute:
- after database/storage initialization succeeds,
- before enrichment requests are accepted,
- exactly once per process startup.
3. Verify the complete response contract before changing the key.
Confirm OutreachCommand.tsx reads the same nested object and exact fields returned by /api/outreach/status:
- status
- total
- processed
- classified
- emailsFound
- phonesFound
- errors
Patch the writer or response adapter deliberately. Do not blindly add fields to both setSystemSetting calls if one is an initial-state write and the other is a progress/final-state write.
4. Confirm polling exists and remains active after the POST returns 202.
Changing the settings key does not by itself guarantee “real-time” progress. Verify:
- polling interval or refetch behavior,
- polling continues while status === "running",
- polling stops or slows on complete/failed/interrupted,
- mutation isPending is not used as the authoritative running state.
5. Use the exact canonical OpenAI configuration check.
Do not assume AI_INTEGRATIONS_OPENAI_API_KEY is the only supported key merely because it appears in the brief. Reuse the same configuration helper or environment-resolution logic used by the actual OpenAI client.
6. Define all terminal states consistently.
The UI and API must intentionally support:
- idle
- running
- complete
- failed, if already produced
- interrupted
An interrupted run must not be displayed as complete, idle, or currently enriching.
7. Add route-contract tests for the existing authorization boundary.
No new endpoint is being added, but /api/outreach/status now exposes configuration-presence metadata. Verify it remains protected by the existing dashboard authorization and is not publicly accessible.

Kill lines

STOP if implementation starts before the production progress rows and deployment logs are captured.
STOP if startup reconciliation discards existing progress counters or error information.
STOP if startup mutation runs before storage initialization or more than once per startup.
STOP if the UI has no polling/refetch mechanism after the 202 response.
STOP if mutation isPending remains the authoritative source for “Enriching...”.
STOP if the wrong OpenAI environment variable is checked instead of the canonical client configuration.
STOP if “interrupted” is not handled as a distinct terminal state in the UI.
STOP if credential presence is exposed through an unauthenticated endpoint.
STOP if background_jobs or lock-reset architecture is added.

Validation

npx tsx scripts/smoke-role-guards.ts
pnpm typecheck
pnpm lint
pnpm test

Required targeted checks:

- Start a 10-entity run and verify the status endpoint reads bulk_classify_progress.
- Verify processed/classified counters advance through polling after the POST returns.
- Verify completion returns accurate terminal counters.
- Preserve a running progress payload, simulate restart, and confirm all counters remain while status becomes interrupted.
- Verify a new run can start after interrupted state.
- Verify idle/running/complete/failed/interrupted each render correctly.
- Verify credential booleans use the canonical configuration resolution.
- Verify no secret metadata beyond true/false is returned.
- Verify /api/outreach/status remains role protected.

Final status: PREFLIGHT REQUIRED

Once the runtime evidence confirms that production is using the orphaned key/stale settings state described here, the corrected implementation is narrowly scoped and build-ready.