675 — Homepage Recovery Patch: Safety, Mobile Architecture, Hero Control

Mode

Surgical Build Mode.

This is not a full homepage redesign. Do not introduce new sections except small mobile fallback links where required. Do not build the new Services / Processing Identity section in this task.

This task applies the confirmed P0/P1/P2 fixes from Task #674.

⸻

Why

The last homepage pass did not work.

The page still reads too much like a free fee-audit funnel, and mobile is still too long, too oversized, and too aggressive.

This patch fixes the urgent issues first:

1. unsafe testimonial savings claims
2. bright blue sticky CTA
3. oversized mobile hero H1
4. statement-first hero emphasis
5. mobile content bloat
6. standalone sections that should be hidden, merged, or removed on mobile

The net-new Services / Processing Identity section is deferred to a separate task because it requires copy approval.

⸻

Scope

Implement only items T1–T10 from the Task #674 audit.

Do not implement T11 in this task.

⸻

P0 — Safety Fixes

T1 — Testimonial claim safety

File:

client/src/pages/Home.tsx

Target:

mobile testimonial card lines ~1217–1238
desktop testimonial copy lines ~1245+
desktop outcome strip lines ~1303–1320

Remove or soften high-risk claims:

"We were paying Square over $1,100 a month"
"processing cost dropped to nearly zero"
"saving us $340 a month"
"saving over $300 a month"

Use this safer mobile testimonial copy:

We uploaded our statement, and Liberty showed us exactly where our fees were coming from. We switched programs and our monthly processing cost dropped meaningfully. I wish I'd done it two years earlier.

Attribution:

Maria R.
Restaurant Owner · South Miami, FL

Add inline mobile disclaimer inside the testimonial card:

*Individual outcomes vary. Based on documented statement review and merchant setup.

For desktop, remove hard-dollar claims unless the linked case-study pages are confirmed to exist and substantiate them.

If keeping annual outcome figures like $4,200/yr, add inline asterisks and keep the disclaimer visible near the figures.

Kill line:

Stop if mobile testimonial still contains "$340", "$1,100", "nearly zero", or named competitor savings language without substantiation.

⸻

T2 — Sticky mobile CTA color

File:

client/src/components/StickyMobileCTA.tsx

Keep the existing IntersectionObserver trigger logic.

Do not change lines ~60–73.

Change the sticky CTA from bright accent blue to premium navy.

Replace:

bg-accent hover:bg-accent/90

with:

bg-primary hover:bg-primary/90

Do not change destination or tracking.

Kill line:

Stop if the sticky CTA remains bright blue on mobile.

⸻

P1 — Hero Fixes

T3 — Mobile H1 font size

File:

client/src/pages/Home.tsx

Target:

Hero H1 class around line ~185

Current mobile typography is too large:

text-[2.75rem] leading-[0.98] sm:text-5xl

Replace with controlled mobile sizing:

text-2xl sm:text-3xl md:text-4xl lg:text-[3.5rem] leading-tight md:leading-[1.04]

Goal:

* mobile H1 should be no more than 3 lines at 390px
* H1 should not consume half of the first viewport
* preserve strong desktop display scale at lg+

Kill line:

Stop if mobile H1 is still 4 lines or visually dominates the first screen.

⸻

T4 — Hero H1 copy

Current:

Merchant Services Built Around Your Statement

Problem:

The eye-path still lands on “Your Statement,” especially because the accent color highlights it.

Replace with a category-first H1.

Use this exact H1:

Credit Card Processing for Businesses That Deserve Clearer Rates

Badge stays:

Merchant Services · South Florida

Do not highlight “statement” in blue.

If an accent highlight is kept, apply it to:

Credit Card Processing

or:

Clearer Rates

not to “statement.”

Kill line:

Stop if the H1 still visually anchors on "statement."

⸻

T5 — Hero subheadline compression

Current subheadline is too long on mobile.

Replace with:

Liberty Bancard provides merchant accounts, terminal setup, and interchange-plus processing for Florida businesses — starting with a free statement review before you switch.

This keeps category clarity and reduces mobile paragraph length.

Do not remove:

* Upload CTA
* Book CTA
* compliance fine print
* report card visual
* trust row

⸻

P2 — Mobile Content Diet

T6 — Rate Calculator desktop-only

File:

client/src/pages/Home.tsx

Target:

section-calculator around line ~550

Add:

hidden md:block

to the main Rate Calculator section.

Add a mobile fallback immediately where the calculator would have appeared:

<section className="md:hidden section-warm py-12" data-testid="section-calculator-mobile-link">
  <div className="container mx-auto px-4 text-center">
    <p className="text-sm font-semibold text-primary mb-2">Want a quick estimate?</p>
    <h2 className="font-display text-3xl font-bold text-foreground mb-3">
      Run the 60-second rate check
    </h2>
    <p className="text-muted-foreground mb-5">
      Enter your volume and fees to estimate your effective rate before uploading a statement.
    </p>
    <Link href="/estimate" className="btn-primary">
      Open Rate Check
    </Link>
  </div>
</section>

Use the project’s existing Button/Link style conventions instead of inventing new classes if needed.

Kill line:

Stop if the full calculator form still renders on mobile.

⸻

T7 — Terminal Showcase desktop-only

Target:

section-terminal-showcase around line ~965

Add:

hidden md:block

to the main terminal showcase section.

Do not delete the section.

Do not change desktop terminal content.

Mobile will get terminal/POS context later in the separate Services / Processing Identity section.

Kill line:

Stop if terminal gallery + setup cards still render as a long mobile section.

⸻

T8 — Merge Savings Guarantee into Risk Reversal

Current standalone Savings Guarantee section is too late and too large on mobile.

Target:

section-savings-guarantee around line ~1164
section-risk-reversal around line ~1339

Move the useful message:

If the math doesn't support switching, we'll tell you.

into the Risk Reversal section as a paragraph or compact bullet group.

Then remove the standalone Savings Guarantee section from mobile and desktop, or hide it if removal risks layout references.

Preferred:

* remove standalone section-savings-guarantee
* preserve any compliance-safe copy inside Risk Reversal
* preserve data-testid only if analytics requires it; otherwise note removal in final report

Kill line:

Stop if the green Savings Guarantee card remains a full standalone mobile section.

⸻

T9 — Remove Security Badges as standalone homepage section

Target:

section-security-badges around line ~1373

Remove the standalone section from Home or move the badge names to the footer if there is an existing safe footer badge area.

Do not create a new large card section.

Do not remove actual compliance/security claims from the site entirely if they are needed elsewhere.

Kill line:

Stop if Security Badges still render as a standalone mobile homepage section.

⸻

T10 — Compress / remove No Lock-In section

Target:

section-no-lockin around line ~1547

Read the exact content before editing.

Fold the useful copy into:

* FAQ, or
* Risk Reversal, or
* Final CTA microcopy

Then remove the standalone No Lock-In section.

Kill line:

Stop if No Lock-In remains a full standalone mobile section before Final CTA.

⸻

Deferred

Do not build this in Task 675:

T11 — Services / Processing Identity section

Reason:

It is necessary, but it is net-new content and needs copy approval.

Next task should be:

676 — Homepage Services / Processing Identity Section

⸻

Protected Invariants

Do not change:

* upload CTA destination
* upload CTA tracking
* book CTA destination
* book CTA tracking
* phone constants
* form behavior
* upload behavior
* booking behavior
* GHL
* analytics event names
* contactability
* backend/API
* dashboard pages
* StickyMobileCTA IntersectionObserver trigger logic
* data-testid attributes unless explicitly reporting removed sections
* compliance fine print around claims

⸻

Validation

Required screenshots:

Mobile 390px

1. Hero first screen
2. Hero after CTAs
3. Sticky CTA after scroll
4. Rate calculator replacement link
5. Terminal section absent on mobile
6. Risk Reversal with merged “math doesn’t support switching” language
7. Testimonial card with safe copy and inline disclaimer
8. Final CTA

Desktop 1440px

1. Hero
2. Rate Calculator still visible
3. Terminal Showcase still visible
4. Risk Reversal
5. Reviews/testimonials
6. Final CTA

Required checks:

grep Home.tsx for "$340" → 0 mobile-visible unsafe matches
grep Home.tsx for "nearly zero" → 0 matches
grep Home.tsx for "Merchant Services Built Around Your Statement" → 0 matches
grep Home.tsx for "section-calculator" confirms desktop-only plus mobile fallback
grep Home.tsx for "section-terminal-showcase" confirms hidden on mobile

Run:

npx tsc --noEmit --skipLibCheck
npx tsx scripts/smoke-role-guards.ts

⸻

Final Report Required

Return:

1. files changed
2. testimonial copy before/after
3. sticky CTA color change
4. hero H1 before/after
5. hero mobile typography class before/after
6. subheadline before/after
7. mobile sections hidden/removed
8. Rate Calculator mobile fallback link
9. Terminal desktop-only confirmation
10. Savings Guarantee merge location
11. Security Badge handling
12. No Lock-In handling
13. CTA/tracking preservation
14. data-testid changes, if any
15. screenshots captured
16. tests run
17. remaining risks
18. recommended Task 676 scope

⸻

Success Definition

Task 675 succeeds when:

The homepage no longer feels like a giant mobile fee-audit funnel.

Specifically:

* mobile H1 is controlled and category-first
* sticky CTA is navy and premium
* unsafe testimonial savings claims are removed
* long mobile-only clutter is reduced
* Rate Calculator and Terminal Showcase no longer consume multiple mobile screens
* Risk Reversal absorbs the useful guarantee message
* desktop still preserves the calculator and terminal sections
* no backend, form, analytics, or CTA behavior regresses