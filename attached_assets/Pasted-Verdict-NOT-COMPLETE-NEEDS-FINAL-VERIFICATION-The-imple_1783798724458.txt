Verdict: NOT COMPLETE / NEEDS FINAL VERIFICATION

The implementation direction is right, but the action log does not prove the critical safety properties actually hold.

Corrections to send back

Provide a completion report with exact file:line evidence for:
1. Preview persistence
- campaign_previews schema
- migration file and journal entry
- previewId generation
- campaignId, operator, status, targeting snapshot/hash, counts, timestamps, expiry
- startup running → interrupted reconciliation
2. Queue enforcement
- queue endpoint requires previewId
- rejects missing previewId
- rejects incomplete, failed, interrupted, expired, wrong-campaign, or already-consumed previews
- rejects when current campaign targeting/template version differs from preview
- prevents two concurrent requests from consuming the same preview
3. Atomic consumption
Preview validation and consumption must be atomic.
Two simultaneous queue requests using one previewId must produce:
- one successful queue execution
- one conflict/rejection
A read-then-update sequence without a transaction or conditional UPDATE remains race-prone.
4. Preview/apply parity
Prove queueing uses the frozen preview criteria or audience snapshot.
A targeting hash alone only detects campaign changes. It does not freeze:
- contact vertical changes
- new contacts entering the vertical
- contacts leaving the vertical
- contactability changes
Clarify the intended contract:
A. Exact snapshot:
queue only the contact IDs approved in the preview.
or
B. Criteria snapshot:
rerun the same frozen criteria, with contactability rechecked at queue time.
If using B, the queued count may differ from the preview and the API/UI must report that clearly.
5. Template/version parity
Confirm the preview invalidation hash includes every field that materially affects the confirmed operation:
- target mode
- verticals
- campaign steps/template version
- sender configuration if relevant
- filtering criteria
- daily limits if part of approval
6. Restart behavior
Show that startup reconciliation:
- marks only running previews interrupted
- preserves counts and diagnostic fields
- cannot block server startup
- does not interrupt previews owned by another live process, if multi-process execution is possible
7. Error sanitization
Confirm persisted preview errors and API responses do not expose raw SQL, provider responses, stack traces, hostnames, or secrets.
8. Migration validation
Provide:
- migration filename
- pg table definition
- indexes
- foreign keys
- actual applied migration-history row
- rollback SQL/strategy
Do not rely only on “table exists.”

Required targeted tests

- Queue without previewId → rejected.
- Running/failed/interrupted/expired preview → rejected.
- Preview for campaign A used on campaign B → rejected.
- Campaign edited after preview → rejected.
- Two concurrent queue requests consume one preview exactly once.
- Restart converts running preview to interrupted.
- Preview owner/operator rules are enforced.
- Contactability is rechecked immediately before queue insertion and again before send.
- Vertical aliases and multi-vertical filtering match preview and queue behavior.
- Queue result reports any difference between preview eligible count and actual queued count.

Validation evidence required

npx tsc --noEmit
npx tsx scripts/smoke-role-guards.ts
npx tsx scripts/check-api-coverage.ts
npx tsx <vertical-semantics-test>

Also include the exact pass totals and any pre-existing failures separately.

Final status: NOT COMPLETE / NOT VERIFIED

The largest remaining question is whether preview consumption is atomic and whether queueing uses an exact audience snapshot or merely reruns live criteria. Those determine whether the server-side gate actually prevents bypasses and audience drift.