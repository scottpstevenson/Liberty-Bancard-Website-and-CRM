You’re right. I audited the attached **Task #1731 / CRO-03C**. My previous response was the wrong deliverable.

## Audit verdict

**BUILD-READY WITH BLOCKING CORRECTIONS.**

The overall architecture is correct, and #1731 should remain the single CRO-03C task. Do not create or use #1732. However, the plan cannot be implemented safely as currently written because it assumes live execution can resume CRO-03B records that are already immutable and terminal.

Verified baseline:

* `origin/main`: `773c50d13584578045026c5923b59ff5c7994a22`
* Migration head: `0194_cro03b_validation_execution_fence`
* Journal: 199 entries; last index `198`
* CRO-03A and CRO-03B are merged
* Live provider transport remains disabled
* Current CRO-03 suites are registered in pre-deploy and CI

Send Replit this consolidated correction addendum.

---

# Task #1731 — CRO-03C blocking corrections

Continue #1731 as one task. Do not split it. Apply every correction below before implementation.

## 1. Do not resume immutable provider-denied CRO-03B items

The current CRO-03B worker permanently creates:

* completed `certification_transport_denied` stage executions;
* immutable stage operations, attempts and receipts;
* immutable disabled evidence;
* immutable field decisions;
* one stage operation per item/stage;
* one field decision per item/field;
* one finalization receipt per item.

This is enforced in [admission-service.ts](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/server/services/cro03/admission-service.ts), [0190](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/migrations/0190_cro03b_recipe_evidence_projection_binding.sql), and [0192](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/migrations/0192_cro03b_denied_stage_accounting.sql).

Therefore:

* Never overwrite, reopen, delete, supersede or reinterpret those denied rows.
* Existing denied items are not eligible for live execution.
* Do not flip `CRO03_PROVIDER_TRANSPORT_ENABLED` to `true` in the legacy factory.

Add an immutable CRO-03C live execution generation. A live command may create/link a fresh CRO-03B execution item only from an eligible CRO-03A handoff that has not already been admitted for that recipe.

Freeze:

* CRO-03A handoff ID/hash;
* CRO-03B recipe version/hash;
* execution mode `cro03c_live_v1`;
* activation revision;
* release/runtime attestation;
* stage-plan hash;
* command/run identity.

The normal provider-denied worker must exclude live-owned generations. The CRO-03C worker must be the sole claimant of those items.

## 2. Separate micro-canaries from canonical projection

Provider micro-canaries must prove real transport, parsing, identity matching, cost and reconciliation without contaminating canonical CRM data.

Micro-canary mode must:

* use exact approved targets;
* persist live operation/evidence receipts;
* perform no contact/business projection;
* create no readiness, scoring, CR-04, CR-06, campaign, GHL or outbound effects.

The one initial batch is what exercises the complete:

```text
live evidence
→ arbitration
→ reviewed/local projection
→ winning-email intent
→ ZeroBounce
→ readiness/scoring
```

flow.

## 3. Persist deterministic stage eligibility

The frozen recipe lists every stage, but it does not authorize calling every provider for every handoff.

Before transport, persist a stage-plan decision for every stage:

* `eligible`
* `skipped_sufficient_evidence`
* `skipped_missing_anchor`
* `skipped_not_applicable`
* `blocked_control`
* `blocked_budget`
* `blocked_authority`
* `review_required`

A skipped stage creates no provider operation, reservation or attempt.

Serper, Outscraper, AI, Apollo and ZeroBounce must run only for a justified unresolved gap. The initial batch must not automatically fan every member through every provider.

## 4. Create CRO-03C-specific provider contexts

The existing Apollo/Outscraper context in [provider-context.ts](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/server/services/cro03/provider-context.ts) is bound to legacy `cro03_enrichment_items`, not CRO-03B recipe items.

Do not reuse or broaden it.

Create a `Cro03cLiveProviderContext` bound to:

* activation revision;
* live execution generation;
* command and run;
* CRO-03B item and stage;
* stage claim token/fence;
* provider operation;
* exact release attestation;
* expiry;
* no-outbound snapshot hash.

Update [provider-manifest.ts](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/server/services/provider-manifest.ts) with only the exact new approved callers. Credentials or `explicitPaidApproval: true` alone must remain insufficient.

## 5. Make provider economics authoritative

Current generic controls store integer units but do not fully describe provider-specific billing.

Persist:

* provider and operation type;
* unit type;
* currency;
* price-schedule version/hash;
* reserved maximum units;
* reserved maximum amount;
* settled units;
* settled amount;
* provider receipt reference;
* billing certainty;
* terminal reconciliation disposition.

Do not guess Apollo credits, Outscraper billable results or provider prices. Missing or unknown commercial terms must block that provider.

Ownership must remain:

* Serper: SerperGateway budget/circuit, linked into CRO-03 lineage without double-reservation.
* Outscraper/Apollo: provider operation and ledger.
* AI: AI token/cost authority.
* ZeroBounce: validation worker and provider controls.
* SafeEgress/internal: zero-cost receipts.

Legacy usage estimates must not become settlement authority.

## 6. Strengthen ZeroBounce authorization

Migration [0194](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/migrations/0194_cro03b_validation_execution_fence.sql) adds only a timestamp and authority string. That does not provide the command binding, expiry or revocation promised by #1731.

Add an immutable validation-authorization record containing:

* validation-intent ID;
* command/run/generation IDs;
* activation revision;
* contact ID;
* normalized-email hash;
* subject generation;
* release attestation;
* expected provider-control revision;
* unit/cost cap;
* authorized and expiration timestamps;
* revocation event/state.

The validation worker must join and revalidate this authority:

1. when claiming;
2. immediately before reservation;
3. immediately before provider I/O.

Only `purpose='cro03_winning_email'` is eligible. Generic marketing intents and stale generations remain denied.

Update the resulting provider operation purpose from generic `marketing_outreach` to the exact CRO-03 winning-email purpose.

## 7. Correct release/runtime attestation

The repository has one `RELEASE_SHA` plus queue process identity and topology evidence. Do not invent separate web and worker SHAs when they run from one artifact.

Persist:

* one exact artifact SHA;
* migration head;
* deployment/environment identity;
* separate web and worker process/boot identities;
* queue-topology/configuration hash;
* worker heartbeat;
* DB/Redis health;
* captured time and expiry.

If workers are independently deployed, attest each actual artifact SHA. Every live stage must refresh the relevant process/runtime evidence immediately before I/O.

## 8. Correct SafeEgress call semantics

[SafeEgress](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-audit-1731/server/services/cro03/safe-egress.ts) currently defaults to a denied transport.

CRO-03C must install one approved production transport that preserves DNS-address pinning. Generic `fetch()` after DNS validation is not acceptable.

“One bounded public-web call” must mean one claimed crawl operation—not literally one HTTP request. A crawl operation may contain:

* robots retrieval;
* homepage;
* up to four additional approved pages;
* bounded redirects.

Persist per-request/hop receipts while enforcing the five-page recipe maximum.

## 9. Freeze approval and stop policies

The legacy descriptive canaries require operator/data/finance/legal approvals, while #1731 refers to one admin approval. Neither is currently executable authority.

Add a versioned activation-approval policy. Do not silently convert the four historical labels into a single admin approval.

Also freeze provider-specific:

* transport-success requirements;
* valid `no_result` treatment;
* minimum sample/yield threshold;
* maximum consecutive failures;
* malformed-output threshold;
* identity-conflict threshold;
* cost/unit ceiling;
* cancellation and expiry behavior.

A legitimate provider `no_result` is not automatically a transport failure. “Zero yield” and “two failures” must have exact denominators and definitions.

## 10. Enforce one initial batch globally

The plan currently says “one batch per approved activation revision.” Multiple activation revisions could therefore create multiple initial batches.

Add a production database singleton such as:

```text
rollout_key = cro03c_initial_v1
```

with a global unique constraint.

The batch must:

* contain 1–100 fresh eligible handoffs;
* have immutable membership and cohort hash;
* run only after every applicable canary passes;
* be incapable of replaying transport;
* schedule no successor;
* create no recurring job;
* remain unique even after cancellation, failure or activation revision changes.

Continuous production scheduling belongs to CRO-08A.

## 11. Make no-outbound evidence causal

Keep pre/post global snapshots, but do not claim any unrelated system counter movement was caused by CRO-03C.

Prove both:

* hard code/database fences prevent CRO-03C-linked GHL, campaign, enrollment, sender, email, SMS and RVM effects;
* run-linked forbidden-effect counts remain zero.

If a global counter changes during a run, stop and mark the run `INCONCLUSIVE_PENDING_RECONCILIATION` unless correlation proves CRO-03C caused it. Never mutate the global/channel pause from this task.

## 12. Tighten certification and completion rules

Add tests proving:

* historical denied rows remain byte-for-byte immutable;
* denied handoffs cannot be reused for live execution;
* fresh live generations cannot be claimed by the denial worker;
* canaries never project canonical data;
* skipped stages create zero provider attempts;
* release/authority loss immediately before I/O prevents transport;
* concurrent commands respect provider and run caps;
* ZeroBounce authorization expires and revokes correctly;
* one initial batch exists globally;
* restart after possible dispatch produces ambiguous reconciliation, never duplicate I/O;
* provider-specific settlement balances exactly;
* all CRO-03C-linked outbound-effect counts remain zero.

Register the suites in CI and pre-deploy and certify:

* empty database migration;
* prior-head `0194` upgrade;
* migration replay;
* schema parity;
* disposable PostgreSQL and isolated Redis;
* public-network denial with realistic secret variables.

## Final task-state rule

The implementation may be merged as:

```text
SAFE TO MERGE — RUNTIME VERIFICATION PENDING
```

But **Task #1731 / CRO-03C must not be called complete** until exact-release receipts prove:

* each applicable real provider micro-canary;
* real result persistence and accounting;
* the single maximum-100 initial batch;
* ZeroBounce current-generation execution;
* zero outbound effects.

Missing credentials, pricing contracts or owner approval may legitimately leave runtime activation pending. They do not justify mocks being reported as live proof.

---

That is the complete correction set. The plan does not need another task split or a broad redesign; it needs these authority and lifecycle corrections inserted into #1731 before Replit builds it.
