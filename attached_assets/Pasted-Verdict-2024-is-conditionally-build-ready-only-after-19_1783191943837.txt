Verdict: #2024 is conditionally build-ready — only after #1984A is merged and its return contract is confirmed.

The scope is right and much safer than the original #1984. This should be the migration task for the four exact-domain public intake callers.

Add this final dependency block before dispatch:

#2024 dependency
Do not start #2024 until #1984A is merged and routeIntake supports:
companySelectionMode: "exact_domain_authoritative_advisory_dedupe"
exactDomainCompanyId?: number | null
And returns a usable resolvedCompanyId / companyId for downstream rich field writes.
If #1984A is not merged, stop.

One wording correction:

Do not remove the exact-domain lookup unless #1984A moved that lookup fully inside routeIntake.
The important removal is:
- inline fuzzy dedupe decision logic
- duplicated advisory-dedupe handling
- duplicated routing/audit/warm-handoff/task logic
But the route may still need to compute/pass exactDomainCompanyId using the same clean-domain logic, depending on #1984A's final API.

Add these kill lines:

Do not let fuzzy dedupe choose companyId for these four routes.
Do not replace existing rich route-specific db.update/db.insert field writes with ingestInboundLead's narrow generic patch.
Do not double-fire warm handoff or deterministic task creation.
If routeIntake now owns those, remove/skip duplicate route-local calls only after verifying equivalent behavior.
Do not touch authenticated portal routes.
Do not touch /signup or /register.
Do not touch public-rfi.ts.
Do not change publisherLiveApprovalStatus or demandLiveApprovalStatus.
Do not change UI/UX, validation, success responses, attribution capture, or launch-gate behavior.

Tighten “Done looks like” with this:

For each migrated route:
- exact-domain match returns the same companyId as before migration
- brand-new domain creates/writes a new company
- near-duplicate different-domain candidate is advisory only
- rich fields written before the migration are still written after migration
- contact writes are preserved
- audit escalation still appears for near-duplicates
- routeIntake decision logging appears
- warm handoff fires once
- deterministic tasks fire once

Final approval language:

#2024 — Migrate Exact-Domain Public Intake Callers to Router: BUILD-READY AFTER #1984A
Approved scope:
- partner-onboarding.ts /onboard/publisher
- partner-onboarding.ts /onboard/demand
- publisher-portal.ts /quiz
- demand-portal.ts /quiz
Not approved:
- authenticated portal routes
- /signup
- /register
- public-rfi.ts changes
- fuzzy-dedupe-selected companyId
- replacing rich field writes with generic intake patch

Final status:

#2024: BUILD-READY, dependency-gated on #1984A merge