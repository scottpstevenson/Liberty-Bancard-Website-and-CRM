Verdict: YES — UPDATE TASK #989

The preflight is complete and materially improves the task. No additional architecture round is needed.

Send this back:

BUILD-READY WITH VERIFIED CORRECTIONS — Task #989

Update the task using the completed preflight.

Exact scope

Harden only the exact-phone matching tier in findMatchingMerchant().

Do not modify the existing fuzzy-name-only tier at lines 295–351.

Required phone-tier behavior

1. Remove .limit(1) from the exact-phone candidate query.
2. Fetch every merchant sharing the normalized phone number, including the fields needed for corroboration:

* merchant ID
* business/legal name
* state
* any existing confirmed identity fields already available to this importer

3. Score every candidate through one pure typed helper.

Required result shape:

type RegistryPhoneCandidateScore = {
  merchantId: number;
  score: number;
  basis: RegistryMatchBasis[];
  contradictions: RegistryMatchContradiction[];
  corroborated: boolean;
};

Use stable reason codes such as:

* phone_exact
* name_strong
* name_moderate
* name_weak
* name_missing
* state_same
* state_different
* state_missing
* name_conflict

Do not store prose as matchBasis.

4. Exact phone alone must never authorize a merchant update.

Require corroboration through compatible business identity.

A strongly conflicting business name must either:

* disqualify the candidate; or
* apply a documented penalty that prevents a phone-only acceptance.

Missing name is not a contradiction.

5. Rank all candidates deterministically:

* score descending
* merchant ID ascending only as a stable ordering mechanism

The ID tiebreak must never turn an ambiguous match into an accepted match.

6. Apply the ambiguity rule:

best.score >= REGISTRY_MATCH_THRESHOLD
best.score - secondBest.score >= REGISTRY_MATCH_MARGIN
best.corroborated === true
best has no disqualifying contradiction

Otherwise return:

* ambiguous when candidates are too close;
* low_confidence when evidence or corroboration is insufficient;
* fall through to the existing fuzzy-name tier when no exact-phone candidate exists.

Only accepted may update sdrMerchants.

Provisional constants

const REGISTRY_MATCH_THRESHOLD = 60;
const REGISTRY_MATCH_MARGIN = 15;
const REGISTRY_MATCH_ALGORITHM_VERSION = "v2";

These values remain provisional until the required historical distribution report is reviewed.

Schema and logging

Add to registry_import_log:

* matchConfidence integer
* matchBasis jsonb
* contradictions jsonb
* runnerUpMerchantId integer nullable
* runnerUpConfidence integer nullable
* matchAlgorithmVersion text

Populate these fields for accepted, low-confidence, and ambiguous phone-tier outcomes.

Continue logging existing matched, unmatched, and skipped outcomes.

Do not change unrelated status behavior.

History endpoint

Update the raw aggregate query in server/routes/registry-import.ts so the history response includes:

* matched
* unmatched
* skipped
* lowConfidence
* ambiguous

Do not allow the new statuses to disappear from totals.

Import summary

Extend ImportSummary with:

* lowConfidence
* ambiguous

Ensure all outcome counts reconcile to processed rows.

Required production distribution check

Before finalizing the threshold and margin, run the phone-tier scorer read-only against a representative historical sample and report:

* accepted count
* low-confidence count
* ambiguous count
* percentage of historical phone-only matches now blocked
* sample phone matches with incompatible names
* sample close runner-up cases

If the distribution is pathological, stop and adjust the constants before shipping.

Do not write any merchant changes during this analysis.

Kill lines

* STOP if .limit(1) remains in the phone query.
* STOP if only the first candidate is scored.
* STOP if exact phone alone can update a merchant.
* STOP if contradictory names have no negative effect.
* STOP if close candidates are resolved by row order or merchant ID.
* STOP if rejected outcomes mutate sdrMerchants.
* STOP if matchBasis is stored as arbitrary prose.
* STOP if the history aggregate omits the new statuses.
* STOP if the existing fuzzy-name-only matching tier is changed.
* STOP if threshold 60 and margin 15 ship without the distribution review.

Validation

All 12 preflight test cases must pass, including:

* conflicting-name phone match rejected;
* strong-name phone match accepted;
* shared-phone candidates produce ambiguity;
* margin-cleared winner accepted;
* missing name treated as missing, not contradictory;
* fuzzy-name-only behavior unchanged;
* no merchant mutation for rejected outcomes;
* typed evidence persisted;
* history totals include both new statuses.

Final status: BUILD-READY

Have the executor update the task and implement it. The next response should be the production-sample distribution results or completed code, not another generic preflight.