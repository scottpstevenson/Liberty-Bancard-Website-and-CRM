Final Addendum to #736 — Safe Validation Environment

Approved to run after these dependencies are merged:

#733 Fix Statement Chain Step 10 False Failure
#734 Surface Real Processor Data in A-Lead Queue
#735 Fix Discovery Source Cost Analytics

Required preflight before running commands

Before executing the validation suite, confirm:

DATABASE_URL points to the intended test/staging database, not production
TEST_MODE=true where supported
SKIP_AI=true where supported
DRY_RUN=true where supported
No live paid discovery source is called
No real email/SMS/voice/ringless/GHL workflow can fire

If the environment cannot be confirmed safe, stop and report:

Validation suite not run — safe test environment not confirmed.

Recommended command environment

Run the suite with safe flags where applicable:

TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsc --noEmit --skipLibCheck
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/smoke-role-guards.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-sequence-compliance.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-contactability.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-sdr-manual-enroll.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-channel-audit.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-discovery-vertical-mapping.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-statement-analyzer.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-sales-prep.ts
TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-discovery-activation.ts

If any script has its own documented mock flag, use that too.

Reporting requirement

Return this exact table:

Command | Exit Code | Result | Notes | Follow-up required?

If all pass:

Pilot validation suite passed. Ready for controlled pilot under defined limits.

If any fail:

Not ready for pilot until these failures are fixed.

Then list one follow-up task per failure.

Kill lines

STOP if any command fails and the report still claims pilot-ready.
STOP if tests run against production data without explicit approval.
STOP if a test calls live paid discovery sources.
STOP if a test sends or queues real email, SMS, voice, ringless voicemail, GHL workflow, or sequence enrollment outside mock/test mode.
STOP if a test flips discovery/channel env flags.
STOP if failures are fixed inside this task instead of reported as follow-up tasks.