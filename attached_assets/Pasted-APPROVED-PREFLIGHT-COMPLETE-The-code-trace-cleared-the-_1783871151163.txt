APPROVED — PREFLIGHT COMPLETE

The code-trace cleared the remaining uncertainty. Task #993 is build-ready once the brief is corrected to match the real routes and backend behavior.

Send this back:

BUILD-READY — use the verified contracts below

Update Task #993 before implementation:

Correct endpoint ownership

Use:

* GET /api/contacts/readiness-stats
* POST /api/admin/readiness-backfill/start
* GET /api/admin/readiness-backfill/status

Do not create new /api/campaigns/readiness-* endpoints.

Authorization already exists:

* Readiness stats: isDashboardUser
* Backfill start/status: admin or manager

Global panel behavior

This panel is global and must not display a supposed system-wide campaign threshold.

Do not show:

Campaign threshold currently set to X

Instead, show readiness distribution and optionally a local, display-only threshold estimator. Label it:

At or above selected threshold

Also state:

Readiness does not determine permission to contact. Campaign contactability checks still apply.

The estimator must not modify any campaign.

Stats contract

Use the existing readiness stats response, including:

* A/B/C/D/F counts
* average score
* null_score
* stale_model
* mutation_stale
* top missing reasons
* latest backfill status

Grade percentages use only scored contacts as the denominator:

A + B + C + D + F

Do not include null or stale counts in grade percentages.

Show null, stale-model, and mutation-stale counts separately.

Shared readiness contract

Import labels and types from:

shared/readiness-types.ts

Use:

* READINESS_REASON_LABELS
* humanizeReasonCode()
* ReadinessBreakdownShape
* existing readiness component metadata

Do not create another client-side reason-label map.

Backfill controls

Show Run Backfill when any of these are greater than zero:

* null score
* stale model
* mutation stale

Admin and manager roles may start the backfill. Agents see the panel read-only.

The backend already supports safely fenced force restart through:

POST /api/admin/readiness-backfill/start

with:

{ "force": true }

A Force Restart control is allowed, but it must:

* appear only while a run is active;
* require a confirmation dialog;
* call the existing endpoint;
* never mutate run rows directly from the UI.

When the backend returns HTTP 202 indicating a run already exists, treat that as reconciliation with the active run and begin polling. Do not show a generic failure.

Polling

Poll every 10 seconds only while the latest run status is:

running

Stop polling immediately for:

* complete
* failed
* interrupted
* idle / absent

No continuous 30-second polling is needed when no run is active.

Loading and failure states

While loading, show a skeleton.

If the endpoint fails, show an explicit error state with retry.

Never render missing response data as real zero counts.

Remove false scheduling copy

There is no nightly BullMQ readiness backfill.

Do not mention automatic nightly backfill anywhere in the UI or task report.

The existing automatic behavior is only per-contact readiness recalculation after relevant contact mutations. Full backfill is currently operator-triggered.

Verified file ownership

Modify:

* client/src/pages/dashboard/Campaigns.tsx
* reusable components under client/src/components/

Read only unless a verified response field is actually missing:

* server/routes/campaigns.ts
* existing readiness services and routes

Do not create client/src/pages/Campaigns.tsx.

Final status

Task #993: BUILD-READY.

The executor already completed the necessary preflight. It should now implement the panel rather than creating another planning task.