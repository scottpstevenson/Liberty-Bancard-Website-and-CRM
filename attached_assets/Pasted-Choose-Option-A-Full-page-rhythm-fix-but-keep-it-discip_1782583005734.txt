Choose Option A — Full page rhythm fix, but keep it disciplined.

The issue is now clearly global section rhythm, not just the hero. #677 should cover the full homepage background/section treatment architecture in one plan.

Important constraint: do not solve this by blindly increasing bg-dots / bg-grid opacity everywhere. The goal is not “more pattern.” The goal is clearer visual chaptering.

Scope #677 as a full-page rhythm repair with these priorities:

1. Hero/top proof stack gets a real structure:
    * clean hero
    * intentional trust bridge
    * distinct sample review block
    * no floating rows all on the same white canvas
2. Trust strip must become a real band:
    * not bg-card that reads identical to white
    * use visible border/tint/compact band treatment
3. Choose Path must stop sitting directly after mobile calculator as another section-warm section:
    * give mobile a distinct treatment or separator
    * avoid two warm sections in a row
4. Verticals → Why Liberty must not be two invisible-white sections in a row:
    * one of them needs a different treatment, stronger border, tinted background, or dark/warm reset
5. Pattern backgrounds need purpose:
    * if dots/grid remain, they must be visibly intentional but subtle
    * if they still read as invisible white, replace with a real section treatment
    * if they read as graph paper, remove them
6. Callback / lower page rhythm should be checked too:
    * do not leave bg-grid at 3% opacity pretending to be a section break

This should still be a visual architecture task, not a content rewrite. Do not change backend, CTAs, tracking, forms, testimonials, upload/booking flow, StickyMobileCTA trigger logic, or the new Services section copy.

Before building, return a revised #677 plan with:

* current full mobile section sequence
* proposed new section treatment sequence
* exact sections changing
* mobile vs desktop differences
* which backgrounds/classes change
* which pattern opacities are removed vs adjusted
* screenshots required
* kill lines

Kill line: if the final page still has consecutive white/near-white sections that visually blend together, the task fails.