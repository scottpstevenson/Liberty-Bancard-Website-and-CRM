Verdict: BUILD-READY WITH CORRECTIONS

This is a reasonable UI task and should stay small. A few assumptions need tightening before Replit builds it.

Corrections to send back

1. Verify the actual contact-detail file path.
The known Liberty path has previously been:
client/src/pages/dashboard/ContactDetail.tsx
Do not create or modify client/src/pages/ContactDetail.tsx unless that is the confirmed route owner.
2. Verify the existing GET /api/contacts/:id response shape.
Confirm it actually includes:
- dataReadinessScore
- dataReadinessGrade
- readinessBreakdown
- readinessUpdatedAt
- lastMeaningfulContactMutationAt
If lastMeaningfulContactMutationAt is not returned or does not exist, do not invent it client-side.
Use the canonical stale signal already established by Phase 2, such as:
- readiness model-version mismatch;
- readinessUpdatedAt older than the contact’s relevant updated timestamp;
- or an explicit stale flag returned by the API.
3. Reuse shared readiness types and reason codes.
Do not independently duplicate the JSONB shape in the component.
Import the existing Phase 2 readiness types/constants if available. Add a shared
client-safe label map beside those types rather than creating a second untyped contract.
Unknown future reason codes must render safely using a humanized fallback rather than
crashing or disappearing.
4. Do not hardcode field rows independently of the persisted breakdown.
Render the components present in readinessBreakdown.components using a stable display
order:
email
company
vertical
phone
geo
name
If a component is missing because an older model version produced the record, show it
as unavailable rather than treating it as zero.
5. Use grade color as supporting information only.
The grade and score must remain understandable without color:
- visible grade letter;
- numeric score;
- earned/max values;
- status icon and text.
Use existing design tokens or semantic component variants where possible. Do not add a
one-off rainbow palette that conflicts with the dashboard visual system.
6. Correct the null-state destination.
Do not link to a “Campaigns readiness stats panel” unless Task #975 is confirmed shipped
and the exact route/anchor exists.
If that surface is not live, show:
“Not yet scored. Readiness will be calculated by the active readiness backfill.”
No dead link.
7. Do not call the warning “Stale” unless the condition is authoritative.
Preferred labels:
- “Update pending” when recalculation is queued;
- “Score may be outdated” when inferred only from timestamps;
- “Stale model version” when the model version mismatches.
Do not imply certainty from an approximate timestamp comparison.
8. Keep the component display-only.
No recalculation mutation, direct JSON editing, campaign eligibility decision, or
contactability inference belongs in this component.
A high readiness score must never be presented as “sendable” or “campaign eligible.”

Kill lines

STOP if the wrong ContactDetail file is modified or a duplicate page is created.
STOP if lastMeaningfulContactMutationAt is assumed to exist without schema/API evidence.
STOP if the client invents a second readiness JSON contract that can drift from Phase 2.
STOP if unknown reason codes break rendering.
STOP if missing old-model components are treated as earnedPoints = 0.
STOP if a dead Campaigns link is added.
STOP if readiness is labeled as permission to contact or campaign eligibility.
STOP if backend routes are changed when the existing contact endpoint already returns
the required fields.

Validation

- Score 85 / grade A renders grade, numeric score, progress and component details.
- Scores at exact A/B/C/D/F boundaries render the correct grade styling.
- Null score renders the unscored state without errors or a dead link.
- Missing or malformed breakdown renders a safe fallback.
- Unknown missing reason code displays a readable fallback label.
- Older model breakdown with a missing component does not display it as a failed field.
- Timestamp/model-version stale state uses the confirmed canonical rule.
- Component is readable without relying on color.
- All dashboard roles that can access Contact Detail can see the card.
- No additional network request is made.

Final status: BUILD-READY WITH CORRECTIONS

This should remain a focused frontend observability task: one reusable card, one verified data contract, and no new backend or campaign logic.