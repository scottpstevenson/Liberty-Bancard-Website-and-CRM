Verdict: NOT BUILD-READY

The migration sequence is now correct, but two architecture blockers remain:

1. The existing SLA title-set guard will still treat completed tasks as duplicates, so completing a task may not actually release the lock despite the new index.
2. source and automationKey must not become client-writable through updateTaskSchema.

Corrections to send back

Fix these blockers before implementation:
1. Align the SLA worker’s existing dedup logic with the chosen invariant.
The worker currently builds existingTaskTitles from all non-deleted tasks. If completed tasks remain non-deleted, their titles will still be present in the Set and the worker will skip creation before createSlaTask() reaches the database.
Update the pre-check so only active, incomplete SLA tasks block creation.
Preferred:
- query existing tasks by dealId + source + automationKey;
- require deletedAt IS NULL;
- require completedAt IS NULL.
After Phase 4, the database constraint is the correctness authority. Any in-memory Set remains a performance optimization only.
Required test:
- complete an existing SLA task;
- run the actual SLA generation path;
- confirm a replacement task is created.
2. Do not add source or automationKey to the public task update schema.
These are server-owned provenance fields.
Do not allow dashboard clients, task-edit endpoints, imports, or generic updateTask() payloads to:
- mark manual tasks as SLA;
- remove SLA identity;
- change automationKey;
- bypass or trigger the partial index.
Add the columns to the database schema and server insert types, but keep them out of client-facing create/update schemas unless a separately authorized internal schema exists.
3. Strengthen historical attribution.
The anchored title is not enough by itself. For every backfilled row:
- parse the numeric deal ID from the title;
- require it to equal the row’s deal_id;
- exclude and report mismatches;
- require deal_id IS NOT NULL.
Do not backfill:
title = "Follow up on stalling Deal #123"
deal_id = 456
4. Do not let generic callers use createSlaTask() with arbitrary identity values.
The storage method should own or strictly validate:
source = "sla"
automationKey = "stalling-deal-follow-up"
Prefer a narrow method such as:
createStallingDealFollowUpTask(payloadWithoutSourceFields)
The caller should not be able to supply another source or automation key while relying on this conflict path.
5. Verify completion state consistency.
Confirm the canonical completion path always sets completedAt, including:
- task update route;
- bulk actions;
- GHL-originated updates;
- any status = completed path.
If some path changes status without completedAt, the index and worker will disagree about whether the task is active.
6. Clarify the GHL deletion action.
The referenced crm-operations.ts deletion path may both delete in GHL and soft-delete locally. During duplicate cleanup, do not invoke a route/service that performs a second uncontrolled local mutation.
Use a narrowly understood canonical GHL cleanup operation, then perform the audited local soft-delete exactly once.

Kill lines

STOP if completed SLA tasks remain in existingTaskTitles and block replacement creation.
STOP if source or automationKey is writable through public create/update task APIs.
STOP if a historical title’s embedded deal ID does not match tasks.deal_id.
STOP if createSlaTask accepts arbitrary source or automationKey values.
STOP if one completion path sets status = completed without completedAt.
STOP if GHL cleanup can trigger duplicate or hidden local deletion mutations.
STOP if the in-memory title check remains the stated correctness authority after the unique index exists.

Validation additions

- Actual SLA worker creates a new task after the prior task is completed.
- Completed non-deleted task does not remain in the worker’s blocking set.
- Public POST/PUT task requests cannot set or alter source/automationKey.
- Backfill rejects title/dealId mismatches.
- Manual task cannot be converted into SLA identity through an API update.
- Every canonical completion path sets completedAt consistently.
- Concurrent createStallingDealFollowUpTask calls produce one incomplete task.

Final status: NOT BUILD-READY

The four-phase rollout is now sound. Fix the worker pre-check lifecycle mismatch and protect the new provenance fields from client mutation before building.