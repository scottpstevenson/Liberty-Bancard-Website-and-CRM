# Task #1726 Audit

## Verdict: NOT BUILD-READY AS WRITTEN

I found **14 required corrections: 2 P0, 9 P1, and 3 P2**.

The central task premise is not yet proved. At the current live repository state, every authoritative source agrees on the existing database rule:

```sql
UNIQUE (import_type, file_hash)
WHERE status IN ('running', 'complete')
```

Therefore, a correctly migrated fresh database should reject a `complete → running` duplicate with `23505`. The task must not change or weaken that rule merely because the current assertion reportedly failed.

## Repository baseline

| Item                                    | Verified state                                                   |
| --------------------------------------- | ---------------------------------------------------------------- |
| Branch                                  | `main`                                                           |
| `HEAD` / `origin/main`                  | `2273f80b0bb4f3f9b628c8a2316d9d445865b1bc`                       |
| Worktree                                | Clean                                                            |
| Latest commit                           | `Correct CSV provider classification and certification coverage` |
| Prior commit                            | `6e4a29f` — Fix CRO-02 redirect race / #1723                     |
| Migration head                          | `0181_cr05_task_ticket_authority`                                |
| Journal                                 | 186 entries, indices `0–185`                                     |
| CI manifest                             | 86 suites                                                        |
| `test-import-idempotency.ts` registered | **No**                                                           |

The current index contract appears identically in:

* [shared/schema.ts](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-repo/shared/schema.ts), lines 1524–1528
* [0061_prospect_lists_idempotency.sql](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-repo/migrations/0061_prospect_lists_idempotency.sql), lines 11–15
* [0109_fearless_starhawk.sql](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-repo/migrations/0109_fearless_starhawk.sql), line 3099
* [server/storage/prospects.ts](sandbox:/workspace/scratch/fd5f269d0f7b/liberty-repo/server/storage/prospects.ts), lines 192–204

The canonical fresh-database migrator applies the `0109` snapshot, which contains that same index definition.

## Required corrections

| ID    | Severity | Required correction                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----- | -------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0-01 |       P0 | Make the suite disposable-only. It currently imports `server/db` and writes to whatever `DATABASE_URL` identifies. Call `assertDisposableTestInfrastructure()` **before** importing `server/db`; require `NODE_ENV=test`, `DATABASE_URL === TEST_DATABASE_URL`, and a clearly disposable database name. Remove “live DB” wording. Never reproduce this on the shared development database again.                                       |
| P0-02 |       P0 | Register `scripts/test-import-idempotency.ts` in `scripts/ci-suite-manifest.ts` as `deterministic-integration`, with disposable PostgreSQL and provider denial. At present the “certification” is omitted from canonical CI and produces no suite-owned terminal receipt.                                                                                                                                                              |
| P1-01 |       P1 | Replace the speculative root cause with an evidence-first decision gate. Capture the exact fresh-database index definition, validity, predicate, actual duplicate outcome, and PostgreSQL error shape. The task currently supplies none of that evidence.                                                                                                                                                                              |
| P1-02 |       P1 | Default to **Migration required: NO**. Do not edit migrations `0061` or `0109`. If a canonically migrated empty database genuinely differs, diagnose the migration runner first. Only then may an additive `0182_*` repair be considered, after an aggregate duplicate census and migration-integrity review. Shared-development drift alone does not justify a source migration.                                                      |
| P1-03 |       P1 | Replace the current index-name-only query. It can false-pass on an invalid, non-ready, wrong-table, wrong-schema, or wrong-predicate index. Verify `public.prospect_lists`, `indisunique`, `indisvalid`, `indisready`, indexed columns, and `pg_get_expr(indpred, indrelid)`.                                                                                                                                                          |
| P1-04 |       P1 | Stop accepting any error message containing `"unique"`. Require `code === "23505"` **and** `constraint === "prospect_lists_import_type_hash_uidx"`. A different unique constraint must fail the certification. If the captured driver error nests those properties, use one shared extractor and update both production race handlers consistently.                                                                                    |
| P1-05 |       P1 | Fix the broken different-execution fixture. `testList.id + 9999` is not a valid `prospect_lists` foreign key and can raise `23503` before testing the email index. Create a real second prospect list and use its actual ID.                                                                                                                                                                                                           |
| P1-06 |       P1 | Add a complete predicate matrix: `running→running`, `running→complete`, `complete→running`, and `complete→complete` must conflict; transitioning the same row from running to complete must succeed; `failed` must permit a new attempt; different hash/type and NULL-key boundaries must remain allowed. Explicitly document that `processing` and `ready` are outside the current predicate.                                         |
| P1-07 |       P1 | Add a real concurrency case using separate connections: two simultaneous inserts for the same active key must yield exactly one committed row, one exact named `23505`, and no third state. The current sequential insert does not certify the production race.                                                                                                                                                                        |
| P1-08 |       P1 | Exercise or accurately describe production ownership. The test currently reimplements queries and direct inserts while claiming it tests `getProspectListByHash`, `createProspectsBulkIdempotent`, CSV replay responses, and COREVT-style upsert behavior. Invoke the actual production methods or narrow the suite header and assertion labels. Include `/api/prospects/import` and `/api/sunbiz/upload-corevt` in the writer census. |
| P1-09 |       P1 | Make fixtures collision-safe and cleanup-provable. Replace `Date.now()` identities with `randomUUID()`. Track every created list and prospect—including an unexpectedly successful duplicate—delete children before parents in `finally`, and assert zero matching residue. The current second fixture deletes only `replayList.id` and can leak the unexpected duplicate it was trying to detect.                                     |
| P2-01 |       P2 | Add task metadata: verified SHA, migration head, journal count, current manifest count, dependency status, and a staleness rule. Explicitly state that #1723 is merged and the latest contact-classification commit is outside this task.                                                                                                                                                                                              |
| P2-02 |       P2 | Correct misleading test documentation: “DB integration tests (live DB),” the unused filename variables, the claim that direct inserts test `createProspectsBulkIdempotent`, and the storage comment naming `prospects_email_unique_idx` instead of `prospects_email_import_unique_idx`.                                                                                                                                                |
| P2-03 |       P2 | Expand Relevant Files, acceptance gates, exclusions, and kill lines. The present three-item file list omits the production lookup/writers, migration runner, infrastructure guard, and CI manifest.                                                                                                                                                                                                                                    |

## Corrected implementation decision tree

1. Run the canonical migration twice against an empty disposable database.
2. Inspect the exact catalog definition and validity of `prospect_lists_import_type_hash_uidx`.
3. Attempt the focused duplicate using raw `pg` or Drizzle while preserving the complete error metadata.
4. Apply the following disposition:

| Finding                                       | Required disposition                                                                                              |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Catalog matches and exact `23505` occurs      | Fix the test/fixture only. No migration.                                                                          |
| Catalog matches but the test misses the error | Fix error extraction/assertion; verify both production race handlers.                                             |
| Canonical fresh catalog differs               | Stop and diagnose `server/db-migrate.ts` and snapshot application before changing the predicate.                  |
| Only shared development differs               | Record environment drift; do not use that database for certification and do not mutate it under #1726.            |
| Predicate genuinely needs repair              | Add a forward-only `0182_*` migration after proving zero conflicting active keys. Never rewrite `0061` or `0109`. |

## Corrected Done Looks Like

* The authoritative rule is proven from schema, historical migration, canonical snapshot, production lookup, keyed writer census, and the disposable database catalog.
* The suite refuses shared, production, or ambiguously named databases before application DB modules load.
* Exact named `23505` behavior is proven across the full status matrix and a simultaneous-insert race.
* A legitimate second prospect-list fixture proves the cross-execution email backstop without an FK violation.
* Production lookup/idempotent methods are exercised, or unsupported coverage claims are removed.
* Every fixture is removed and zero residue is verified.
* The suite is registered in the canonical deterministic-integration capability and owns a valid terminal receipt.
* The focused suite passes **five consecutive guarded runs**.
* The complete deterministic-integration capability passes.
* Canonical migration replay, migration integrity, and suite-manifest validation pass.
* Contact CSV classification, email-format policy, production data, providers, and unrelated import systems remain unchanged.

## Corrected Relevant Files

Must inspect or modify:

* `scripts/test-import-idempotency.ts`
* `scripts/ci-suite-manifest.ts`
* `scripts/test-infrastructure-guard.ts`
* `server/storage/prospects.ts`
* `server/routes/prospects.ts`
* `server/db-migrate.ts`
* `shared/schema.ts`
* `migrations/0061_prospect_lists_idempotency.sql`
* `migrations/0109_fearless_starhawk.sql`
* `migrations/meta/_journal.json`

Conditional only if fresh-catalog drift is proved:

* `migrations/0182_<descriptive_name>.sql`
* `migrations/meta/_journal.json`

## Kill lines

* Stop if the suite can reach a shared or production database.
* Stop if the fix changes the predicate, adds `status` to the unique key, or accepts only same-status conflicts; those changes weaken the cross-status fence.
* Stop if historical migrations `0061` or `0109` are edited.
* Stop if an unrelated unique violation can satisfy the assertion.
* Stop if any fixture remains after success or failure.
* Stop if the suite continues claiming production-path coverage while only testing local simulations.
* Do not modify the contact CSV classification corrected at current `HEAD`.

The likely task-owned change is a **test safety, fixture, catalog-verification, and CI-registration correction—not a database-rule change**.
