These updates are materially correct against live `main`.

### Verified at `2db2f01a…`

| Claim                                          | Verdict                                                              |
| ---------------------------------------------- | -------------------------------------------------------------------- |
| Current HEAD                                   | Confirmed                                                            |
| Package and lock hashes unchanged              | Confirmed                                                            |
| 91 internal lockfile URLs remain               | Confirmed                                                            |
| #1700 changed no package/lock/CI/install files | Confirmed                                                            |
| Four new dynamic Drizzle identifiers           | Confirmed at the stated lines                                        |
| Import/residual/email boundaries changed       | Confirmed                                                            |
| Manifest contains 67 suites                    | Confirmed: 22 static, 26 integration, 13 server-required, 6 optional |
| Migration head is 0173                         | Confirmed                                                            |
| Migration integrity                            | Passed: 388 checks, two historical warnings                          |
| Obsolete CRO-01 assertion                      | Confirmed failure at line 80                                         |
| #1706/#1707 not prerequisites                  | Consistent with current ownership boundaries                         |

One correction to my earlier commentary: **#1700 did not change `.replit`.** The intervening #1701 repository-containment work changed only port mappings. The current Node configuration is unchanged. RVR-02 may drop its wait on #1700, but it should retain coordination with RVR-05 for the `.replit` Node-module line.

### Three final precision requirements

These should be explicit in the revised task if they are not already:

1. **Complete CSV-only UI cleanup**

   Removing XLSX parsing must also update:

   * `client/src/pages/dashboard/ResidualRevenue.tsx`
   * Every XLS/XLSX validation, picker, label, and help-text occurrence in `LeadImports.tsx`
   * `replit.md`
   * `docs/platform-walkthrough.md`

   `MerchantPortal.tsx` may retain spreadsheet document uploads only if those files are stored—not parsed through the vulnerable `xlsx` package.

2. **Freeze the historical-script disposition**

   Before removing `xlsx`, both historical import scripts must either:

   * become bounded CSV-only utilities, or
   * be retired so they cannot execute.

   Acceptance should require zero executable imports/requires of `xlsx`, not merely removal from the live HTTP routes.

3. **Constrain the CRO-01 test repair**

   Remove only:

   ```ts
   check(!migrationNames.some((name) => /0166/i.test(name)), "no prohibited migration 0166 exists");
   ```

   Do not weaken the remaining CRO-01 contract. Afterward require:

   * CRO-01 static suite passes.
   * Migration integrity still passes through 0173.
   * CRO-02 structural authority guard passes.
   * No migration is added or edited by RVR-02.

### Corrected verdict

**RVR-02 no longer depends on #1700.** Its compatibility baseline is correctly rebased. With the three precision requirements above incorporated, the task is:

> **BUILD-READY, BUT IMPLEMENTATION REMAINS BLOCKED UNTIL YOU EXPLICITLY AUTHORIZE the narrow root `package-lock.json` exception.**

The owner approval can be:

> I authorize Task #1702 as a narrow exception to `replit.md:10` for changes to the root `package-lock.json` only, within the audited RVR-02 scope.
