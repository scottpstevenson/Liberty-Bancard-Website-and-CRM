Verdict: NOT BUILD-READY

The compliance concern is valid, but the proposed guard is too broad and could preserve or block the wrong channel state. It also assumes createContactGhlFirst() has a reliable existing-contact update path and enough audit context for every caller.

Required corrections

1. Protect consent independently by channel.
Do not use an email opt-out to automatically block SMS re-consent, or vice versa.
Email re-enable is blocked when persisted state shows an email prohibition, such as:
- doNotContact = true
- doNotAutoContact = true, if it canonically applies to this workflow
- emailStatus = opted_out | unsubscribed
- consentEmail = false when that false represents an explicit withdrawal
SMS re-enable is blocked when persisted state shows an SMS prohibition, such as:
- doNotContact = true
- doNotAutoContact = true
- smsStatus = opted_out | unsubscribed | blocked, using the actual canonical values
- consentSms = false when backed by a recorded withdrawal
- any canonical TCPA/DNC state that prohibits SMS
Do not remove both consentEmail and consentSms merely because one channel is opted out.
2. Do not strip doNotContact or doNotAutoContact from incoming payloads as the primary fix.
Public forms should not be allowed to submit or mutate those protected fields at all.
Verify every public form schema excludes:
- doNotContact
- doNotAutoContact
- emailStatus
- smsStatus
- consentTier
- other compliance-owned fields
The form should submit affirmative consent inputs only. Server-owned status fields must
never flow from the public body into the contact write payload.
3. Create one canonical channel-specific consent merge function.
For example:
mergePersistedConsentState({
  existingContact,
  incomingConsent,
  sourceEvent,
  occurredAt,
}): {
  updates;
  blockedAttempts;
}
It must:
- read persisted state;
- apply channel-specific rules;
- preserve protected opt-outs;
- return explicit blocked reason codes;
- never infer consent merely from absence of an opt-out.
Do not scatter consent-field deletion logic inside createContactGhlFirst().
4. Verify createContactGhlFirst() actually updates an existing contact.
The task assumes a matching email lookup already returns an existing contact and then
continues through an update path.
Provide file:line evidence for:
- normalized email lookup;
- what happens when an existing record is found;
- whether the function updates that contact or attempts a new insert;
- how duplicate-email 23505 is handled;
- whether all public forms use that same path.
If resubmissions currently fail or create only a new deal without updating the contact,
the guard must be placed in the real existing-contact update owner instead.
5. Do not place this behavior on every createContactGhlFirst caller indiscriminately.
createContactGhlFirst() is also used by manual CRM entry and possibly prospect
conversion. Those paths may have different authority and audit context.
The protection should apply based on a verified source mode, such as:
- public_form: preserve opt-outs; no re-consent
- manual_crm: preserve by default; dedicated privileged re-consent action required
- prospect_conversion: cannot re-enable consent
- other internal paths: explicit policy
Do not let a generic writer silently impose or bypass consent rules without source
context.
6. Treat re-consent as a dedicated future action.
A normal form resubmission should not clear an existing opt-out.
However, do not state that re-consent is universally impossible. A future dedicated
flow may permit it only with:
- explicit channel-specific language;
- timestamp;
- source;
- IP/user agent where applicable;
- disclosure/version evidence;
- prior and resulting state;
- audit event;
- canonical compliance review.
This task should block ordinary resubmission-based re-enablement only.
7. Make audit and contact update atomic.
If a blocked re-enable is detected:
- preserve the persisted opt-out;
- write one consent audit row for each affected channel;
- commit the contact update and audit entries in the same DB transaction.
Do not allow:
- contact mutation to commit while the required audit fails;
- audit to claim a block if a later write re-enabled the field.
If the canonical contact writer cannot include the audit in its transaction, establish
a dedicated transactional update method.
8. Use the real consent-audit schema and canonical action taxonomy.
Verify:
- exact table name and columns;
- whether action is constrained;
- channel representation;
- actor/source fields;
- priorValue/newValue fields;
- submissionId or sourceEventId support.
Prefer stable details such as:
action: consent_reenable_blocked
channel: email | sms
reasonCode: existing_email_opt_out | existing_sms_opt_out | global_dnc
attemptedValue: true
persistedValue: false
sourceEventId
formType
contactId
Do not invent columns that the table does not contain.
9. Use stable form/submission identity for audit idempotency.
A retried form handler or duplicate delivery must not create many identical blocked
audit entries.
Use the canonical submissionId/source event as the idempotency key.
Do not use only contactId + timestamp.
10. Preserve persisted status fields exactly.
Blocking incoming consentEmail=true must not accidentally:
- convert emailStatus from unsubscribed to opted_out;
- clear a bounce/invalid state;
- change smsStatus;
- clear doNotContact;
- alter consentTier.
Only apply permitted updates. Preserve the exact existing compliance state otherwise.
11. Correct the non-opted-out resubmission expectation.
“Can update consent normally” is too vague.
Define permitted transitions explicitly. For example:
- unknown/null → true may be allowed with affirmative form evidence;
- true → true is idempotent;
- false caused by explicit withdrawal → true is blocked;
- opted_out/unsubscribed status → true is blocked;
- one channel’s status does not silently govern the other unless global DNC applies.
Do not let a normal form turn arbitrary false values back to true without knowing why
they were false.
12. Avoid unsupported legal conclusions in code comments and task reports.
Describe this as preserving recorded opt-out and consent state.
Do not claim the implementation by itself establishes CAN-SPAM or TCPA compliance.
Legal applicability depends on message type, jurisdiction, evidence, and workflow.

Corrected implementation shape

A. Confirm the existing-contact form resubmission path.
B. Add a pure, typed mergePersistedConsentState() service.
C. Pass source context and stable submissionId into the canonical contact update path.
D. For each channel:
   - inspect persisted state;
   - determine allowed transition;
   - preserve blocked fields;
   - produce typed blocked reason codes.
E. Persist permitted contact updates and consent audit entries atomically.
F. Leave new-contact behavior unchanged, except public schemas must still prevent
   clients from setting server-owned compliance status fields.

Kill lines

STOP if an email opt-out automatically blocks SMS consent without a global prohibition.
STOP if SMS opt-out automatically blocks email consent without a global prohibition.
STOP if public request bodies can set doNotContact, status, tier, or other
server-owned compliance fields.
STOP if ordinary form resubmission can clear an explicit opt-out.
STOP if createContactGhlFirst is assumed to update existing contacts without evidence.
STOP if the guard affects unrelated writer modes without source-specific policy.
STOP if contact updates and blocked-consent audit rows can commit independently.
STOP if duplicate form delivery creates duplicate audit events.
STOP if bounced/invalid/unsubscribed statuses are rewritten merely to preserve consent.
STOP if this task introduces a re-consent flow.

Required validation

- Existing email-unsubscribed contact submits consentEmail=true:
  email status and consent remain protected; SMS remains unaffected.
- Existing SMS-opted-out contact submits consentSms=true:
  SMS state remains protected; email remains unaffected.
- Global doNotContact contact submits either consent:
  both prohibited channels remain blocked according to canonical policy.
- Existing email bounce/invalid state is not cleared by form consent.
- New contact with affirmative email/SMS consent stores permitted values normally.
- Null/unknown consent with valid affirmative form evidence follows the documented
  allowed transition.
- Explicit prior withdrawal cannot be reversed through ordinary form resubmission.
- Exactly one idempotent audit event is written per blocked channel/source event.
- Forced audit insertion failure rolls back the corresponding contact update.
- Manual CRM and prospect-conversion paths cannot bypass protection.
- All six public forms are proven to use the guarded existing-contact path.
- Existing contactability and consent compliance tests remain green.

Final status: NOT BUILD-READY

The correct task is channel-specific, transactional preservation of persisted consent and opt-out state on ordinary form resubmission. The current plan’s combined email/SMS guard and broad field stripping are not safe enough.