Verdict: NOT BUILD-READY

The proposed SLA definition is unsafe. dealId IS NOT NULL AND createdBy IS NULL does not prove a task was SLA-generated and can misclassify legitimate system-created or imported tasks.

Corrections to send back

Do not build the filter using:
title LIKE 'Follow up on stalling Deal #%'
OR (dealId IS NOT NULL AND createdBy IS NULL)
That creates false positives.
Use only a source signal that is proven canonical.
For the current schema, the safe interim definition is:
SLA-generated:
title starts with the exact canonical prefix used by sla-worker.ts:
"Follow up on stalling Deal #"
Manual:
all other tasks
Also verify:
- the exact title format in sla-worker.ts;
- whether historical SLA titles have variants;
- whether any manually created tasks can use the same title pattern;
- whether createdBy exists on the task schema at all.
Add a small helper in Tasks.tsx:
isSlaGeneratedTask(task)
Use it for:
- filter logic;
- optional source badge;
- tests.
Do not imply that dealId identifies source. dealId only identifies deal association.
Because filtering is client-side, label the behavior clearly if the page loads only a subset:
“Filters loaded tasks”
or confirm the full active task dataset is loaded.
If task pagination or a row cap exists, this is not a true global Source filter and should either:
- remain explicitly loaded-row-only, or
- move to the API in a separate task.

Kill lines

STOP if dealId or createdBy-null is treated as proof of SLA origin.
STOP if the exact SLA title prefix is not verified from the worker.
STOP if manually created tasks can match the same title pattern without an acknowledged false-positive risk.
STOP if the UI presents a global filter while only filtering loaded rows.
STOP if server/services/sla-worker.ts is modified for this UI-only task.

Validation

- Exact SLA title → appears under SLA-generated only.
- Manual task with dealId → remains Manual.
- Task with createdBy null but unrelated title → remains Manual.
- Historical title variants are tested.
- All/Manual/SLA counts reconcile with the loaded task list.
- Existing selection state is cleared or reconciled when switching filters so hidden selected rows are not accidentally bulk-deleted.

Final status: NOT BUILD-READY

The filter is easy to build, but the proposed source classification is not trustworthy. Use the exact SLA title signature as an interim heuristic, and keep source attribution as a future schema-backed improvement.