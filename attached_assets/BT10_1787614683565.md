# LIBERTY BANCARD — PREFLIGHT + BUILD MODE

## MODE

PREFLIGHT + BUILD. Verify every provider/readiness claim against current code and current repository state, correct the plan and implement safe code-owned work now. Stop only for a false finding, wrong canonical owner, unmet dependency, runtime/provider evidence required for a live operation, necessary split or kill line.

Do not initiate unapproved paid calls, bulk backfills, GHL mutations or outbound sends; do not treat provider failure as no-result/success; do not create competing readiness/scoring/taxonomy owners; do not use `db push` or weaken tests.

Required sequence: baseline → VFC → searches → root cause → ownership → blast radius → data/auth/concurrency/external checks → verdict → corrected plan → kill lines → build → tests/gates → post-build searches → diff → final VFC → merge verdict.

## 1. REPOSITORY BASELINE

Capture branch, HEAD SHA, working tree and migration head. Record current provider feature flags/control rows only through safe read-only evidence. Never print secret values.

## 2. VERIFIED FROM CODE — PREFLIGHT

Create:

| ID | Task Claim | Verdict | Verified Reality | Evidence |
|---|---|---|---|---|
| VFC-01 | ... | CONFIRMED / PARTIAL / FALSE / OUTDATED | ... | `file:line` |

Verify ZeroBounce, Serper and every paid/free provider path; gateway/circuit/budget/cooldown ownership; discovery/enrichment orchestration; provider observation storage; field arbitration; validation-on-create/change; GHL reconciliation/circuit/workflow registry; vertical taxonomy; readiness and lead-score engines; campaign preview/run parity; run-state APIs/UI and tests. Trace all callers/writers/consumers.

## 3. REQUIRED SEARCH / GREP CHECKS

Search and inspect:

- provider clients, raw `fetch`/SDK calls, API routes and secrets-by-name only;
- gateway, circuit, rate/budget/quota, cooldown, retry and outcome codes;
- ZeroBounce validation status, attempts and creation/change hooks;
- Serper search/places and other paid enrichment;
- Sunbiz/discovery/re-enrichment/provider queues;
- GHL unsynced selectors, identity conflicts, invalid contacts, circuit and workflow registry;
- provider observations, confidence/freshness/manual override;
- `vertical`, `subvertical`, readiness and lead-score writers/readers;
- campaign preview/run selection and enrollment;
- backfill/run ledger/status and related tests.

Inspect behavior; counters and timestamps do not prove successful yield.

## 4. VERIFIED ROOT CAUSE

Separate shipped controls from remaining gaps and runtime evidence. State whether calls are routed through canonical gateways, whether outcomes distinguish success/no-result/provider failure/data conflict, whether readiness/scoring prerequisites are enforced, and whether current GHL/provider health is proven. Correct outdated assumptions.

## 5. SOURCE-OF-TRUTH CHECK

Identify canonical provider gateway per integration, observation/field-arbitration owner, validation owner, vertical taxonomy owner, readiness engine, scoring path, GHL owner, campaign selector and run/audit owner. BT-04 owns contactability; BT-06 classification; BT-07 identity; BT-08 provenance/business authority; BT-09 durable jobs. Do not duplicate them.

## 6. BLAST RADIUS

### In scope

- validation-on-create/enrichment-change forward path;
- provider outcome/cost/yield/circuit accounting;
- append-only observations and deterministic arbitration;
- versioned vertical taxonomy;
- readiness/score prerequisite enforcement and bounded backfill tooling;
- identity-clean bounded GHL reconciliation tooling;
- campaign preview/run parity and unready/unclassified exclusion;
- explicit idle/blocked/failing/paused/complete run state;
- tests and exact runtime-evidence instructions.

### Out of scope

- spending credits or running production backfills without approval;
- altering consent semantics or outbound authority;
- identity merges/provenance reconstruction;
- UI navigation overhaul;
- claiming provider yield from mocks.

List exact expected/untouched files.

## 7. DATA / SCHEMA CHECK

Verify provider attempts/observations/runs, validation status/history, GHL identity, taxonomy, readiness and score columns/tables, defaults, constraints, timestamps, indexes, archive and readers/writers. Provider failure must not update a no-result counter or authorize a field/status.

If schema changes, create the next migration/journal and prove replay. Backfills must be resumable, bounded, idempotent and prerequisite-filtered. Do not backfill history unless explicitly approved; no `db push`.

## 8. AUTHORIZATION CHECK

Produce a role matrix for viewing provider state, enabling/disabling gateways, manual recovery, resetting windows, starting/canceling backfills, overriding fields/taxonomy and initiating GHL reconciliation. Provider-spending and override actions require explicit server-side admin gates and audits.

## 9. CONCURRENCY / IDEMPOTENCY CHECK

Check duplicate provider requests, simultaneous enrichments, circuit transitions, quota windows, backfill claims, contact changes during validation, GHL retries and campaign preview/run races. Require stable idempotency, atomic claims/cooldowns, monotonic observation history and deterministic arbitration. A retry must not double-charge or overwrite stronger/manual evidence.

## 10. EXTERNAL SIDE-EFFECT CHECK

Document prerequisite/authority lookup → durable attempt/claim → provider call → response classification → observation/state/audit ordering. Cover timeout, provider success/DB failure, DB claim/provider failure, 4xx data-quality response, 429/quota, replay and duplicate execution. Never claim provider and DB atomicity. Use fake transports in tests.

## 11. PREFLIGHT VERDICT

Choose BUILD-READY, BUILD-READY WITH CORRECTIONS, PREFLIGHT REQUIRED, NOT BUILD-READY, NOT NEW TASK or WATCH. Continue immediately with safe implementation. If live credits, target cohort approval or runtime secrets are unavailable, complete gateways, controls, tooling and tests, then mark the bounded live run as runtime verification pending.

## 12. CORRECTED BUILD PLAN

State corrected root cause, exact outcomes and file-specific steps. Separate forward-path correctness, bounded operational tooling, live execution and evidence. Preserve already shipped Serper/GHL safeguards as regressions.

## 13. KILL LINES

- KILL LINE: If provider failure, timeout, quota exhaustion, invalid identity or missing prerequisite can still be recorded as successful enrichment or make a record outreach-ready, the task has FAILED.
- STOP if a raw paid-provider bypass remains.
- STOP if a backfill includes unclassified, identity-conflicted or untraceable records.
- STOP if preview and execution select different cohorts.
- STOP if last-write-wins can overwrite manual or stronger evidence.
- STOP if test execution reaches a real provider.
- STOP if runtime success is claimed from counters/timestamps without outcome/yield evidence.

## 14. IMPLEMENTATION RULES

Use existing gateways, circuits, observation/readiness/scoring owners and smallest safe diff. No unrelated refactor, broad rename, dependency/config mutation or provider enablement. Preserve manual authority and append-only evidence.

## 15. TEST REQUIREMENTS

Cover success, zero result, provider failure, invalid input, 429/quota, timeout, circuit open/half-open/recovery, budget limit, cooldown, validation hooks, observation arbitration, manual override, taxonomy compatibility, readiness/score prerequisites, bounded/replayed/concurrent backfills, GHL skip/failure/success, preview/run parity, authorization and partial failures.

## 16. SMOKE / INTEGRATION TEST

Extend existing provider/readiness suites or add `scripts/test-bt10-provider-readiness.ts` using fake transports. Prove:

1. all calls route through canonical gateways;
2. each outcome is classified and counted correctly;
3. failures cannot approve fields/readiness;
4. stronger/manual observations win deterministically;
5. unclassified/conflicted/unvalidated rows are excluded;
6. bounded backfill replay is safe;
7. preview and run cohorts match;
8. GHL reconciliation is identity-clean and circuit-safe;
9. no real provider call occurs.

## 17. POST-BUILD GREP CHECKS

Prove no raw provider call bypass remains, all outcomes use one classification, all readiness/score writers enforce prerequisites, taxonomy consumers use one versioned authority, campaign selectors share one predicate, run states are consumed correctly and no stale status vocabulary remains.

## 18. REQUIRED GATES

Run targeted provider/validation/readiness/GHL tests, queue/outbound/contactability/identity/provenance regressions, typecheck, build, migration replay, contracts/RBAC/pre-deploy/invariants and `git diff --check`. Report actual commands and reserve live yield/coverage for VG-03.

## 19. DIFF REVIEW

Run `git status`, `git diff --stat` and `git diff`. Confirm intended files only, no secrets, real API calls, production flags, debug response bodies, unrelated lockfile/formatting/config changes or accidental backfill artifacts.

## 20. FINAL VFC TABLE

Produce:

| ID | Requirement | Evidence | Test / Gate | Status |
|---|---|---|---|---|
| VFC-F01 | ... | `file:line` | test | PASS |

Map every requirement and kill line; mark live cohort/provider proof separately.

## 21. FINAL RESPONSE FORMAT

Return: VERDICT (COMPLETE / VERIFIED, PARTIALLY COMPLETE or DO NOT MERGE); Repository State (starting SHA, ending SHA/working tree, migration head); Verified Root Cause; Preflight Corrections; Implementation (`file:line`); Tests/Gates; Grep Verification; Kill-Line Verification; Runtime Verification; Remaining Risks; and Final Status (SAFE TO MERGE, SAFE TO MERGE — RUNTIME VERIFICATION PENDING or DO NOT MERGE). State what needs exact-release runtime windows: provider success/yield, cohort validation/readiness/score coverage, worker health and stable GHL completion.

## LIBERTY-SPECIFIC SAFETY RULES

- Data readiness, score, consent, contactability and promotional eligibility are independent.
- Paid provider use must be bounded, budgeted and observable.
- Provider observation is evidence, not automatic authority.
- GHL/local state is distributed.
- Durable jobs only; no `db push`.

## PRACTICAL REVIEW STANDARD

Block quota burn, false readiness, provider bypass, wrong-customer mutation, conflicting-field overwrite, duplicate charges/effects or unsafe backfill cohorts. Do not require live paid calls to merge safe control-plane and test improvements.

# TASK TO PREFLIGHT + BUILD

## BT-10 — Provider Health, Validation, Readiness & Backfills

**Primary findings:** `OUT-06`, `OUT-11`, `DAT-08`, `DAT-09`, `DAT-13`, `ENR-01`, `ENR-02`, `ENR-03`, `ENR-04`, `ENR-05`, `ENR-07`

**Dependencies:** BT-04 and BT-06 through BT-09; VG-02 passed for the target cohort before live backfill/reconciliation

### What & Why

Validation/readiness/scoring coverage is incomplete; provider outcome, cost and yield evidence is uneven; field writes lack universal observation/arbitration; vertical authority is inconsistent; and GHL/provider runtime success needs bounded exact-release proof. Controls shipped for Serper/GHL must remain while productive use is restored safely.

### Done Looks Like

- Validation is enforced on creation and material email changes.
- Every paid provider has bounded circuit/budget/cost/yield/outcome accounting.
- Success, no-result, provider failure and data conflict are distinct.
- Provider observations are append-only and arbitrated by authority/confidence/freshness/manual override.
- One versioned taxonomy feeds readiness, scoring and campaigns.
- Backfills require classification, identity, provenance and validation prerequisites.
- Campaign preview/run parity is preserved.
- Bounded GHL reconciliation uses identity-clean records.
- Run state is explicit and operator-visible.
- Live positive yield/coverage is reserved for VG-03 evidence.

### Out of Scope

- Unapproved paid calls, production-wide backfills, outbound sends, identity merges or UI navigation redesign.

### Proposed Implementation Steps

1. Inventory providers, bypasses, statuses, observations and consumers.
2. Normalize canonical outcome/circuit/budget accounting.
3. Enforce validation forward-path hooks.
4. Add observation/arbitration and taxonomy authority.
5. Enforce readiness/score/backfill prerequisites.
6. Preserve preview/run parity and GHL safety.
7. Add bounded run tooling/status and fake-transport tests.
8. Produce exact VG-03 runtime instructions without executing unapproved operations.

### Relevant Files and Areas to Verify

- provider gateway/client/services including ZeroBounce, Serper, Sunbiz and others
- discovery/enrichment/post-enrichment queues and workers
- GHL sync/circuit/workflow registry
- readiness, scoring, taxonomy and campaign selectors
- provider observation/run/control schema, migrations and admin APIs/UI
- existing provider/compliance/pre-deploy tests

### Existing Kill Line

KILL LINE: Provider failure or missing prerequisites must never approve data, readiness, scoring, GHL reconciliation or outreach eligibility.

## FINAL DIRECTIVE

Verify first and build the complete safe control/data path now. Never spend credits, mutate production cohorts or claim provider success without authorized exact-release runtime evidence.
