Verdict: BUILD-READY WITH CORRECTIONS

The task is mostly sound, but it contains one direct contradiction: it requires primary and secondary labels to never duplicate, then says showing companyName twice is acceptable. That must be corrected before implementation.

Corrections to send back

Make these corrections before building:
1. Resolve the duplicate-label contradiction.
The task’s acceptance criteria says primary and secondary labels must never show the same value. Therefore this statement is wrong and must be removed:
“if getContactName resolved to companyName, getCompanyName may still return companyName and the duplicate is acceptable.”
Duplicates are not acceptable.
2. Do not solve this with two independent helpers that cannot coordinate.
Create a deal-card identity helper that receives the deal and contact and returns both values together:
{
  primary: string;
  secondary: string | null;
}
Recommended logic:
Primary:
full name
→ companyName
→ email
→ phone
→ Deal #${deal.id}
Secondary:
the first available value among companyName, email, and phone that is not equal to primary
→ otherwise null
Normalize with trim() and compare case-insensitively when preventing duplicates.
3. Use the shared identity helper only for visual Pipeline surfaces:
- Kanban card
- drag overlay
- deal detail panel
Do not automatically reuse the visual secondary-label logic for CSV export.
4. Handle CSV export explicitly.
Contact column:
full name
→ companyName
→ email
→ phone
→ Deal #${deal.id}
Company column:
companyName || "N/A"
The company export field may intentionally equal the contact field when companyName is the only available identifier. The no-duplicate requirement applies to stacked visual labels, not separate CSV columns.
5. Update helper signatures correctly.
The existing getContactName(contactId) cannot produce Deal #${deal.id} unless deal.id is passed. Either:
- pass the full deal into the new helper, or
- pass both deal.id and contactId.
Do not reference an unavailable deal variable inside the existing helper.
6. Confirm the vertical query’s actual state model before writing JSX.
Use the existing query’s real properties, which may be:
- isPending instead of isLoading,
- isError/error,
- data.
Do not introduce duplicate local state if TanStack Query already owns it.
7. Disabled SelectItem values must remain valid and unique if the component library requires value.
Use explicit sentinel values such as:
_loading, _error, and _empty, or render the explanatory state outside SelectContent if disabled items without values are invalid.
8. Keep the successful-empty guidance concise inside the dropdown. Place the longer explanation or link below the Select, not inside an oversized SelectItem.
9. Verify route access, not only route existence.
If Sequences is visible to roles that cannot access Outreach Command Center, render plain explanatory text for those users rather than a dead or forbidden link.

Kill lines

STOP if companyName can render as both the primary and secondary visual label.
STOP if getContactName references deal.id without receiving the deal or deal ID.
STOP if visual display helpers are reused blindly for CSV export.
STOP if CSV company output changes to blank without an explicit fallback.
STOP if local loading/error state duplicates or conflicts with the canonical query state.
STOP if loading, error, and legitimate-empty vertical states are inferred only from verticals.length.
STOP if a SelectItem is rendered with an invalid or duplicate value.
STOP if the enrichment CTA appears after a failed vertical request.
STOP if the Outreach Command Center route exists but the current user role cannot access it.

Validation

pnpm typecheck
pnpm lint

Targeted checks:

- Full name + company → name / company
- No name + company + email → company / email
- No name/company + email + phone → email / phone
- Phone only → phone / no secondary
- No contact data → Deal #N / no secondary
- Case or whitespace differences cannot produce duplicate labels
- Kanban, drag overlay, and detail panel use identical identity rules
- CSV contact always has a fallback
- CSV company explicitly outputs companyName or N/A
- Vertical loading, error, empty, and populated states render independently
- Error state never shows enrichment guidance
- CTA/link is shown only to a role that can access its destination

Final status: BUILD-READY WITH CORRECTIONS

Once the contradictory duplicate-label instruction is removed and the display logic is consolidated into a coordinated primary/secondary helper, this is safe to build.