Verdict: BUILD PHASE 2 NEXT; PREFLIGHT PHASE 3 IN PARALLEL

Yes. Phase 1 is closed. The next move should be:

Phase 2 — Contact Readiness & Audience Intelligence

This should ship next because it immediately improves targeting across the 154K CRM contacts without changing intake architecture.

Keep it separate from lead_score.

Scope:

* Add a dedicated data_completeness_score or equivalent readiness field.
* Score based on record completeness, not behavioral engagement.
* Backfill all active CRM contacts in restart-safe batches.
* Recalculate on relevant contact updates/imports.
* Surface readiness distribution and missing-data reasons.
* Allow campaign preview/filtering by readiness threshold.
* Preserve lead_score, hot/warm/cold sales semantics, and existing routing behavior.

Important correction: completeness alone must not mean sendable. Campaign eligibility remains:

readiness threshold
+ canonical evaluateContactability()
+ vertical/campaign criteria

Phase 3 — Universal Lead Intake & Enrichment Routing

This should begin with a preflight while Phase 2 is being built. It is broader and touches every source.

Scope:

* Inventory all intake paths: CSV, GHL, forms, manual entry, registry imports, Apollo, Outscraper, Apify.
* Make contacts the canonical operational record after promotion/ingestion.
* Preserve source provenance and import history.
* Build safe dedupe rules with confidence and review paths.
* Route incomplete contacts into enrichment.
* Auto-score and classify after successful ingestion or enrichment.
* Promote enriched registry/discovery records into contacts without auto-sending.
* Add direct API ingestion only after each provider’s ownership, limits, and field mapping are verified.

Do not bundle these into Phase 3 yet:

* Deprecating prospects.
* Collapsing sunbiz_entities.
* Removing navigation pages.
* Bulk-archiving pipeline deals.
* Automatic campaign enrollment after enrichment.

Those belong in Phase 4 after the reader/writer ownership audit.

Send this back

Phase 1 is complete.
Proceed with:
Phase 2 — Contact Readiness & Audience Intelligence
Build a separate data-readiness model for CRM contacts without modifying or replacing lead_score.
Required outcomes:
- persisted readiness/completeness score;
- reason-coded missing fields;
- restart-safe full CRM backfill;
- automatic recalculation on relevant contact mutations/imports;
- campaign preview filtering by readiness threshold;
- readiness dashboards and distribution;
- evaluateContactability remains the final send authority.
Phase 3 — Universal Lead Intake & Enrichment Routing
Start with a read-only ownership preflight while Phase 2 is implemented.
Map every current intake source and define:
- canonical contact creation/update owner;
- provenance fields;
- dedupe order and collision handling;
- enrichment eligibility;
- vertical classification ownership;
- score/readiness triggers;
- promotion from discovery/registry records into contacts;
- audit, rollback, and idempotency behavior.
Do not deprecate prospects, sunbiz_entities, or existing pages in Phase 3.
Do not auto-enroll or send after enrichment.
Do not merge records on weak phone/company matching without confidence safeguards.

Order: Phase 2 build now. Phase 3 preflight now, implementation after its ownership audit is approved.