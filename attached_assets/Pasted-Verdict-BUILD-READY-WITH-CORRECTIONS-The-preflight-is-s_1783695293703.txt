Verdict: BUILD-READY WITH CORRECTIONS

The preflight is strong and it correctly catches the four major false assumptions. Do not keep this as one blended emergency task.

The main remaining weakness is that a process-level _aiOpsRunning flag is only a single-process guard. It does not prevent duplicate task creation across multiple server instances, restarts, overlapping cron invocations in different processes, or failed deployments. It is useful as a first guard, but it is not a database-level backstop.

Corrections to send back

Split Task #918 into two independently deployable tasks.
918-A — SLA Task Idempotency + Controlled Task Cleanup
1. Confirm how runScheduledAiOps() is scheduled and whether more than one application/worker instance can execute it.
2. Add the process-level _aiOpsRunning guard to prevent overlap within one process, but do not describe this as complete idempotency.
3. Update SLA-generated tasks to populate dealId: deal.id.
4. Preserve the existing bulk title pre-check as a performance optimization only.
5. Before approving cleanup, determine the durable cross-process backstop:
   - Prefer an existing distributed job lock or database advisory lock around runScheduledAiOps().
   - Do not add a new schema constraint in this task unless a duplicate audit and migration plan are included.
   - If no cross-process guard exists, classify the worker fix as partial and open a separate migration/idempotency task.
6. Add bulkSoftDeleteTasks() using deletedAt, max 2,000 validated integer IDs, chunks of 500, and one audit record containing actor, timestamp, requested count, and actual deleted count.
7. Change the existing DELETE /api/tasks/:id endpoint to use softDeleteTask() so single and bulk deletion share one lifecycle.
8. Build the Tasks selection UI from scratch. Clearly label whether Select All means:
   - visible page only, or
   - every result in the current filtered dataset.
   Never display a global selected count unless the server actually tracks that full selection.
9. Deploy and verify the SLA overlap guard before deleting any historical tasks.
10. Before cleanup, produce a dry-run:
    - exact matched count,
    - exact matching criteria,
    - sample task IDs/titles/deals,
    - proof that legitimate human-created tasks are excluded.
11. Only then apply soft deletion and verify that tasks do not regenerate.
918-B — Pipeline Identifier + Vertical Picker UX
1. Update the contact identifier fallback:
   full name → companyName → email → phone → Deal #<deal.id>.
2. Use deal.id for the final fallback, not contactId.
3. Do not reuse one helper blindly across Kanban, drag preview, detail view, and CSV export. Verify each call site independently.
4. The company/secondary line must not duplicate the primary identifier. If companyName became the primary label, use email/phone as secondary or omit the line.
5. Preserve a meaningful CSV value. Do not silently change the exported company column to an empty string without an explicit CSV decision.
6. Distinguish vertical picker states:
   - loading,
   - request error,
   - successful empty result,
   - populated result.
7. Show enrichment guidance only for a successful empty result. Do not show it when the API failed.
8. Verify the Outreach Command Center route and role access before rendering a link. Otherwise use explanatory text without a link.

Kill lines

STOP if _aiOpsRunning is treated as protection across multiple processes or deployments.
STOP if the SLA worker can run on more than one instance and no distributed/advisory lock exists.
STOP if task cleanup criteria cannot distinguish SLA-generated garbage tasks from legitimate tasks.
STOP if cleanup happens before the duplicate-generation fix is deployed and verified.
STOP if single deletion remains hard-delete while bulk deletion becomes soft-delete.
STOP if bulk deletion has no row-level authorization or audit record.
STOP if Select All only selects loaded rows while showing the total dataset count.
STOP if the pipeline final fallback uses contactId instead of deal.id.
STOP if primary and secondary deal-card labels can show the same value.
STOP if CSV behavior changes without explicit verification.
STOP if a vertical API error displays the legitimate-empty enrichment message.

Validation

npx tsx scripts/smoke-role-guards.ts
pnpm typecheck
pnpm lint
pnpm test

Required targeted checks:

- Two simultaneous executions in the same process create at most one SLA task.
- If multiple app instances are possible, test or prove the distributed lock behavior.
- Bulk delete rejects unauthenticated, unauthorized, malformed, negative, duplicate, and excessive IDs.
- Already-deleted IDs do not inflate the deleted count.
- Single and bulk delete both set deletedAt.
- Audit log records requested and actual deleted counts.
- Pipeline fallback covers full name, partial name, company, email, phone, and no contact data.
- Primary and secondary identifiers never duplicate.
- CSV export remains intentional and readable.
- Vertical picker separately renders loading, error, empty, and populated states.

Final status

Split it into two tasks.

The UX task is close to build-ready. The SLA and cleanup task remains conditionally build-ready only after Replit proves whether runScheduledAiOps() can execute across multiple processes and identifies the cross-process lock owner.