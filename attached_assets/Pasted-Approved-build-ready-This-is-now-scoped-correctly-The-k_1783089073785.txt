Approved — build-ready.

This is now scoped correctly. The key correction is solid:

sdrLeadState has no subvertical
sdrMerchants has subvertical
therefore orchestrator must resolve merchant.subvertical via merchantId

That prevents the most likely bad implementation.

Final verdict

Build #725.

The intended behavior is exactly right:

canonical vertical = merchant.subvertical || merchant.vertical || "Unknown"

Then use that canonical value for:

orchestrator inbox tagging
SMS enrollment tagging
email enrollment tagging
reply-intelligence context
webhook smart-reply context

No vertical-name hardcoding. No classifier changes. No historical backfill.

One implementation guard to add

The only thing I’d add is an N+1 / missing-merchant guard for orchestrator.ts.

Because the orchestrator may process many leads, Replit should avoid doing a fresh merchant lookup for every tag call if the merchant is already loaded or can be batched.

Add this small note:

Final Addendum to #725 — Merchant Lookup Guard

Approved to build.

One final implementation guard:

orchestrator.ts must not assume lead.subvertical exists, because sdrLeadState has no subvertical column. It must resolve canonical vertical from the related sdrMerchants row via lead.merchantId, or use an already-loaded merchant object if one is available in scope.

Required helper behavior:

getCanonicalLeadVertical({
  subvertical: merchant?.subvertical,
  vertical: merchant?.vertical ?? lead.vertical,
})

Fallback behavior:

merchant.subvertical exists → use it
merchant.subvertical missing/empty → use merchant.vertical
merchant missing → use lead.vertical
all missing → "Unknown"

Kill line:

STOP if orchestrator.ts reads `lead.subvertical` directly.

Performance guard:

STOP if the fix adds repeated per-lead merchant lookups inside a hot loop when merchant data is already available or can be loaded once.

If no batching/helper exists today, a single lookup per enrollment action is acceptable, but repeated duplicate lookups inside the same action should be avoided.

Tests must cover:

merchant.subvertical = Med Spa, lead.vertical = Salon/Spa → Med Spa
merchant.subvertical = null, merchant.vertical = Salon/Spa → Salon/Spa
merchant missing, lead.vertical = Salon/Spa → Salon/Spa
all missing → Unknown
Retail or any future subvertical → returned as-is

Final build notes

This is a surgical consistency fix. It should not touch:

classifyVertical()
normalizeDiscoveryVertical()
seeded campaign copy
routing rules
discovery activation
outreach sending
schema/migrations

Once this lands, outbound targeting and inbound/reply handling will finally agree on the same vertical identity.