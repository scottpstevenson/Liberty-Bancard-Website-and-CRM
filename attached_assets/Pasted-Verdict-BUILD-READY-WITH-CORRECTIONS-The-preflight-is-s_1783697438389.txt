Verdict: BUILD-READY WITH CORRECTIONS

The preflight is strong and correctly blocks the original “Delivered” wording. It has enough evidence to rewrite the task, but two details need correction before Replit proposes the final plan.

Corrections to send back

Update Task #917 using this preflight, with these additional corrections:
1. Do not invent “Pending” from absence alone.
The current audit model only records terminal outcomes:
- workflow enrolled
- sent
- failed
- skipped
There is no confirmed “confirmation attempt started” event.
Therefore:
- No terminal event cannot reliably mean Pending.
- It may mean the confirmation workflow was never invoked.
Use:
- Not Sent / No confirmation record
Only add Pending if a verified non-terminal start/queued event exists or the submission record itself proves a confirmation attempt is expected and currently unresolved.
2. Confirm the canonical audit_logs entity fields before fixing the query shape.
Do not assume:
entity_type = 'contact'
entity_id = contactId
Provide file:line evidence for the actual audit_logs schema and the values written by these four event producers. The shared projection must query by the fields actually persisted.
3. Separate contact-level and global projection functions.
Use one canonical event-normalization module, but do not force every consumer through a function shaped only as:
getConfirmationStatus(contactId, submissionId?)
Recommended service responsibilities:
- normalizeConfirmationAuditEvent(row)
- getContactConfirmationStatuses(contactId)
- getConfirmationMetricForRange(start, end, timezone)
- getUnresolvedConfirmationFailures(scope)
All must reuse the same normalization and latest-event-per-submission rules.
4. Define event ordering beyond created_at alone.
If two retry events share the same timestamp resolution, ordering must be deterministic. Use:
- createdAt DESC
- then audit log ID DESC
or the repository’s canonical monotonic ordering field.
5. Verify JSON field query support and indexes.
The dashboard metric and unresolved-failure query may scan a large audit_logs table and group by details->>'submissionId'.
Before approving:
- inspect current audit-log indexes,
- estimate today’s event volume,
- explain whether the query remains bounded,
- avoid loading all historical rows into application memory.
No schema change is required only if current indexes/query volume are acceptable.
6. Do not automatically add this to AnomalyAlertsPanel.
The preflight correctly proves confirmation failures are operational exceptions, not statistical anomalies.
Preferred UI:
- a sibling Confirmation Failures panel or alert section,
- unless the existing panel supports heterogeneous alert sources through a canonical aggregation layer.
7. Define which submission appears on Contact Detail.
If one contact has multiple submissions:
- show the latest submission prominently,
- provide recent submission history or a count,
- do not collapse all submissions into one ambiguous badge.
8. Metric naming must remain:
“Confirmation send success rate”
Never:
“Delivery rate”
“Delivered”
“Email delivery success”
9. Define timezone from an existing canonical application/business timezone source. Do not add a new hardcoded timezone inside the metric endpoint.

Kill lines

STOP if Pending is inferred without a confirmed attempt-start or queued event.
STOP if the projection assumes audit entity_type/entity_id values without checking the producers and schema.
STOP if created_at is the only ordering field and ties can occur.
STOP if dashboard aggregation scans the entire audit history on every refresh.
STOP if raw audit parsing is duplicated across contact, dashboard, and alerts.
STOP if confirmation failures are mixed into statistical anomaly detection without a shared alert architecture.
STOP if multiple submissions are collapsed into one contact-level status without identifying which submission won.

Validation

npx tsx scripts/smoke-role-guards.ts
npx tsc --noEmit --skipLibCheck
pnpm test

Required tests:

- No event → Not Sent, not Pending.
- Workflow enrollment → Workflow Enrolled.
- SMTP/direct send → Sent with correct channel.
- Failure then later success for same submission → Sent and no active alert.
- Two submissions for one contact remain separate.
- Same-timestamp events resolve deterministically.
- Metric uses the canonical timezone and excludes skipped/pending states correctly.
- Global metric/failure queries remain bounded and tenant-scoped.

Final status: BUILD-READY WITH CORRECTIONS

Yes—have Replit update and propose the corrected plan. The main architectural direction is now right; it just must not invent a Pending state or assume the audit-log query shape.