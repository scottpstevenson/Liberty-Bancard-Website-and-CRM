#1613 is mostly fixed, but the report overstates completion.

What passed:

* The true contaminating suite was identified: `test-nba.ts`, not #1609.
* The 34 historical orphaned holds were deactivated by exact UUID.
* NBA tests pass 27/27.
* Three direct NBA runs created zero additional holds.
* The current 17 `global_outbound` holds are expected while global pause is active. Do not delete them.

What remains unresolved:

* The required full pre-deploy run never completed.
* The epoch changed from 230 to 268 during attempted verification, so “epoch unchanged” is not established for the entire gate.
* Directly changing the shared canonical `outbound_pause_control.state` during tests is risky. It temporarily changes the real pause state outside the production coordinator path. At minimum, all workers must be stopped and test safeguards enabled; ideally the test should use an isolated/mock pause authority instead.
* The current 17-hold baseline must remain identical—not merely remain at a count of 17—across complete gate runs.

Next, have Replit do this:

```text
Do not mark #1613 complete yet.

1. Stop the application/workers so port 5000 is free.
2. Record the exact pre-test baseline:
   - outbound pause state and epoch
   - every active hold_id, job_key, hold_type, source_key and source_epoch
3. Run:
   GHL_TEST_MODE=true bash scripts/run-pre-deploy.sh
   Allow enough time for the complete gate; do not substitute targeted tests.
4. Record the same post-test snapshot and prove:
   - all suites passed
   - pause state is unchanged
   - epoch is unchanged
   - the exact active-hold set is unchanged
   - no release_pending or correlation_id=NULL test artifacts were added
5. Repeat the complete pre-deploy run once more and prove the same invariants.
6. Investigate the observed epoch change from 230 to 268. Identify the exact code/test responsible; do not attribute it to ordinary coordinator synchronization without evidence.
7. Review setTestPauseState(). Because it directly mutates the shared canonical pause record, confirm that no live worker can observe the temporary unpaused state. If that cannot be guaranteed, replace it with test-isolated pause-state injection/mocking.
8. Keep the 17 legitimate global_outbound holds active while the system is paused. Do not remove them.

Return the complete gate outputs and before/after hold fingerprints.
```

After that passes: commit, merge, publish, verify the production SHA, and run the final 39-row runtime packet.
