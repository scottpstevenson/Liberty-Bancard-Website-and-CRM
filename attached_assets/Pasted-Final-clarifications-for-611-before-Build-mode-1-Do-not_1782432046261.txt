Final clarifications for #611 before Build mode:

1. Do not instrument server/services/contactability.ts directly in this wave. Record channel_allowed/channel_blocked only at verified actual execution points, such as sequence-worker send/block result points or other concrete send handlers. Avoid dry-run contamination and duplicate events.
2. recordAnalyticsEvent() must sanitize metadata recursively, not just top-level fields. Reject or strip PII-like keys including email, phone, firstName, lastName, fullName, name, address, fileName, statementFileName, statementText, rawPayload, notes, messageBody, body, content, replyContent.
3. Do not create a broad unauthenticated frontend analytics ingestion endpoint unless one already exists. For Wave 8, form_abandoned can remain GA4/fbq/client-side only. analytics_events should focus on server-side CRM milestones and safe booking callback events.
4. Do not double-fire statement upload completion events. Add trackStatementUploadCompleted() for the canonical event going forward, keep existing trackStatementUpload() for backward compatibility, and ensure the new dashboard funnel counts only the canonical completion event.
5. Do not duplicate PEWC events. Map canonical constants to the existing event names in shared/analytics-events.ts. Only patch PewcCheckbox if the existing consent_field_interaction payload cannot distinguish checked vs unchecked.
6. GET /api/public/booking-confirmed must not update contacts.lifecycleStage from unsigned query params alone. It may record appointment_booked analytics with bookingTrackingId/UTM. Only update contact lifecycle if contact resolution is strong and server-verified through a GHL webhook/contact ID match.
7. Keep the OperatorDashboard Conversion tab v1 minimal: KPI cards, funnel chart, landing page table, UTM source/campaign table, CTA performance table, form performance table, date filter, warnings, and empty states. No dashboard route creation, no broad OperatorDashboard refactor, no advanced interactions.
8. For the analytics_events migration, follow the repo’s exact Drizzle migration/journal pattern and verify npx tsx scripts/migrate.ts applies 003x once in correct order.
9. All analytics recording must be non-blocking. recordAnalyticsEvent()/recordCrmEvent() failures must log internally and never break forms, uploads, contactability, offer routing, sequence execution, proposal generation, or stage progression.
10. No PII in GA4, fbq, or analytics_events. No phone/email/name/file names/raw statement content. No SMS/AI voice/RVM/GHL workflow behavior changes.

Approved for Build mode only after these clarifications are reflected.