You’re right. I gave you the safety objection but not the actual second task. Here are the two tasks I’d use.

788A — Route-Preserving Sidebar IA Cleanup

788A — Route-Preserving Sidebar IA Cleanup

What & Why

The admin dashboard sidebar currently exposes ~85 items across too many groups, making the CRM hard to navigate. This task performs the first safe IA cleanup pass: reorganize the sidebar, remove true duplicate nav entries, move misplaced items, surface PCI Assessment, and preserve every existing route.

This task does not build new hub pages yet. It prepares the sidebar and route map for the consolidation pass in #788B.

Done Looks Like

* Admin sidebar is reorganized into 7 groups.
* No group has more than 12 visible items.
* Admin sidebar is reduced as much as safely possible without building new tab hubs.
* True duplicate sidebar entries are removed.
* Misplaced items are moved to better groups.
* PCI Assessment becomes reachable from the sidebar.
* Agent sidebar remains clean and role-appropriate.
* Every existing /dashboard/* route still works.
* No page files are deleted.
* No backend routes are changed.
* No child page functionality is changed.

Required Groups

Use these 7 groups:

WORK
MERCHANT OPS
LEAD ENGINE
AUTOMATION
INTELLIGENCE
GROWTH & PARTNERS
SYSTEM

Required Placement Decisions

Use this placement unless preflight proves a better existing grouping:

Merchant Health → MERCHANT OPS only
Reporting → INTELLIGENCE
Growth Metrics → INTELLIGENCE for now; will become Reporting tab in #788B
Win/Loss → INTELLIGENCE for now; will become Reporting tab in #788B
Outreach Analytics → INTELLIGENCE for now; will become Reporting tab in #788B
Automation overview → first item in AUTOMATION
Email Health + Inbox Health → AUTOMATION together
GHL Settings / Workflow IDs / Sequence Guide → SYSTEM for now; will become GHL Integration hub in #788B
Content tools → SYSTEM for now; will become Content hub in #788B
Marketing Playbook + Growth Playbook → GROWTH & PARTNERS for now; will become Playbooks hub in #788B
PCI Assessment → SYSTEM or near Underwriting, but it must be reachable

Required Changes

1. Remove true duplicate sidebar entries only

Remove entries that point to the exact same route and are not role-specific variants.

Do not remove intentional role-specific variants like:

My Pipeline / Pipeline
My Contacts / Contacts
My Tasks / Tasks
My Calendar / Calendar

2. Surface PCI Assessment

Add /dashboard/pci-assessment to the sidebar.

Preferred location:

SYSTEM → PCI Assessment

or near Underwriting if that better matches current IA.

3. Move misplaced items

Move:

Automation overview → first item in AUTOMATION
GHL admin/config pages → SYSTEM
Marketing/Growth Playbooks → GROWTH & PARTNERS
Content tools → SYSTEM
Email Health + Inbox Health → same group
Reporting/Growth/Win-Loss/Outreach Analytics → INTELLIGENCE

4. Preserve agent sidebar

Agent-specific nav must remain scoped and usable.

Preserve:

My Day
My Contacts
My Pipeline
My Tasks
My Calendar
Collateral / Training / Knowledge resources if currently agent-visible

Do not expose admin-only System pages to agents.

Out of Scope

Do not:

build Outreach Hub
build GHL Integration hub
build Reporting hub
build Content hub
build Playbooks hub
build Merchant Success hub
merge page components
delete routes
delete page files
change backend routes
change role guards
change page functionality

Kill Lines

STOP if any existing App.tsx /dashboard route is removed.
STOP if any old sidebar page becomes unreachable.
STOP if PCI Assessment is still hidden.
STOP if Merchant Health appears in two sidebar groups.
STOP if Outreach Analytics appears in both Lead Engine and Intelligence as duplicate sidebar entries.
STOP if agent sidebar loses My Day or role-specific work items.
STOP if role guards are weakened.
STOP if this task builds the tab hubs reserved for #788B.

Validation

Run:

npx tsc --noEmit --skipLibCheck
npx tsx scripts/smoke-role-guards.ts

Run these checks:

grep -c "path=\"/dashboard/" client/src/App.tsx
grep -rn "href.*dashboard" client/src/pages/DashboardLayout.tsx
grep -rn "pci-assessment" client/src/App.tsx client/src/pages/DashboardLayout.tsx

Manual checks:

Admin sidebar uses 7 groups.
No group has more than 12 visible items.
PCI Assessment is reachable.
Every old route still opens.
Agent sidebar still shows agent work items.
Admin-only System pages are not exposed to agents.

Final Report Required

Return:

Old admin sidebar item count:
New admin sidebar item count:
Largest group count:
Routes removed from App.tsx:
PCI Assessment location:
Duplicate entries removed:
Items moved:
Agent sidebar changes:
Role-guard smoke result:
Files changed:

⸻

788B — Route-Preserving Hub Consolidation

788B — Route-Preserving Dashboard Hub Consolidation

What & Why

After #788A reorganizes the sidebar safely, this task performs the actual consolidation pass. It creates lightweight route-preserving hub pages for overlapping sidebar areas so the admin sidebar can drop from ~85 items to ~50 items without deleting functionality.

This task must preserve every existing route. Old routes may remain mounted or redirect to the new hub with a tab parameter.

Done Looks Like

* Admin sidebar is reduced to ≤55 visible items.
* No group has more than 12 visible items.
* Existing pages are consolidated behind lightweight tab hubs.
* Every old route still works or redirects cleanly.
* No backend routes are changed.
* No page files are deleted.
* Child page business logic is not rewritten.
* Agent sidebar remains role-appropriate.

Required Hubs

Create lightweight hubs for these areas only:

Outreach Hub
GHL Integration
Playbooks
Content
Reporting
Merchant Success
Onboarding

Each hub should render existing components as tabs or route to existing components. Do not rewrite child page internals.

⸻

Hub 1 — Onboarding

Consolidate:

/dashboard/onboarding
/dashboard/onboarding-board

Required behavior:

One sidebar entry: Onboarding
Two internal views/tabs if both pages are distinct
Old /dashboard/onboarding-board route still works or redirects to /dashboard/onboarding?tab=board

Kill line:

STOP if either Onboarding view becomes unreachable.

⸻

Hub 2 — Outreach Hub

Consolidate sidebar access for:

Outreach
Outreach Command
Outreach Prospects / Cold Leads

Important decision:

Outreach Analytics belongs under Reporting, not Outreach Hub, to avoid duplicate ownership.

Required behavior:

One sidebar entry under LEAD ENGINE: Outreach Hub
Tabs: Overview, Command, Prospects
Old routes still work or redirect to /dashboard/outreach-hub?tab=...

Do not change outreach execution logic.

⸻

Hub 3 — GHL Integration

Consolidate:

GHL Settings
GHL Workflow IDs
GHL Sequence Guide

Required behavior:

One sidebar entry under SYSTEM: GHL Integration
Tabs: Settings, Workflow IDs, Sequence Guide
Old routes still work or redirect to /dashboard/ghl-integration?tab=...

Do not change GHL payload mapping, workflows, or credentials.

⸻

Hub 4 — Playbooks

Consolidate:

Marketing Playbook
Growth Playbook

Required behavior:

One sidebar entry under GROWTH & PARTNERS: Playbooks
Tabs: Marketing, Growth
Old routes still work or redirect to /dashboard/playbooks?tab=...

Do not rewrite playbook content.

⸻

Hub 5 — Content

Consolidate:

Content Engine
Blog Generator
LinkedIn Composer
Blaze.ai Marketing

Required behavior:

One sidebar entry under SYSTEM or CONTENT if a separate group already exists
Tabs: Content Engine, Blog Generator, LinkedIn Composer, Blaze.ai
Old routes still work or redirect to /dashboard/content?tab=...

Do not change AI generation, Blaze integration behavior, or content publishing logic.

⸻

Hub 6 — Reporting

Consolidate:

Reporting
Growth Metrics
Win/Loss Analysis
Outreach Analytics

Required behavior:

One sidebar entry under INTELLIGENCE: Reporting
Tabs: Overview, Growth Metrics, Win/Loss, Outreach Analytics
Old routes still work or redirect to /dashboard/reporting?tab=...

Do not change analytics queries or reporting calculations.

⸻

Hub 7 — Merchant Success

Consolidate:

Review Requests
Testimonial Submissions
NPS/CSAT
Retention Campaigns

Required behavior:

One sidebar entry under GROWTH & PARTNERS: Merchant Success
Tabs: Review Requests, Testimonials, NPS/CSAT, Retention
Old routes still work or redirect to /dashboard/merchant-success?tab=...

Do not change review, testimonial, NPS, or retention logic.

⸻

Sidebar Updates

After hubs are created, remove absorbed standalone sidebar entries.

The sidebar should keep only:

Outreach Hub
GHL Integration
Playbooks
Content
Reporting
Merchant Success
Onboarding

instead of their child entries.

Route Preservation

Every old route must still work.

Allowed:

Old route remains mounted
Old route redirects to hub with tab query

Not allowed:

Old route returns 404
Old page disappears with no replacement

Out of Scope

Do not:

delete page files
delete App.tsx routes
change backend routes
change page data fetching
change role permissions
rewrite child page internals
merge business logic
change CRM workflows
change AI behavior
change GHL behavior
change reporting calculations

Kill Lines

STOP if any existing /dashboard route returns 404.
STOP if a hub rewrites child page business logic.
STOP if Outreach Analytics is duplicated in both Outreach Hub and Reporting as a sidebar item.
STOP if Win/Loss remains both standalone and a Reporting tab.
STOP if Merchant Health is added to two groups.
STOP if admin sidebar remains above 55 visible items.
STOP if any group has more than 12 visible items.
STOP if role guards are weakened.
STOP if agent sidebar exposes admin-only hub pages.

Validation

Run:

npx tsc --noEmit --skipLibCheck
npx tsx scripts/smoke-role-guards.ts

Run route/nav checks:

grep -c "path=\"/dashboard/" client/src/App.tsx
grep -rn "href.*dashboard" client/src/pages/DashboardLayout.tsx
grep -rn "outreach-hub\|ghl-integration\|playbooks\|content\|merchant-success" client/src/App.tsx client/src/pages/DashboardLayout.tsx

Manual checks:

1. Admin sidebar has ≤55 visible items.
2. No group has more than 12 visible items.
3. Onboarding hub renders both views.
4. Outreach Hub renders Overview, Command, Prospects.
5. GHL Integration renders Settings, Workflow IDs, Sequence Guide.
6. Playbooks renders Marketing and Growth.
7. Content renders Content Engine, Blog Generator, LinkedIn Composer, Blaze.ai.
8. Reporting renders Reporting, Growth Metrics, Win/Loss, Outreach Analytics.
9. Merchant Success renders Review Requests, Testimonials, NPS/CSAT, Retention.
10. Each old child route still opens or redirects cleanly.
11. Agent sidebar remains role-appropriate.

Final Report Required

Return:

Hubs created:
Old routes preserved:
Old routes redirected:
Admin sidebar item count before:
Admin sidebar item count after:
Largest group count:
Absorbed standalone entries removed:
Agent sidebar changes:
Role-guard smoke result:
Files changed:

That’s the actual two-task split. 788A gives immediate sidebar cleanup. 788B does the consolidation work.