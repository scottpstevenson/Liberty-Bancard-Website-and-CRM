TypeScript clean. Rebuild dist and run pre-deploy simultaneously:
Pre-deploy is running. While it completes, I'll tackle the WizardFlags DB pool timeout — it's the source of all those [WizardFlags] Failed to read override errors flooding production:
I see the DB backup is still failing with +statement_timeout in the dev server — that means the tsx restart happened before my fix landed. Let me verify the fix is in place, check pre-deploy status, and handle the stale QA contacts circuit breaker issue in parallel:
19/19 suites passed — the gate is green. Let me update memory with the wizard-flag fix and health monitor improvements, then suggest publish:
All done. Here's what was completed this session:

HealthMonitor hardening (3 new improvements):

Startup grace period — first 3 minutes after a deploy restart, critical alert emails are suppressed (checks still run and store results). No more false-positive incidents on every deployment.
RESOLVED recovery notification — when a critical check flips back from degraded → ok, one recovery email goes out (subject: [HealthMonitor] RESOLVED — ...), rate-limited to 15 minutes.
Alert subject cleanup — subject now reads CRITICAL — (was just Critical alert —) to match the recovery subject's RESOLVED — convention.
WizardFlags log storm fix:

Cache TTL extended from 30 s → 5 min (flags rarely change)
Each DB read races against a 2-second timeout — fails fast instead of holding a pool slot for 10 s during enrichment load
Timeout warnings are now deduplicated (logged on first occurrence, then every 20th) instead of 7 lines every 30 seconds
Pre-deploy gate: 19/19 suites green ✅

The production server is still running the previous dist (you can see the old WizardFlags errors in its logs). Deploying will push the rebuilt dist with all fixes — HealthMonitor flooding resolved, GHL task sync fixed, backups restored, and the log storm suppressed.
Everything is complete and ready. The pre-deploy gate passed 19/19 and the publish suggestion is waiting for your action whenever you're ready.