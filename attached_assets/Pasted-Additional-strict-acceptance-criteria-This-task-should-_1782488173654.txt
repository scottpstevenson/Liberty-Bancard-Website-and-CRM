Additional strict acceptance criteria

This task should not be marked complete unless the following are visually verified at 375px, 390px, and 430px mobile widths:

1. Statement Fee Audit card

* The “Savings opportunity / On review / Identified” row is fully visible.
* No word, badge, token, value, or status text extends beyond the right edge of the card.
* No text is hidden with truncate, overflow-hidden, clipping, or ellipsis.
* Long row content stacks naturally instead of being forced into a fixed-width desktop-style row.
* The row remains readable even if the value/status text is longer than expected.
* Status labels use a compact, flat token style and align consistently with the rest of the rows.

2. Report/card backgrounds

* No ruled-paper or ledger-line background appears behind readable text inside report cards.
* Decorative grid/ledger textures may only remain as low-opacity background accents outside readable card content.
* The Statement Fee Audit, Review Contents, Estimate Preview, Calculator Result, Timeline, Rate Comparison, and Yours to Keep cards must use clean readable content surfaces.

3. Review Contents card

* Mobile layout is stacked, not squeezed into a fixed chip column.
* Each item reads as: label token, title, description.
* Labels align consistently and do not float awkwardly.
* Descriptions wrap naturally and remain inside the card.

4. Timeline card

* Mobile layout is single-column.
* Each step reads as a compact row with the step marker and text aligned cleanly.
* No 2-column mobile timeline grid remains.
* No timeline text is hidden by the sticky CTA.

5. Sticky mobile CTA

* The sticky CTA must no longer feel like a giant blue/navy dock.
* The surrounding sticky container should be minimal, translucent or light, and low-height.
* The CTA button may remain blue, but the total sticky area must feel compact and secondary to page content.
* Sticky CTA must not cover card content, form fields, timeline rows, footer links, or card bottoms.
* If the CTA still visually dominates the screen after the change, reduce height, padding, shadow, and surrounding background further.

6. No layout masking

Do not solve overflow by hiding content. Do not use overflow-hidden, truncate, max-width clipping, or ellipsis as the primary fix for broken report rows. Fix the layout with proper mobile stacking, flex-wrap, min-w-0, max-w-full, and whitespace-normal.

7. Final response required from implementation

When complete, report:

* exactly which card/report sections had ledger-texture removed
* how the Statement Fee Audit row layout changed
* how tokens/stamps were restyled
* how the sticky CTA was reduced
* which mobile breakpoints were checked
* whether any remaining Home page section still has known visual issues