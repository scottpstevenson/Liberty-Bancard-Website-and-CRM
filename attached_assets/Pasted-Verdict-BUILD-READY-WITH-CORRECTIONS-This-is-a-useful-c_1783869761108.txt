Verdict: BUILD-READY WITH CORRECTIONS

This is a useful, contained UI task. Keep it focused. Five details need correction before implementation.

Corrections to send back

1. Verify the actual page and endpoint ownership.
Confirm:
- the routed file is client/src/pages/dashboard/Campaigns.tsx, not a duplicate client/src/pages/Campaigns.tsx;
- GET /api/campaigns/readiness-stats exists and is role-gated;
- POST /api/campaigns/readiness-backfill already exists and returns the persisted run.
Do not create parallel endpoints if Phase 2 already established canonical owners.
2. Remove the ambiguous “current campaign threshold” metric.
This panel is global, while readinessThreshold is campaign-specific.
Do not display:
“Campaign threshold currently set to X”
unless the panel is scoped to one selected campaign.
For the global dashboard, either:
- show fixed grade/readiness distribution only; or
- add a local, clearly labeled “Estimate at threshold” control that does not mutate any campaign.
Do not imply there is one system-wide campaign threshold if none exists.
3. Do not add “Force restart” without proven backend semantics.
Phase 2 uses DB-level singleton ownership and run fencing.
A UI button must not overwrite or interrupt an active run directly.
Only expose force/restart if the canonical endpoint:
- atomically marks the prior run interrupted;
- creates a new runId;
- prevents the prior worker from writing;
- records an operator audit event.
Otherwise omit force restart from this task.
4. Use one polling contract.
The brief says both 30 seconds and 10 seconds.
Use:
- no active polling when idle/complete/failed, or normal query freshness;
- 10-second polling only while status === "running".
Stop immediately when the run becomes terminal.
5. Share readiness labels and types.
Do not duplicate REASON_LABELS between ReadinessCard and this panel.
Extract a shared client-safe module containing:
- readiness response types;
- grade metadata;
- reason-code labels;
- safe fallback humanization for unknown future codes.
The dashboard must not invent a second readiness contract.

Additional corrections

- Percentages must use an explicit denominator:
  grade percentages = scored contacts only.
- Display null and stale contacts separately; do not include them in grade percentages.
- “Campaign-ready” is misleading unless a threshold is selected.
  Prefer “At or above threshold” and explicitly state that contactability still applies.
- The Run Backfill CTA should appear for null OR stale/model-mismatched records,
  not only null scores.
- Backfill POST must handle 409/already-running cleanly by refreshing the current run,
  not displaying a generic failure.
- Loading and endpoint failure states must not render zero counts as real data.
- Verify the claimed nightly BullMQ backfill actually exists before mentioning it in UI
  copy or marking scheduling out of scope.

Kill lines

STOP if a global threshold is presented when thresholds are campaign-specific.
STOP if “campaign-ready” implies permission to send without contactability evaluation.
STOP if Force Restart bypasses runId fencing or singleton ownership.
STOP if the dashboard creates a second backfill execution path.
STOP if reason labels/types are duplicated across components.
STOP if null or stale contacts are included in A–F percentage denominators.
STOP if polling continues every 10 seconds when no run is active.
STOP if a missing/error response is displayed as zero contacts.

Validation

- Grade counts reconcile to totalScored.
- A+B+C+D+F percentages equal 100% of scored contacts.
- Null and stale counts are shown independently.
- Selected threshold count matches the server aggregate or verified calculation.
- Threshold copy states that contactability still applies.
- Agent sees the panel but no mutation controls.
- Admin/manager can start one backfill.
- Second start during an active run returns/reconciles the existing run.
- Polling starts only while running and stops at completion/failure.
- Unknown reason codes render a readable fallback.
- Endpoint error shows an error state, not zero-valued metrics.

Final status: BUILD-READY WITH CORRECTIONS

The main correction is to avoid pretending there is one global campaign threshold and to omit unsafe force-restart behavior unless Phase 2’s fenced restart contract already supports it.