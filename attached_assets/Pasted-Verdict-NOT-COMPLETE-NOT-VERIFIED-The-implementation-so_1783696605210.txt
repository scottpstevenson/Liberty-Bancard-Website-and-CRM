Verdict: NOT COMPLETE / NOT VERIFIED

The implementation sounds directionally correct, but this completion report does not prove the task is finished or safe to clean up production data.

Corrections to send back

Do not mark Task 918 complete yet. Provide the following evidence:
1. Exact file:line evidence for every change:
   - _aiOpsRunning declaration, early-return guard, and finally reset
   - SLA createTask() with dealId
   - bulkSoftDeleteTasks() implementation
   - IStorage signature
   - single DELETE using softDeleteTask()
   - bulk-delete route authorization, validation, audit write, and response
   - Tasks selection UI and loaded-row-only select-all behavior
2. Correct this statement:
   “dealId makes tasks traceable by source.”
   dealId provides deal traceability, not source attribution. Human-created tasks can also have dealId.
3. Explain the count discrepancy.
   The original incident referenced approximately 2,427 garbage tasks, but the dry-run found only 669 active matches.
   Provide:
   - count of matching soft-deleted tasks,
   - count of matching hard-deleted/missing tasks if recoverable from audit/history,
   - title variants that may not match the LIKE pattern,
   - status distribution,
   - exact reason the current cleanup population is 669.
4. Provide proof of the two clean SLA cycles.
   Include timestamps and before/after task counts showing:
   - runScheduledAiOps() executed twice,
   - no new matching tasks were created,
   - no overlapping invocation created duplicates.
   The cleanup must not run before this evidence exists.
5. Add or provide an automated re-entrancy regression test.
   Required:
   - first invocation remains active,
   - second invocation returns without creating tasks,
   - an exception in the first invocation resets _aiOpsRunning,
   - a later invocation can run normally.
6. Verify bulk deletion authorization scope.
   isDashboardUser proves endpoint access, but provide evidence that dashboard users are intentionally allowed to delete every submitted task ID. If task access is tenant/team scoped, the update query must enforce that scope.
7. Verify requested-count semantics after deduplication.
   The audit entry must clearly distinguish:
   - submitted count,
   - unique validated count,
   - actually deleted count.
   Do not report duplicated input IDs as separate requested records without making that explicit.
8. Fix the confirmation copy.
   Since this is soft deletion, replace:
   “Cannot be undone.”
   With:
   “This will remove N tasks from active task views. Confirm the reviewed selection before continuing.”
9. Confirm whether the bulk-delete UI can handle all 669 tasks.
   The report says select-all is loaded-row-only. Prove:
   - how many rows are loaded at once,
   - whether pagination or a 500-row cap exists,
   - how the operator will delete all 669 without accidentally missing rows,
   - selection resets correctly after each batch.
10. Provide the audit-log record from a test bulk deletion, including actor and requested/unique/deleted counts.

Kill lines

STOP if the 2,427 versus 669 discrepancy is unexplained.
STOP if cleanup begins before two verified clean SLA cycles.
STOP if no automated test proves the guard resets after exceptions.
STOP if bulk deletion can mutate out-of-scope task IDs.
STOP if the UI implies all 669 tasks are selected when only loaded rows are selected.
STOP if “Cannot be undone” remains on a soft-delete action.
STOP if the final cleanup count differs from the reviewed dry-run count without explanation.

Validation still required

pnpm typecheck
pnpm lint
pnpm test
npx tsx scripts/smoke-role-guards.ts

Also provide:

- targeted SLA overlap test
- targeted bulk-delete validation/auth test
- single-delete soft-delete test
- already-deleted count test
- duplicate-ID count test
- audit-log assertion
- UI loaded-row selection test

Final status: NOT COMPLETE / NOT VERIFIED

Do not run the 669-task cleanup yet. The biggest blocker is the unexplained reduction from approximately 2,427 reported garbage tasks to 669 active matches, followed by missing proof that duplicate generation has actually stopped.