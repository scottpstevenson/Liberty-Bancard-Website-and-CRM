Verdict: the revised Task #1730 is build-ready after two surgical contract corrections. It incorporates the prior 16 audit findings; no additional redesign or task split is needed.

### 1. Correct the business/contact/link sequence

The plan says authority-mediated linking precedes contact projection, but the existing link authority requires both an existing contact and a contact-owned source event.

Replace the sequence with:

> Resolve/replay the canonical business → create/replay the local-only contact without directly setting `business_id` → persist the contact source event → execute the authority-mediated contact/business link decision → apply local-only field CAS projections → persist projection receipts → create the winning-email validation intent.

If matching is ambiguous, leave the contact unlinked and review-required. Do not claim terminal canonical linkage.

Update this wording in:

* F12
* Done Looks Like
* External Side-Effect Ordering
* Phase 5
* Integration certification

### 2. Govern every automatic contact-writer hook

The current contact writer automatically performs more than generic validation:

* Creates the default validation intent.
* Enqueues current validation.
* Enqueues readiness recalculation.
* Enqueues lead scoring.

A CRO-03B contact could therefore be evaluated or scored before projection and linking finish.

Add this requirement:

> CRO-03 writer mode must suppress the generic validation intent and post-commit validation enqueue, and defer readiness and lead-scoring hooks during intermediate create/update operations. After canonical projection and link disposition are terminal, enqueue exactly one coalesced lead-scoring request. Readiness must resume through the existing current-generation validation/readiness authority after the winning-email result, never from an intermediate CRO-03 write. Replays and crash recovery must not duplicate these hooks.

Add tests proving:

* Zero generic validation intents.
* Zero readiness/scoring jobs from intermediate writes.
* Exactly one final scoring request.
* Readiness remains blocked until current-generation validation completes.
* No CR-04, CR-06, campaign, enrollment, GHL, or outbound mutation occurs.

### Editorial cleanup

Change:

* `# #1730` → `#1730`
* `BUILD-READY WITH BLOCKING CORRECTIONS` → `BUILD-READY — AUDIT CORRECTIONS INCORPORATED`

After those edits, I would approve [Task #1730](sandbox:/workspace/scratch/fd5f269d0f7b/upload/Pasted%20markdown%2820260830-040257%29.md) for implementation without another planning audit.

This remains CRO-03B: code-complete with real provider transport denied. CRO-03C is still responsible for credentials, governed live canaries, production activation, and proving Apollo, Outscraper, Serper, AI enrichment, and ZeroBounce against their real APIs.
