Must-fix before go-live (security / data integrity)

#1175 — Merchant can finalize another merchant's application if they guess the application ID. The autosave route is correctly token-gated but the finalize path needs an ownership check to prevent cross-merchant data access.

#1175, #1145, #1085, #1130 are the four compliance/security tests that should pass before the first live contact hits the platform:

#1145 — Verify duplicate-send protection actually blocks a second send after the first succeeds (test is missing, behavior unproven)
#1085 — Verify the no-prospect-send guard blocks real-prospect emails before outbound goes live
#1130 — Enrollment job silently enrolls contacts even when the target sequence is paused mid-job (race condition)
#700 — The guarded unique-index migration (tasks_sla_stalling_active_unique) needs to be verified against the production database, not just dev — the migration was designed for new-rows-only but was never confirmed to build cleanly on the live dataset.

#430 — The Drizzle-kit snapshot is out of sync (manual migrations weren't auto-generated). This doesn't break anything today but means drizzle-kit generate will produce wrong diffs or hang on the next schema change — a timebomb for the next sprint.

High-value feature gaps (will be noticed in week 1)

#980 — GHL identity conflicts (24k+ log entries/session from a shared placeholder GHL ID) surface nowhere in the UI. Admins can't detect or fix merged-contact issues without querying the DB directly.

#995 — Reps can't manually trigger a readiness re-score from a contact profile — they have to wait for the nightly backfill job. Common enough that reps will ask for it immediately.

#905 — AI proposals silently fall back to template content when the AI call fails mid-chain. No error indicator is shown. Reps send what they think is an AI-written proposal but it's a generic template.

#470 — The GHL circuit breaker never auto-clears. Once it trips it stays open until someone manually resets it via API, even if GHL fully recovers.

#710 — The nightly abandoned-upload reclaim worker only runs on-demand, not automatically. Missed statements pile up silently if ops don't remember to trigger it.

Meaningful-but-not-urgent (week 2–3)

#	Item
#1040	Weekly system audit defaults to "every 7 days from first run" — should lock to Monday morning
#1070	System audit failing subsystem cards have no Quick Fix links — admins hunt for the right settings page
#1250	NPS survey emails — created in DB but never confirmed they actually send
#350	Growth KPI data can't be exported to CSV for team reporting
#260	No AI spend alert — budget overruns discovered only by checking the OpenAI billing dashboard
#300	Residual imports and CSV uploads have no audit trail
#1100	ZeroBounce MX verification not run on master_leads email list before outreach
#180	Partner approvals don't trigger an invite email with a set-password link
#370	No real-time alert when a merchant opens their savings results link
Nice-to-have / polish (month 1)

#500 — "Areas We Serve" missing from nav Resources dropdown
#590 — /free-smart-terminal not linked from nav or Solutions dropdown
#100 — Equipment page not printable as a product sheet
#750 — Other tab-heavy CRM pages not verified for the mobile tab-overlap bug that was fixed in Pipeline
#250 — Document access history not shown to admins
Short answer: There are 6 things that should be done before the first real prospect hits the system (#1175, #1145, #1085, #1130, #700, #430). Everything else is feature depth that makes the product stronger but won't break go-live. Want me to start building them in priority order?