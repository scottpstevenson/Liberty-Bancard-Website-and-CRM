Correction: Replit manages this application’s database. I do not use or administer Neon directly, and I will not create an external database account.

Task #1674 is already merged. This is Task #1675, the isolated post-merge verification task.

Do not ask me to provide a TEST_DATABASE_URL unless you have first exhausted Replit-native disposable test provisioning.

PROVISION THE TEST DATABASE YOURSELF

Preferred method: temporary local PostgreSQL inside the Replit workspace.

1. Check whether postgres, initdb, pg_ctl, createdb, and psql are available.
2. If unavailable, use Replit’s supported Nix dependency mechanism to launch PostgreSQL 16 or 17 for this verification process.
3. Do not permanently change application dependencies merely to retain the temporary database.
4. Create a temporary data directory under /tmp using mktemp.
5. Initialize a new local PostgreSQL cluster.
6. Start it on an unused localhost port or private Unix socket.
7. Create an empty database named:

   bt12_revenue_ci

8. Construct TEST_DATABASE_URL internally for that local database.
9. Do not print the connection URL or credentials.
10. For migration and test processes only, set:

   NODE_ENV=test
   DATABASE_URL=$TEST_DATABASE_URL

11. Apply migrations with the repository’s canonical migration runner.
12. Run SELECT current_database() and require bt12_revenue_ci.
13. Run the complete #1675 isolated BT-12 integration/recovery suite.
14. Stop PostgreSQL after testing.
15. Remove only the exact temporary /tmp database directory after validating its path.

SAFETY RULES

- Do not use the Replit development DATABASE_URL.
- Do not use or modify the Replit production database.
- Do not overwrite the permanent DATABASE_URL environment variable.
- Do not use db push.
- Do not copy development or production data.
- Do not use schema-only isolation as a substitute.
- Do not use Redis; this #1675 suite does not require it.
- Do not create an external Neon, Supabase, or other provider account.
- Do not ask the user to manually construct a database URL.
- Never run destructive commands against an unresolved path.

FALLBACK CHECK

If temporary local PostgreSQL genuinely cannot run:

1. Inspect whether the Replit DEVELOPMENT database role has CREATE DATABASE permission without displaying credentials.
2. If permitted, create a separate database named bt12_revenue_ci on the development database server.
3. Confirm it is a distinct database using SELECT current_database().
4. Run migrations and tests only against that new database.
5. Drop only bt12_revenue_ci after successful cleanup.
6. Never modify or clean the normal development database.

If neither local PostgreSQL nor a separate Replit-managed database can be provisioned, report the exact failed commands and platform restriction. Do not merely report that TEST_DATABASE_URL was absent, because provisioning it is part of #1675’s verification work.

FINAL REPORT

Include:

- Provisioning method used.
- PostgreSQL version.
- Redacted localhost/managed host classification.
- SELECT current_database() result.
- Migration command and exit code.
- Integration suite command and exit code.
- Confirmation development and production databases were untouched.
- Temporary database shutdown and cleanup evidence.
- Complete #1675 gate table.
- Final verification verdict.

Do not reopen or modify #1674 unless #1675 proves a specific merged-code defect requiring an additive correction.