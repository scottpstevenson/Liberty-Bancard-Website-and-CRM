Approved — build-ready.

This is a real correctness fix, and it is tightly scoped. It should be fixed before the final pilot validation suite because the Sales Prep cache test is now correctly catching it.

Verdict

Build #745.

The right fix is:

first generate call returns the persisted cache row timestamp
not a separately computed service timestamp

I’d prefer this implementation:

setContactAiCache(...)
→ insert/update row
→ .returning()
→ generateSalesPrepAi() returns returnedRow.generatedAt

That makes the database/cache record the source of truth.

Add this final guard

Final Addendum to #745

Approved to build.

Required behavior

The first uncached generation and all later cache-hit reads must return the exact same generatedAt value for the same cached Sales Prep record.

Preferred implementation:

generateSalesPrepAi()
→ generate fixture/OpenAI output
→ storage.setContactAiCache(...)
→ storage returns persisted row from .returning()
→ response uses persistedRow.generatedAt

Alternative acceptable implementation:

generateSalesPrepAi() creates generatedAt once
→ passes generatedAt into setContactAiCache()
→ storage persists that exact generatedAt
→ response returns that same value

Do not keep two independent new Date() calls for the same cache record.

Kill lines

STOP if first POST /generate and second cache-hit POST /generate return different generatedAt values for the same cache record.
STOP if GET /sales-prep returns a different generatedAt than either POST call for the same cache record.
STOP if this task changes sales-prep prompt/content/generation logic.
STOP if this task disables caching or forces regeneration to pass the test.
STOP if this task changes unrelated SDR storage behavior.

Validation

Run:

TEST_MODE=true SKIP_AI=true DRY_RUN=true npx tsx scripts/test-sales-prep.ts
npx tsc --noEmit --skipLibCheck

The Sales Prep smoke test must prove:

first POST /generate generatedAt === second POST /generate generatedAt
second POST /generate fromCache === true
GET /sales-prep cached.generatedAt === first POST /generate generatedAt

Final report must include:

Storage timestamp source:
First generatedAt:
Second generatedAt:
GET cached generatedAt:
Equality assertion passed: yes/no

This is a small fix, but it matters: if the UI says something was generated at a time, that timestamp should be the persisted record’s timestamp, not a transient service-side timestamp.