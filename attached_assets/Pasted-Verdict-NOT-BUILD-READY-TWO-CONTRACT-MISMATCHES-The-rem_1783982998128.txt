Verdict: NOT BUILD-READY — TWO CONTRACT MISMATCHES

The remaining work should be small, but this brief appears to reference fields and values that may not match what Task #973 actually shipped.

Required corrections

1. Verify importBatchId exists on contacts.
    The context says Task #973 added batch fields to prospects and sunbiz_entities. If contacts.importBatchId does not exist and is not accepted by GHL mappings, adding "importBatchId" to REPLIT_OWNED_FIELDS is meaningless.

The guard should protect only real contact fields that GHL inbound could otherwise write. Likely required:

"sourceCategory"
"primarySourceCategory"
"primarySourceType"
"primarySourceEventId"

Add importBatchId only if it is an actual contacts column and part of the inbound mapping surface.

2. Use the exact shipped source taxonomy.
    The test expects:

ghl_sync
public_form
manual_crm

Earlier plans used website_form, not public_form. The test must read the actual database check constraint and shared constants rather than inventing “six canonical values.”

Do not build a test that disagrees with the migration.

3. Do not verify a TypeScript constant through source-text inspection.
    Export a small predicate or the protected-field set if needed, then test behavior:

* send an inbound GHL update containing forged provenance;
* verify the persisted provenance remains unchanged.

That proves the guard works. Merely asserting that a string appears in a set does not prove the sync path strips it.

4. Separate schema tests from full intake integration tests.
    Creating records through GHL inbound, public forms, CSV import, COREVT upload, and manual CRM requires very different fixtures and side-effect controls. Do not call that a simple eight-assertion smoke test unless all those paths already have safe test harnesses.

A focused test can cover:

* check constraint rejects an invalid source category;
* GHL inbound cannot overwrite protected provenance;
* direct canonical writer assignments for GHL, form, and manual origins;
* imported prospect and Sunbiz rows carry their execution references.

5. Fix the prospect batch identity assertion.
    Confirm the actual schema type and ownership:

* prospects.importExecutionId may reference prospect_lists.id as an integer;
* sunbiz_entities.importExecutionId may reference a UUID import_executions.id.

Do not assert that both equal an import_executions UUID unless the shipped schema proves that.

Corrected scope to send back

PREFLIGHT REQUIRED — verify the shipped provenance contract before implementing the final guard and tests.

1. Read the actual Task #973 migration and shared/schema.ts and report:

* every provenance column on contacts;
* the exact contacts.source_category check-constraint values;
* the type and FK owner of prospects.importExecutionId / importBatchId;
* the type and FK owner of sunbiz_entities.importExecutionId;
* which provenance fields are present in the GHL inbound field mapping.

2. Update REPLIT_OWNED_FIELDS to include every real, server-owned contact provenance field that GHL inbound could otherwise mutate.

At minimum verify:

* sourceCategory
* primarySourceCategory
* primarySourceType
* primarySourceEventId

Add importBatchId only if it is an actual contacts field and mapped by the inbound sync.

3. Add behavior-based tests:

* Existing contact with website/manual provenance receives a GHL payload attempting to change provenance → all protected fields remain unchanged.
* A newly created GHL contact receives the exact canonical GHL source value.
* Canonical form and manual contact writes produce the exact shipped source values.
* Invalid sourceCategory is rejected by the real DB constraint.
* Prospect CSV import and COREVT upload persist the correct execution reference using their actual schema types.

4. Import canonical source constants from the shipped provenance module. Do not hardcode a separate six-value list in the test.
5. Keep this task limited to the GHL protection patch and focused regression coverage. Do not change provenance schema, intake ownership, contactability, or import behavior unless the tests expose a genuine implementation bug.

Kill lines

* STOP if importBatchId is added to a contact guard when no such contact field exists.
* STOP if the test hardcodes public_form while the canonical value is website_form, or vice versa.
* STOP if prospect integer execution IDs are compared to UUID import executions.
* STOP if success is proven only by checking set membership rather than persisted behavior.
* STOP if the smoke test triggers real GHL, outbound, or uncontrolled bulk-import side effects.

Final status: PREFLIGHT REQUIRED. Once the exact shipped schema and taxonomy are confirmed, the implementation should be a very small patch.