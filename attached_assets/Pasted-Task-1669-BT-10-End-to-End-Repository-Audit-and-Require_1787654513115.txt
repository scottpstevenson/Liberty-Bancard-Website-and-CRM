Task #1669 / BT-10 — End-to-End Repository Audit and Required Corrections

Audit date: 2026-08-25 (America/New_York)
Repository: Libertybancard1/LibertybancardCRM
Audited branch: main
Audited commit: c0753e9c7bcb6ca35b4e85c8b54c02736ce54b1e (Harden queue ownership and truthful diagnostics)
Migration head: 0157_csv_import_execution_projection
Task reviewed: #1669 - Provider Health and Readiness

1. Verdict

NOT BUILD-READY AS WRITTEN. DO NOT IMPLEMENT OR MERGE THE ATTACHED PLAN WITHOUT THE P0/P1 corrections in this report.

The task identifies the correct strategic problem, preserves the right Serper/GHL/BT-04 through BT-09 authorities, and correctly reserves live provider yield for runtime verification. It is not yet an implementation-safe plan because it:

omits active paid provider paths (Apollo and Proxycurl) and other enrichment sources from the mandatory inventory;

understates fail-open ZeroBounce behavior in campaign and sequence execution;

does not require a materialized preview cohort, so preview and queue can select different contacts;

does not explicitly replace existing process-memory provider, preview, scoring, readiness, and GHL runs with durable jobs;

leaves validation-on-create/change vulnerable to post-commit enqueue loss and direct email writers;

risks creating new provider evidence/state owners that compete with BT-04 reachability, BT-07 identity, and BT-08 GHL projections;

treats one blanket prerequisite Boolean as suitable for computations with different semantics;

misses route-level credit-spend authorization gaps, false provider accounting, and fabricated healthy status fallbacks.

The correct preflight status is NOT BUILD-READY — CORRECTED DESIGN REQUIRED. After all P0 corrections are incorporated and the file-level implementation contract is accepted, the safe code-owned control plane can be built without making live provider calls. The final merge verdict for that future implementation may be SAFE TO MERGE — RUNTIME VERIFICATION PENDING only after its deterministic gates pass.

No repository files were modified during this audit.

2. Repository and evidence baseline

GitHub main and the clean local clone both resolve to c0753e9c7bcb6ca35b4e85c8b54c02736ce54b1e.

The migration journal has 162 entries. There are 162 numbered SQL migrations, with no missing journaled files and no unjournaled numbered SQL files. The next migration is therefore 0158_* at this exact baseline, subject to rechecking immediately before implementation.

git status --short --branch returned ## main...origin/main; git diff --check returned no output.

Dependencies were not present in the audit clone. TypeScript and repository script execution were therefore not represented as passed. An attempted npm run check/npx tsx run could not proceed without dependency/network setup. Static migration parity was independently checked with Node.

No provider secrets, control rows, production database rows, provider balances, paid calls, GHL mutations, backfills, or sends were accessed or executed.

Earlier runtime counts in the supplied audits remain historical evidence, not exact-release proof. The supplied runtime register itself marks provider health/yield, coverage, worker health, and GHL completion as current-release verification requirements.

3. Corrected verified-from-code table

ID

Task claim

Verdict

Current repository reality

VFC-01

Serper has a canonical fail-closed boundary

Confirmed with status-surface corrections

Network access is centralized in serper-gateway.ts, but serper.ts:getSerperUsage() fabricates a zero-use/default-budget response on control-read failure, and the legacy /api/serper/reset-usage route bypasses the audited optimistic reset route.

VFC-02

Serper distinguishes useful result, zero result, and failure

Confirmed in the enrichment path

Preserve the gateway and cooldown behavior as regressions. Do not generalize its wrappers' null result as the universal outcome model.

VFC-03

Every paid provider has equivalent controls

False, and inventory is incomplete

ZeroBounce, Outscraper, Apify, Apollo, and Proxycurl have direct clients without a common circuit/operation ledger. Apollo is silently invoked in pilot discovery when configured. OpenAI/Facebook Ad Library must be explicitly classified in or out based on whether their output can affect an in-scope field.

VFC-04

Provider validation failure cannot allow outreach

False, broader than stated

Campaign queue, campaign send, sequence pre-enrollment, and per-step sequence validation write or allow unknown/unverified; campaign/sequence exception paths can proceed. verifyEmail() returns failure as data, so exception-only handling does not catch the normal failure shape.

VFC-05

ZeroBounce batch validation is durable and nonduplicating

Confirmed for the dedicated batch worker only

zerobounce_campaigns/runs/attempts and BullMQ are good foundations. Direct campaign, sequence, manual, and discovery validation paths are not covered by that claim.

VFC-06

Universal append-only observations and arbitration exist

False

Identity and reachability evidence exist in separate authorities, while provider enrichers still write fields directly. BT-10 must integrate with those authorities; it must not create a competing identity, reachability, or GHL projection owner.

VFC-07

Vertical taxonomy is singular and versioned

False, not partial

Readiness has one hard-coded set, vertical-constants.ts has the canonical coarse set, and lead-scoring.ts has two incompatible coefficient maps. The proposed task mentions only part of this three-way drift.

VFC-08

Readiness and scoring consume one evidence prerequisite

False, with a semantic correction

Data readiness is intentionally a pure completeness score and should stay computable independently. Lead-score publication and operational eligibility need versioned, purpose-aware prerequisite decisions. One blanket Boolean would incorrectly couple consent/contactability to readiness or scoring.

VFC-09

Readiness/scoring backfills are safely durable and filtered

False for execution; partial for pagination

Readiness has a DB run/cursor but starts with setImmediate; scoring uses a JSON system_settings run and setImmediate. Readiness skips a batch after three failures and can finish despite stale rows. Neither selector implements the required evidence prerequisites.

VFC-10

Preview and execution share the cohort

False

They rerun similar mutable queries. A valid preview can be consumed before a non-durable queue operation; execution may include contacts not in the preview and explicitly accepts a count difference. Readiness filtering is optional when the threshold is null.

VFC-11

GHL reconciliation is bounded, durable, and identity-clean

Partial

Durable contact projections and the circuit exist, but direct single-contact sync and two broad fire-and-forget full-sync routes remain. The periodic worker also retains a legacy unsynced-contact scan after processing projections.

VFC-12

BT-09 contracts are usable

Confirmed, but not fully consumed

Per-contact readiness/scoring jobs use the enrichment queue; many in-scope admin runs still execute in process memory. The task must name and migrate each of them, not just say all new work is durable.

4. P0 blockers and exact corrections

P0-01 — ZeroBounce provider failure can authorize email outreach

Evidence:

server/services/campaign-engine.ts:26-39 documents Phase 3 as fail-open.

campaign-engine.ts:71-74 treats persisted unknown and unverified as already validated and immediately safe.

campaign-engine.ts:98-106 returns true on a thrown provider error.

campaign-engine.ts:109-146 writes the provider status before deciding whether it blocks; a normal failure result from verifyEmail() is status: unknown plus reason, not an exception.

campaign-engine.ts:963-1009 writes the lazy-validation result and continues on budget denial, failed claim, unknown, or exception.

server/services/sequence-worker.ts:456-505 and :837-887 write unknown/unverified and allow the sequence to proceed; the per-step catch explicitly proceeds.

server/services/contactability.ts:741-760 does not block unknown or unverified.

server/services/sdr/zerobounce.ts:10-61 marks missing-key, HTTP, timeout, and exception outcomes as unknown, and maps a provider-reported raw unknown to unverified.

Required correction:

Replace status-only decisions with a typed canonical result, for example:

validated_valid

validated_invalid

validated_risky (including catch-all/unknown policy results)

no_result only where the provider contract genuinely supports it

not_configured

budget_blocked

circuit_blocked

rate_limited

provider_rejected

timeout

transport_failure

parse_failure

ambiguous_billing

A provider failure/blocked/indeterminate result must never update the positive email projection and must never make an email send eligible.

A positive validation must bind to the normalized email lookup token/hash, provider, policy version, observation ID, and freshness expiry. If the current email token differs or evidence is stale, the send is deferred and a new durable validation intent is created.

Centralize this in one validation eligibility service. Campaign queue, campaign send, sequence pre-enrollment, sequence step execution, manual validation, discovery activation, and batch validation must all call it. Remove handwritten status tests from those paths.

Use isRetryableZbFailure() only as a transitional compatibility helper. The final result type must make provider completion and provider failure mutually exclusive at the type/schema level.

Route delivery/reachability outcomes through BT-04 recordReachabilityObservation(); do not let a new provider projection directly compete with consent_subject_reachability. A valid-email provider result is evidence for eligibility, not consent.

Define the risk policy for catch-all and provider unknown. Unless a reviewed policy explicitly permits them, they are not marketing-send approval.

Remove raw email values from routine audit details; store contact ID, hashed email token, operation ID, outcome code, and policy version.

Acceptance: every unavailable, blocked, failed, stale, mismatched, risky, or indeterminate validation path leaves the message/enrollment deferred or blocked and creates no send-eligible projection.

P0-02 — The provider inventory misses active paid bypasses

Evidence:

server/services/sdr/apollo.ts:225-315 directly calls Apollo and collapses missing configuration and remote failures to [].

server/services/sdr/lead-finder.ts:1220-1228 exposes Apollo in discovery.

lead-finder.ts:1375-1383 silently enables Apollo in pilot discovery whenever a key exists, although Apollo is not user-selected.

lead-finder.ts:1453-1499 performs the paid Apollo call and follow-on ZeroBounce calls.

server/services/linkedin-enrichment.ts:29-68 directly calls Proxycurl; :70-152 writes returned fields directly.

server/routes/contacts.ts:1253-1278 lets any dashboard user start single/bulk Proxycurl enrichment, with bulk execution in a detached promise.

The supplied enrichment runtime audit explicitly lists Apollo, Proxycurl, AI, ZeroBounce, Serper, Outscraper, Apify, OSM, YellowPages, BBB, RDAP, JSON-LD, contact-page, Sunbiz, and related web sources as runtime/provider surfaces.

Required correction:

Before schema or service implementation, add a checked-in provider/source manifest with one row for every external input that can write or approve an in-scope contact, merchant, readiness, score, campaign, or GHL field.

Minimum manifest rows: Serper, ZeroBounce, Outscraper, Apify, Apollo, Proxycurl, Sunbiz, Google/direct web search, first-party website/contact page, OSM/Overpass, YellowPages, BBB, Yelp, Facebook page scraping, LinkedIn page scraping, RDAP, JSON-LD, and Facebook Ad Library. Classify OpenAI enrichment/classification call sites as in-scope when they can affect these fields; explicitly list unrelated content/advisor/social-publication calls as excluded.

Each row must declare: capability, paid/free/local-parser, canonical adapter, allowed call sites, secret names, external side effect, durable operation type, local budget unit, provider billing evidence availability, timeout/retry policy, normalized outcome set, field candidates emitted, retention/redaction rule, and test transport.

Apollo and Proxycurl are mandatory paid gateways in this task. The P0 raw-paid-provider scanner must fail on imports/URLs outside their approved adapter files.

Remove silent Apollo activation. Every paid provider operation must be explicitly selected by an authorized server-side command and constrained by a persisted plan/budget.

Do not place simple local parsers under a paid circuit, but do normalize their attempt/outcome/evidence so no_result cannot mask an HTTP or parsing failure.

Acceptance: a source scanner built from the manifest identifies every in-scope network call, and no paid call can occur outside an authorized, durable, budget-claimed operation.

P0-03 — Preview and queue do not share a stable cohort

Evidence:

server/services/campaign-engine.ts:415-560 reruns a live mutable selector during queueing.

campaign-engine.ts:574-577 explicitly permits new/changed contacts and count differences.

campaign-engine.ts:653-786 computes preview separately.

server/routes/campaigns.ts:223-243 consumes the preview before calling the queue function and treats differing counts as expected.

campaign-engine.ts:672-675 and :471-474 make readiness filtering optional when the campaign threshold is null.

server/storage/contacts.ts:500-585 applies only part of the required predicate and uses OFFSET pagination over mutable ordering.

Required correction:

Materialize campaign_preview_members (or equivalent) with preview_id, contact_id, eligibility decision, reason codes, normalized email token, contact version/mutation timestamp, taxonomy version, prerequisite policy version, readiness model version, and contactability snapshot reference.

Preview completion and its member rows must commit durably. Use keyset pagination; never OFFSET over a mutable scored ordering for cohort construction.

Queueing must create a durable campaign_queue_run linked to the unconsumed preview, atomically claim/consume the preview and run, and queue only contacts present in that preview.

Immediately before each outbound row, re-evaluate the current purpose-specific prerequisite and contactability. A changed contact may be excluded with a reason; no contact absent from the preview may be added.

Make outbound-row creation idempotent with a database uniqueness fence on campaign/contact/step or a stable equivalent.

On partial failure/restart, the durable queue run resumes the unprocessed preview members. Do not permanently consume an unrecoverable preview before a durable run owns it.

A null numeric readiness threshold must not disable the core eligibility prerequisite. Numeric completeness thresholds may remain optional, but versioned data/identity/provenance/validation prerequisites are mandatory for marketing.

Acceptance: queued_contact_ids is always a subset of preview_eligible_contact_ids; every exclusion is reasoned; replay cannot duplicate outbound rows; no new live-query contact can enter the run.

P0-04 — In-scope accepted runs are not durably owned

Evidence:

Readiness backfill: server/services/contact-readiness-backfill.ts:48-115 creates a DB run then uses setImmediate.

Scoring backfill: server/services/contact-scoring-job.ts:235-345 stores progress in system_settings, relies partly on _scoringJobRunning, and uses setImmediate.

Campaign preview: server/services/campaign-engine.ts:789-871 creates running state then uses setImmediate despite route comments calling it restart-safe.

Contact enrichment: server/routes/contacts.ts:1168-1211 returns started and detaches enrichContactBatch().

Proxycurl bulk: server/routes/contacts.ts:1266-1278 returns started and detaches the paid operation.

Sunbiz re-enrich/mass enrich/classification/pipeline: server/routes/prospects.ts:1647-1713 detaches long work.

Discovery/pilot/re-enrichment: server/routes/sdr.ts:1042-1054, :1071-1088, and :2408-2409 detach work; a logical lease alone does not make an HTTP-accepted run restart-resumable.

GHL full sync: server/routes/integrations.ts:192-201 detaches broad sync work.

Required correction:

Add every in-scope long operation to the BT-09 logical-job manifest and give it a durable run/command row plus a BullMQ producer/worker. Reuse the existing enrichment queue only if documented capacity and isolation are acceptable; otherwise add a reviewed queue within BT-09 connection limits.

Routes create/claim durable commands and enqueue via getQueueManagerProducers()/ready-only access. Queue unavailability must return 503 with run state unavailable or leave an atomically recoverable deferred intent; never return started: true without durable ownership.

Required run states: pending, deferred, blocked, running, degraded, cancel_requested, cancelled, failed, complete, and interrupted only where stale-lease recovery has proved interruption. Keep provider-attempt outcomes separate from run states.

Each run has an immutable request/idempotency key, bounded cohort/query fingerprint, actor, policy/model versions, cursor, lease/claim token, heartbeat, counters by outcome, sanitized failure code, cancellation state, and terminal reconciliation check.

Migrate the listed existing BT-10-owned paths; the phrase “all new runs” is insufficient.

Acceptance: killing the app after route acceptance cannot lose the run; restart resumes or truthfully terminalizes it; duplicate route submission does not duplicate provider calls or field effects.

P0-05 — Validation-on-create/change is not atomic or universal

Evidence:

server/services/contact-writer.ts:174-310 correctly commits contact, source event, identity observation, audit, and GHL projection together.

contact-writer.ts:313-325 enqueues readiness/scoring only after commit and swallows failure.

contact-writer.ts:401-443 does the same for updates.

server/storage/contacts.ts:187-245 remains a direct create/update path. Multiple routes/services still call storage.updateContact; at least one direct path updates email (server/routes/partner-orgs.ts:1014).

There is no atomic email-validation intent created with the new/current email value.

Required correction:

Add a durable validation intent/outbox row in the same transaction that creates a real email or changes its normalized token. Do not call the provider inside the contact transaction.

Use a stable key derived from subject/contact, email lookup token, validation policy version, and purpose—not raw email, filename, or a time window.

Create or advance a contact mutation generation. The worker may project a result only if the contact still resolves to the same live contact and email token/generation; otherwise mark the attempt superseded.

Route every email-bearing writer through the canonical mutation service or add a database-owned outbox trigger plus static writer scanner. Direct generic email writes must fail the scanner.

Convert readiness/scoring hooks to durable transactional intents or a recoverable outbox. The current best-effort post-commit enqueue is not sufficient.

Acceptance: every real create/material email change has exactly one recoverable current-generation validation intent; a queue outage cannot permanently lose it; stale provider responses cannot overwrite the new address's evidence.

P0-06 — GHL reconciliation still has direct/broad mutable paths

Evidence:

server/routes/integrations.ts:101-112 directly calls upsertGhlContact() for a manual sync.

integrations.ts:192-201 exposes two broad detached full-sync routes.

server/services/ghl-sync.ts:2517-2573 processes durable projections, then also scans legacy unsynced contacts.

contact_provider_projections is already the BT-08 durable local-to-GHL projection owner.

Required correction:

Do not repurpose contact_provider_projections as enrichment observation storage. Preserve it as the GHL command/projection owner.

Convert manual sync and broad sync routes into bounded reconciliation commands with preview, explicit maximum, keyset cursor, actor/reason, direction, purpose, identity/provenance criteria, and durable queue ownership.

Outbound commands create/reuse projection intents and let the existing claimed worker perform I/O. Inbound reconciliation writes through writeContact/source-event authority.

Remove or permanently disable the broad legacy scan once every eligible unsynced record is represented by an explicit projection/reconciliation command. Until then, report it as a separate compatibility lane with bounded batch and truthful counters.

Apply purpose-aware policy: transactional/local-first projection may accept a newly submitted unknown commercial class where BT-06 permits it; a marketing/backfill cohort requires production classification. Do not impose one blanket production rule that breaks legitimate inbound intake.

Acceptance: no admin route performs GHL I/O after returning; every mutation is attributable to a durable projection/reconciliation row; identity conflicts and provider circuit blocks become explicit terminal/deferred outcomes.

5. P1 corrections

P1-01 — Normalize outcomes without losing provider-specific meaning

Outscraper (server/services/sdr/outscraper.ts:135-194), Apify (server/services/sdr/apify.ts:132-166), and Apollo (server/services/sdr/apollo.ts:225-315) collapse missing configuration, HTTP failure, timeout, and true zero yield into empty arrays.

Apify then records the same failed call as successful zero-yield in searchYelp()/searchFacebook() (apify.ts:169-199), producing double/false accounting.

Sunbiz fetchPage() (server/services/sunbiz-enrichment.ts:291-300) collapses HTTP/timeout into null across Google, websites, social/directory pages, and Sunbiz.

Correction: adapters return a discriminated result, never a bare array/null. One operation has exactly one terminal outcome and separate accepted/yield counts. A legitimate zero result requires a completed provider/parser response. Missing config, circuit/budget block, HTTP error, timeout, parse error, and database projection failure remain distinct.

P1-02 — Durable atomic budgets must be operation controls, not billing claims

Outscraper, Apify, and Apollo usage tracking is non-atomic read/modify/write JSON in system_settings.

ZeroBounce has an atomic local count, but the source comment says 500/day while the code defaults to 5,000 (zerobounce-daily-limiter.ts:10,16). Missing explicit configuration therefore enables a large default spend.

A reserved local unit does not prove a provider charge. A timeout may be billing-ambiguous.

Correction: use provider/capability control rows with atomic reservation before I/O, immutable attempt linkage, committed/released/ambiguous states, currency/unit metadata, and separate provider-reported billing evidence. Require an explicit configured budget to enable a paid gateway; missing control/config is not_configured/blocked, not a permissive default. Do not automatically replay an ambiguous paid attempt unless the provider supports the same idempotency key or an operator resolves it.

P1-03 — Preserve and repair Serper truthfulness

server/services/serper.ts:70-92 returns a default healthy-looking budget/zero-use structure on a control-row or DB read error.

/api/serper/reset-usage allows admin/manager and calls an unaudited legacy counter reset, while /api/admin/serper/reset-window already provides admin-only reason/CAS handling.

Correction: status reads return unavailable with observedAt and sanitized reason code when the authority cannot be read. Remove/deprecate the legacy reset route or route it through the admin-only audited CAS command. Preserve the canonical gateway and raw-fetch scanner.

P1-04 — Fix provider route authorization

Required server-side matrix:

Action

Minimum role

Additional requirements

View sanitized provider/run health

Admin or manager

Never expose keys, raw payloads, emails, or provider bodies

View cost/budget/ambiguous billing

Admin

Read-only; exact window and units

Enable/disable paid gateway

Admin

Reason, optimistic version, audit

Reset budget/circuit window

Admin

Reason, expected version/window, audit

Test provider connection

Admin

Explicit one-call cap, durable attempt, no user-supplied target containing PII

Single paid enrichment

Admin or explicitly delegated manager

Confirm provider/cost cap; durable operation

Bulk enrichment/validation/backfill

Admin

Preview token, typed confirmation, exact max, durable run

Cancel run

Admin

Token-fenced cancellation and audit

Manual field/taxonomy override

Admin or approved data steward

Before/after, reason, append-only decision

GHL bounded reconciliation

Admin

Preview token, direction, max, purpose, circuit/pause checks

Current gaps include any-dashboard-user Proxycurl spending (contacts.ts:1253-1278), authenticated-only discovery source health (sdr.ts:1270-1292), and legacy manager-level Serper reset (contacts.ts:1236-1242). UI visibility is not authorization.

P1-05 — Use a purpose-aware prerequisite authority, not one universal Boolean

Create one versioned evaluator API with a required purpose, for example:

data_completeness_compute: record exists and is live; no consent/contactability gate.

lead_score_publish: production classification (unless an explicitly approved noncommercial/test purpose), current live identity, traceable provenance, resolved required input conflicts, compatible taxonomy/model inputs; provider validation only for provider-derived fields that require it.

marketing_preview / marketing_queue / marketing_send: production classification, current nonconflicted identity, traceable provenance, current email-token validation evidence, no unresolved field conflict, compatible taxonomy/model, plus BT-04 contactability at the appropriate dry-run/enforcement boundary.

ghl_transactional_projection: live identity and provenance with BT-06 transactional semantics; does not blanket-require production.

ghl_marketing_reconciliation: production classification plus identity/provenance and the approved reconciliation cohort.

provider_backfill: approved source cohort, identity/provenance, provider policy, budget, and capability-specific prerequisites.

Return eligible, versioned reason codes, evidence references/high-water marks, and the evaluated subject generation. Fail closed on authority/database unavailability for side-effecting purposes.

Do not make consent, contactability, lifecycle, lead score, or promotional eligibility inputs to the pure data-completeness score. Conversely, do not let a high completeness score stand in for those independent gates.

P1-06 — Correct all three taxonomy owners

Canonical coarse set: server/services/sdr/vertical-constants.ts:7-29.

Readiness hard-coded set: server/services/contact-readiness.ts:63-67.

Lead-scoring coefficient vocabularies: server/services/lead-scoring.ts:53-63 and :126-136.

Correction: the canonical resolver and coarse constants own stored taxonomy/version. Readiness imports that authority. Lead scoring uses a versioned compatibility/coefficient adapter from canonical coarse vertical to scoring segment; it must not treat a missing map key as silent Other without recording the mapping. Persist taxonomy/model versions with readiness/score decisions. Manual vertical authority remains strongest and is never overwritten by a provider/backfill.

P1-07 — Do not award success-like score on dependency failure

server/services/lead-scoring.ts:263-307 assigns five engagement points when database reads fail. This converts unavailability into positive score.

Correction: return a typed unavailable input and suppress/mark the score publication as deferred/blocked. Do not publish a new score/tier from incomplete dependency reads. Store model version, input high-water marks, prerequisite decision, and a reasoned no-write outcome.

P1-08 — Backfill terminal truth must reconcile every claimed row

server/services/contact-readiness-backfill.ts:203-209 can skip a batch after three failures, advance the cursor, and later complete. This can make a run appear complete with stale contacts.

Correction: create per-range/per-subject dispositions or a durable run-item ledger. complete requires eligible_total = succeeded + intentionally_skipped_terminal and zero unresolved/retryable items. Persistent batch failure ends degraded or failed, not complete. Preview and execution must share a frozen eligibility cutoff/query fingerprint. Cancellation and lease loss are separate terminal reasons.

P1-09 — Integrate evidence with existing authorities

The new provider evidence layer must:

reference BT-07 contact identity observation IDs/tokens rather than copy raw identity values;

emit delivery/reachability facts through BT-04 recordReachabilityObservation();

use BT-06 authorizeUse() for classification purpose decisions;

consume BT-08 source events/provenance and preserve contact_provider_projections for GHL work;

use the canonical vertical resolver/manual authority;

present candidate field values to one deterministic field-arbitration service; it must not directly last-write fields.

Manual decisions win until explicitly superseded by another authorized manual decision. Source authority, confidence, freshness, provider completion, and subject generation are deterministic inputs. An unresolved conflict produces no field projection and blocks only purposes that require that field.

P1-10 — Define schema constraints before implementation

At the rechecked 0157 baseline, reserve 0158_bt10_provider_readiness_control (final name may change, but sequence must be revalidated). The migration should provide narrowly separated concepts:

provider_controls: one row per provider/capability; enabled state, circuit state/version, atomic budget window/units, thresholds, policy version, last outcome timestamps.

provider_operations: immutable request key/fingerprint, provider/capability, subject/cohort, actor, bounded max, claim/lease, attempt count, budget reservation state, run state, normalized terminal outcome, sanitized error code, timestamps.

provider_observations: append-only subject/field candidate evidence referencing the operation and identity/source evidence; normalized value or protected value reference, confidence, freshness/expiry, outcome, policy/schema version. No secrets/raw provider bodies.

field_arbitration_decisions: append-only decision version, winning observation/manual decision, candidate set hash, reason code, policy version, subject generation; a separate current projection may cache the winner.

validation_intents or a general durable provider intent: unique by subject/email-token/policy generation and recoverable before enqueue.

eligibility_decisions/snapshots and campaign_preview_members/queue runs for cohort parity.

Use database CHECK constraints for provider, capability, run state, attempt outcome, budget state, and subject type; foreign keys; partial unique indexes for active claims; idempotency uniqueness; keyset indexes; and append-only permissions/triggers where appropriate. Do not put all states in one unconstrained polymorphic JSON table. Do not fabricate historical provider outcomes. Historical rows remain unknown/unverified until a bounded approved run produces evidence.

P1-11 — Redact provider errors and define retention

Apollo, Apify, Outscraper, and Proxycurl log or return raw response/error text (apollo.ts:266-274, apify.ts:153-165, outscraper.ts:170-193, linkedin-enrichment.ts:40-42,149-152).

Apify and ZeroBounce place secrets in query URLs; those URLs must never enter logs/errors.

Current campaign/sequence audits include raw emails.

Provider objects expose/store rawData in several discovery adapters.

Correction: adapter errors expose only normalized code, HTTP class/status, retry-after, provider request ID if safe, and truncated allowlisted metadata. Raw bodies and URLs containing tokens are never logged or placed in API responses. Define per-provider raw payload retention and PII policy; observation/status tables store only necessary normalized evidence or protected references.

P1-12 — Make test transport denial universal

The current CI manifest documents provider denial for GHL and Serper and assumes OpenAI is not called. BT-10 covers more providers.

Correction: every in-scope adapter accepts/inherits an injectable transport; deterministic tests install a global fail-fast network guard and per-provider fakes. Add a manifest-driven raw-client scanner and a test that deliberately attempts each real adapter without a fake and proves it is denied. Register the new BT-10 suite and scanners in both scripts/ci-suite-manifest.ts and scripts/pre-deploy.ts.

6. P2 corrections and clarity improvements

Fix stale comments and names that claim DB-backed setImmediate work is restart-safe (campaigns.ts:254-256, campaign-engine.ts:789-791).

Standardize run vocabulary. Do not mix error and failed, or call queue/config unavailability failed when no provider attempt occurred.

Fix /api/sdr/discovery/key-status to use APIFY_API_TOKEN, matching the adapter (server/routes/sdr.ts:1061-1067).

Remove the unused static getQueueManager import from contact-readiness.ts:15 after the enqueue contract is repaired.

Make provider UI state carry observedAt, exact policy/window, configured/enabled/circuit/budget distinctions, last completed outcome, yield, and stale/unavailable status. A configured key is not health.

Use 202 Accepted only when a durable command exists; return its run/operation ID and polling URL. 200 started: true followed by a detached promise is prohibited.

Add operator-visible reason counts for exclusions, ambiguous billing, superseded results, conflicts, deferred queue, circuit block, budget block, retryable failure, and terminal failure.

The exact-release checklist must name query/route, time window, release SHA, provider/capability, denominator, attempts by outcome, yield fields, billed/local units, p50/p95 latency, circuit transitions, oldest backlog, heartbeat age, reconciliation totals, and stop thresholds.

Update the task's file list to include at minimum apollo.ts, linkedin-enrichment.ts, consent-authority.ts, contact-writer.ts, commercial-classification-authority.ts, contact-identity.ts, contact_provider_projections schema/migration, provider-spending routes, logical-job manifest, and campaign preview schema/storage.

Add the supplied finding DAT-14 to the primary finding map for validation-on-create; the supplied next-tranche document explicitly maps automatic validation-on-create to OUT-06 and DAT-14.

7. Correct source-of-truth contract

Concern

Canonical owner / integration rule

Provider call control

One adapter/gateway per manifest provider/capability; SerperGateway remains Serper owner

Provider operation/run

New BT-10 durable operation/run service using BT-09 queue and lease contracts

Provider evidence

New append-only provider observations referencing existing source/identity evidence

Email validation eligibility

One BT-10 validation decision service; provider evidence is not consent

Delivery/reachability

BT-04 recordReachabilityObservation() and consent_subject_reachability

Commercial classification

BT-06 commercial-classification-authority.ts

Identity/current subject

BT-07 identity observations, redirects, and conflict detection

Intake/provenance

BT-08 writeContact, contact_source_events, primary-source authority

GHL local-to-provider work

Existing contact_provider_projections and GHL claimed worker/circuit

Vertical

Canonical resolver + vertical-constants.ts, with versioned scoring adapter

Data readiness

contact-readiness.ts, pure completeness only

Lead score

lead-scoring.ts plus guarded/versioned publication

Operational eligibility

New purpose-aware evaluator that consumes, never replaces, the above authorities

Contactability/consent

BT-04 evaluateContactability() at outreach boundaries

Campaign cohort

Durable preview-membership and queue-run owner

Queue/worker ownership

BT-09 QueueManager, logical manifest, durable claims/heartbeats

8. Correct execution and failure ordering

For each paid/provider operation:

Authenticate and authorize the actor/purpose.

Resolve the current live subject and prerequisite decision.

Insert or idempotently recover the durable operation and bounded target manifest.

Atomically claim the operation and local budget unit before provider I/O.

Call through the canonical adapter with timeout and sanitized correlation ID.

Classify exactly one attempt outcome.

In one local transaction, append observation(s), run deterministic arbitration, apply a projection only if the subject generation still matches, record budget accounting state, and advance the run item/cursor.

If provider succeeds but the DB commit fails, retain/recover the same operation and do not blindly repeat a potentially billed call. Reapply the captured sanitized response only if retention policy and encrypted storage permit; otherwise mark reconciliation required.

If the DB claim succeeds but the provider fails, record failure and keep fields/eligibility unchanged.

On timeout after request transmission, mark billing ambiguous. Do not auto-retry without provider idempotency or operator resolution.

Audit writes that are compliance/authorization evidence commit with the command. Diagnostic logging may remain best-effort but cannot change the operation result.

9. Corrected build sequence

Implementation must proceed in this order:

Rebaseline main, migration head, working tree, and existing tests. Stop on drift.

Commit the provider/source manifest and manifest-driven scanners; close Apollo/Proxycurl/AI/ad-detector scope explicitly.

Define the normalized operation/attempt/outcome and purpose-aware prerequisite types with no network implementation.

Add migration 0158_* for controls, operations, observations, arbitration, validation intents, eligibility/cohort/run state; add schema and integrity tests.

Add durable BT-09 job handlers/producers and logical-manifest entries for validation, provider operations, readiness/scoring backfills, campaign preview/queue, bounded GHL reconciliation, and every in-scope detached admin run.

Build ZeroBounce gateway/decision integration first; remove all fail-open campaign/sequence paths and bind evidence to current email token/generation.

Add atomic create/change validation intents and canonical email-writer scanner.

Wrap Outscraper, Apify, Apollo, and Proxycurl; then normalize free web/source outcomes. Remove silent Apollo use and false/double accounting.

Add append-only observations and deterministic field arbitration integrated with BT-04/06/07/08 authorities.

Consolidate taxonomy and add the versioned scoring adapter. Keep readiness pure.

Add purpose-aware eligibility snapshots, suppress unsafe score publication, and replace backfill selectors/runs.

Materialize campaign previews and make durable queue runs consume only preview members with send-time revalidation.

Replace direct/broad GHL routes with bounded durable reconciliation commands and preserve existing circuit/projection safeguards.

Add sanitized operator APIs/UI, role gates, typed confirmations, cancellation, and truthful status.

Run deterministic/fake-transport tests, full regressions, static scanners, typecheck, build, migration replay/integrity, RBAC/contracts/pre-deploy/invariants, and diff review.

Merge only if all code gates pass. Execute VG-03 live provider/cohort/GHL checks later under separate exact-release approval and budget.

10. Mandatory deterministic test matrix

At minimum, the future implementation must prove:

every manifest provider routes through its sole adapter and real network is denied in tests;

missing config, disabled, circuit open, budget blocked, rate limited, provider 4xx/422, 429, 5xx, timeout, network failure, parse failure, zero result, risky result, and useful result are distinct;

Apify/Apollo/Outscraper/Proxycurl have one terminal attempt count per call and no read/modify/write budget race;

a duplicate command/retry produces no double call when recoverable and no duplicate observation/projection;

an ambiguous paid timeout is not automatically replayed;

provider success plus DB failure is reconciled without last-write-wins or blind charge duplication;

contact creation/email change atomically produces the correct validation intent; stale-email responses are superseded;

ZeroBounce failure/unknown/catch-all/stale evidence blocks or defers queue, send, and sequence paths;

provider results never overwrite a manual or stronger observation; conflict is explicit and deterministic;

BT-04 reachability and BT-07/08 evidence remain canonical and are not duplicated;

readiness remains pure completeness while operational eligibility remains separate;

lead-score dependency failure produces no positive default or new published tier;

all canonical verticals map deterministically into readiness and scoring at a recorded version;

readiness/scoring/provider/GHL backfills are keyset-paginated, bounded, resumable, cancellable, lease-fenced, and reconciliation-complete;

preview is durable; queue IDs are a subset; changed members are excluded; replay cannot duplicate outbound rows;

all paid/spending/override/recovery/reconciliation routes enforce the role matrix and audit requirements;

queue unavailable, circuit blocked, budget blocked, provider failed, degraded, and complete states render truthfully;

Serper gateway/cooldown, existing ZeroBounce campaign worker, GHL circuit/projections, contactability, classification, identity merge, intake/provenance, queue ownership/topology, migration integrity, CSRF, and pre-deploy behavior regress cleanly.

Register the BT-10 suite and its raw-provider/email-writer/status scanners in both scripts/ci-suite-manifest.ts and scripts/pre-deploy.ts. Tests that merely exist but are not mandatory do not satisfy a kill line.

11. Required implementation kill lines

The future build fails if any of the following remains:

Provider failure, timeout, quota/circuit block, invalid identity, stale evidence, unresolved required conflict, or missing prerequisite can approve a field, publish a score as current, enter GHL marketing reconciliation, or authorize outreach.

Any paid in-scope provider call exists outside its manifest adapter/operation/budget boundary.

A paid call can be silently activated by presence of a secret.

A route can return accepted/started before recoverable durable ownership exists.

A current email can be considered validated by evidence for a different normalized email token/generation or expired policy window.

Provider observations directly compete with BT-04 reachability, BT-07 identity, BT-08 provenance/GHL projections, or manual field authority.

Preview execution can add a contact that was not a preview member.

Backfill/run complete can coexist with unresolved claimed/retryable items.

Provider test execution can reach the public network.

Runtime success/yield/coverage/GHL completion is claimed from configured keys, counters, timestamps, mocks, or stale reports.

12. Exact-release runtime verification (not authorized by this audit)

After a safe implementation is merged and deployed, a separate approved VG-03 run must capture:

exact deployed SHA and migration head;

provider/capability enabled/configured/circuit/budget state without secret values;

a preapproved small cohort and maximum provider units/currency;

attempts by normalized outcome, useful yield by field, no-result rate, error rate, p50/p95 latency, local reservations, provider-reported billed units where available, and ambiguous charges;

validation evidence freshness/token match and coverage by approved source cohort;

readiness and lead-score null/blocked/current/version distributions by approved cohort;

queue depth, oldest age, heartbeat/lease age, retries, failed/degraded runs, and terminal reconciliation equality;

GHL projection/reconciliation attempted/succeeded/retried/terminal/identity-blocked totals and stable circuit state;

stop thresholds that automatically disable/stop the bounded run on cost, error, conflict, latency, or unexpected cohort growth.

No production-wide backfill, paid provider run, GHL reconciliation, or outbound send is part of Task #1669's code-build approval.

13. Final disposition

Current task document: DO NOT IMPLEMENT AS WRITTEN.
Repository at audited SHA: unchanged and clean; no merge decision is being made for code because no BT-10 implementation exists.
Corrected task status: NOT BUILD-READY until all P0 corrections are incorporated into the implementation contract.
Potential future implementation status: SAFE TO MERGE — RUNTIME VERIFICATION PENDING only after all deterministic gates and kill lines pass.
Live operational status: RUNTIME VERIFICATION PENDING; no provider yield, cohort coverage, worker health, or GHL completion was proven here.