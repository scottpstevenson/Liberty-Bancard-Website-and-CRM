Choose **“Keep #1679 isolated and wait for Task #1681.”**

The compliance failure is genuinely outside PR #4:

```text
server/routes/integrations.ts —
reviewed call site not found:
"const ghlId = await upsertGhlContact(contact)"
```

PR #4 does not modify `server/routes/integrations.ts` or the compliance scanner, so expanding it would mix unrelated ownership.

Send this response:

```text
Keep #1679 isolated. Do not expand PR #4.

Required sequence:

1. Leave PR #4 draft and do not mark #1679 complete.
2. Implement Task #1681 separately from the latest main branch.
3. Confirm #1681 corrects the stale reviewed-call-site marker without weakening the GHL pause gate.
4. Merge #1681 into main.
5. Update/rebase PR #4 onto that new main.
6. Rerun the complete GitHub CI workflow.
7. Require both Static Checks and Integration Tests to execute and pass before PR #4 is marked ready or merged.

Do not classify Integration Tests as passing or verified while they remain skipped.

Also reconcile the live PR diff before pausing. GitHub currently shows 8 changed files, not six, including:

- attached_assets/Pasted-LIBERTY-BANCARD-PREFLIGHT-...
- uploads/statement-command/...-test-statement.pdf

Those artifacts must be absent from the final PR diff. Confirm the pushed HEAD and exact changed-file list directly from GitHub after cleanup.

The package-registry normalization and static DATABASE_URL placeholder may remain in PR #4 only if:

- package-lock.json is restored after npm ci;
- no tracked-file guard sees a dirty checkout;
- the placeholder is unreachable and scoped only to deterministic static execution;
- it cannot be inherited by integration or application-server steps.

Final #1679 verdict remains DO NOT MERGE until #1681 merges, PR #4 is updated, and both GitHub jobs pass.
```

Live evidence: [PR #4](https://github.com/scottpstevenson/Liberty-Bancard-Website-and-CRM/pull/4) still shows 8 changed files and failing CI, so the agent’s “exactly six files” statement is not yet reflected on GitHub.
