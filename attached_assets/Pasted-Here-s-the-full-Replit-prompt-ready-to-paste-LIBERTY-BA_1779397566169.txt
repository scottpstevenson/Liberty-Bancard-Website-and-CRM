Here's the full Replit prompt, ready to paste:

---

**LIBERTY BANCARD AI BUSINESS OPERATING SYSTEM — FULL PLATFORM HARDENING PROMPT**

---

You are working on the Liberty Bancard AI Business Operating System, a full-stack merchant services ISO platform built on Replit with React/Node.js/PostgreSQL. The platform includes a public marketing website, admin CRM dashboard, merchant portal, partner/referral portal, GHL integration, OpenAI AI advisors, NMI virtual terminal, and a multi-step merchant application wizard.

This prompt covers everything that needs to be fixed, hardened, and added to make this platform production-ready and capable of acquiring and managing 1,000–5,000 merchants. Work through each section systematically. After completing each section, confirm what was done before moving to the next.

---

**SECTION 1 — CRITICAL FIXES (DO THESE FIRST, NOTHING ELSE WORKS WITHOUT THEM)**

1.1 Fix the failed production build. The last successful build was 9 days ago and a subsequent build failed. Identify the build error from the logs, resolve it, and confirm a clean production deployment with zero TypeScript errors and zero failed imports.

1.2 Validate all API keys in production. Make live test calls to verify:
- GHL API key returns 200 (not 401)
- OpenAI API key returns 200 (not 401 or 429)
- NMI gateway responds to a test ping
- Email service (or GHL email) successfully delivers to a real inbox
Log the result of each test to the console and to a new file at docs/api-key-status.md. If any key is missing or failing, surface a clear error in the operator dashboard.

1.3 Fix email delivery. The build notes indicate email sending was logging to console instead of actually sending. Connect a real email delivery service (GHL email, SendGrid, or Resend). Test by triggering a password reset and confirming the email arrives in a real inbox within 2 minutes.

1.4 Verify all 3 domains resolve correctly with valid SSL:
- libertybancard.com
- dev.libertybancard.com
- liberty-bancard-system.replit.app
Confirm HTTPS is enforced on all three and SSL certificates are valid and not expiring within 60 days.

1.5 Confirm the production database is connected and healthy. Run a test query against the production database. Confirm all tables exist and have the expected schema. Add a database health check endpoint at /api/health/db that returns status, table count, and last migration timestamp.

---

**SECTION 2 — GHL INTEGRATION HARDENING**

GHL is the entire outreach and automation engine. Every item here must be verified with a real live test, not just a UI status card.

2.1 SMS sending — trigger a test SMS from the platform to a real phone number. Confirm delivery within 60 seconds.

2.2 Email sending via GHL — trigger a test email to a real inbox. Confirm delivery and that it doesn't land in spam.

2.3 Voicemail drop — trigger a test voicemail drop. Confirm the audio file plays correctly and the drop registers in GHL.

2.4 Calendar booking — book a test meeting via the GHL calendar integration. Confirm it appears in GHL and sends a confirmation email to the test address.

2.5 E-signature flow — submit a complete test merchant application and confirm GHL triggers the signing request within 60 seconds of submission.

2.6 Contact sync — create a test contact in the platform CRM. Confirm it syncs to GHL within 60 seconds and all fields map correctly.

2.7 GHL conflict logging — verify that sync conflicts are being logged to the conflicts table and surfaced in the operator dashboard. Confirm no conflicts are being silently overwritten.

2.8 GHL workflow ID manager — load /dashboard/ghl-workflows, save a test workflow ID mapping, confirm it persists after page refresh.

2.9 Build a GHL integration health dashboard widget. It should show, in real time: connection status (green/red), last successful sync timestamp, SMS sent today, emails sent today, voicemail drops today, active sequences count, and any sync errors from the last 24 hours. Place this widget on the operator dashboard.

---

**SECTION 3 — FULL MERCHANT APPLICATION CHAIN**

This is the most important conversion flow in the platform. Every step must work end to end.

3.1 Verify all 6 steps of the merchant application wizard complete without errors: business info, owner info, bank details, processing details, equipment selection, e-signature.

3.2 Add real-time EIN lookup to the business info step. When the merchant enters their EIN, auto-validate format and optionally look up the business name via an IRS or third-party EIN validation API.

3.3 Add address autocomplete to all address fields in the application wizard using Google Places API or equivalent.

3.4 Verify document uploads work correctly for all required files: voided check, business license, government ID, processing statements. Confirm files save to the database and are retrievable from the admin dashboard.

3.5 After a complete application submission, verify the full downstream chain fires correctly:
- Admin receives an immediate notification (email + in-app)
- GHL triggers the e-signature request to the merchant
- Merchant receives a confirmation email within 2 minutes
- Application appears in the admin CRM with status "Submitted"
- Onboarding progress tracker updates to the correct stage
- A new entry is created in the onboarding pipeline

3.6 Build an application status page at /application-status that a merchant can access without logging in, using their application reference number, to check where they are in the process. Show: submitted, under review, approved, equipment shipped, live.

3.7 Add an incomplete application recovery flow. If a merchant starts the wizard but doesn't complete it, trigger a GHL sequence after 24 hours: SMS + email with a direct link to resume their application at the exact step they left off.

---

**SECTION 4 — PUBLIC WEBSITE CONVERSION OPTIMIZATION**

4.1 Fix the SEO score from 69 to 85+. Specifically:
- Add proper meta descriptions to every public page
- Add Open Graph tags to every public page
- Add JSON-LD schema markup for LocalBusiness on the homepage
- Fix all missing image alt tags
- Add canonical tags to all pages
- Ensure page titles are unique and keyword-rich on every page
- Generate a sitemap.xml and robots.txt

4.2 Build a savings calculator on the homepage. The merchant enters their monthly processing volume and current rate. The calculator shows their estimated monthly savings with Liberty Bancard using interchange-plus pricing. Make it interactive with a slider for volume. Below the result, show a single CTA: "Get your free statement review."

4.3 Build a dedicated outreach landing page at /switch. This page has no navigation, no footer, one headline, one subheadline, one form (name, business name, phone, monthly volume), and one CTA button. This is the page all GHL sequences, SMS links, and ads will point to. Optimize it for mobile first. Target load time under 2 seconds.

4.4 Add a live chat widget to every public page and both portals. The widget should offer three options: chat with AI, book a meeting, or call us. The AI chat should use the OpenAI integration. The book a meeting option should open the GHL calendar. The call us option should display the business phone number.

4.5 Add social proof to the homepage. Build a testimonials section with at least 3 merchant testimonials. Each testimonial must include: business name, business type, owner first name, and a specific dollar savings figure (e.g. "We save $340/month"). Add a Google reviews aggregate rating badge.

4.6 Add urgency to the homepage and the /switch landing page. Show a time-limited offer banner: "Free terminal for merchants who sign up before [date 30 days from today]." Make the date dynamic so it always shows 30 days from the current date.

4.7 Verify all CTAs on the public site point to the correct destination:
- "Get started" → /switch landing page
- "Free statement review" → statement upload form
- "Book a call" → GHL calendar
- "Apply now" → merchant application wizard
Fix any CTAs that point to the homepage or a 404.

---

**SECTION 5 — ADMIN CRM HARDENING**

5.1 Verify all 80+ dashboard pages load without errors, white screens, or console errors. For any page that crashes, fix it. Document all fixes in docs/qa-report.md.

5.2 Fix the onboarding pipeline. When a merchant application is marked "Approved" in the CRM, automatically create a new deal in the onboarding pipeline with the following stages: Approved → Equipment Ordered → Equipment Shipped → Merchant Live → First Batch Processed. Each stage transition should trigger a GHL automation.

5.3 Build a real-time merchant acquisition dashboard on the main admin homepage. Show: total merchants (all time), merchants this month, merchants this week, pipeline value, average time to close, top performing source (GHL sequence vs inbound vs referral).

5.4 Verify the residual revenue dashboard populates with real data. If it's showing an empty state, seed it with the correct data structure and document what data needs to be imported from the processor.

5.5 Verify role-based access control is working correctly. Test with three user types:
- Admin: full access to everything
- Manager: access to CRM, pipeline, reporting — no access to billing, legal, system settings
- Agent: access only to their own contacts and pipeline — no access to other agents' data
Fix any role that can access areas it shouldn't.

5.6 Add a notifications center to the admin dashboard. Admins should receive real-time in-app notifications for: new merchant application submitted, new lead captured, GHL sync error, AI advisor error, merchant portal support ticket created, new referral submitted. Each notification should be clickable and take the admin directly to the relevant record.

5.7 Verify CSV import handles large files correctly. Test with a 10,000-row CSV. Confirm no timeout, no data loss, and the import completes with a progress indicator visible to the admin.

5.8 Verify CSV export works for contacts, deals, and merchants. Confirm exported files open correctly in Excel with proper column headers.

---

**SECTION 6 — AI FEATURES HARDENING**

6.1 Verify all 7 AI advisors respond correctly in production. Send a test prompt to each: Sales, Support, Onboarding, Marketing, Finance, Compliance, Executive. Confirm each returns a relevant response within 10 seconds.

6.2 Verify AI token usage is being logged to the audit table for every call. Confirm the daily token cost rollup is visible in the operator dashboard.

6.3 Add an AI fallback handler. If the OpenAI API returns an error (rate limit, timeout, or server error), the platform should not crash or show a blank response. Instead show a user-friendly message: "Our AI advisor is temporarily unavailable. Please try again in a moment or contact support." Log the error to the audit table.

6.4 Add a responsible AI disclosure page at /legal/ai-disclosure. Content should cover: how AI is used on the platform, what decisions AI informs vs. what humans decide, data used by AI systems, and how to request human review of any AI-informed decision.

6.5 Add AI-powered merchant health alerts. Every 24 hours, the system should analyze each active merchant's transaction volume trend. If volume drops more than 20% week-over-week, automatically create a health alert in the admin CRM and trigger a GHL outreach sequence to check in with the merchant.

---

**SECTION 7 — MERCHANT PORTAL HARDENING**

7.1 Verify merchant login works correctly. Test with a real merchant account.

7.2 Verify all 4 portal tabs work correctly: Account, Onboarding Progress, Documents, Support.

7.3 Add a merchant analytics tab to the portal. Show the merchant: total transactions this month, total volume this month, average transaction size, chargeback ratio, and estimated monthly savings vs. their old processor. Use placeholder data if live processor data isn't available yet, but flag it clearly as estimated.

7.4 Add a merchant dispute/chargeback tab. Merchants should be able to see open chargebacks, their status, and upload evidence files directly from the portal. Submitted evidence should trigger an admin notification.

7.5 Build a 90-day post-activation nurture sequence in GHL. Triggered when a merchant goes live, the sequence sends:
- Day 3: "How's everything going?" check-in SMS
- Day 14: Email with tips on reading their statement
- Day 30: Email asking for a Google review
- Day 60: SMS offering a referral incentive
- Day 90: Email with a summary of their savings to date

7.6 Build an automated review request flow. After a merchant has been live for 60 days, trigger a GHL sequence asking for a Google review. The email/SMS should include a direct link to the Google review page for Liberty Bancard. Track which merchants have been asked and which have responded.

---

**SECTION 8 — PARTNER/REFERRAL PORTAL HARDENING**

8.1 Verify partner registration, login, and password reset all work correctly.

8.2 Verify referral tracking records the correct partner attribution on every referral submission.

8.3 Verify commission calculations are accurate. Show partners: referrals submitted, referrals approved, referrals live, total commissions earned, commissions pending, commissions paid.

8.4 Build a co-branded proposal generator for partners. A partner enters a merchant's name, business type, and estimated monthly volume. The system generates a PDF proposal with the partner's name and Liberty Bancard co-branding, showing estimated savings and a custom link to apply. This proposal should be shareable via a unique URL.

8.5 Add tiered partner incentives. Build a tier system: Bronze (1–5 merchants), Silver (6–15 merchants), Gold (16–30 merchants), Platinum (31+ merchants). Each tier gets a higher commission rate and additional perks. Show the partner their current tier and how many merchants they need for the next tier.

---

**SECTION 9 — OUTREACH & LEAD ACQUISITION INFRASTRUCTURE**

9.1 Build a GHL sequence builder interface in the admin dashboard. Allow the admin to create and manage multi-channel sequences (SMS, email, voicemail drop) without going into GHL directly. Each sequence should have: name, trigger (manual, form submission, CSV import, tag applied), steps with delay between each, and status (active/paused).

9.2 Build a lead list manager. Allow the admin to: upload a CSV of business contacts, map columns to CRM fields, assign the list to a GHL sequence, and track send/response/conversion rates per list.

9.3 Build a sunbiz import pipeline. Create a dedicated import flow for Florida sunbiz records. Allow filtering by: entity type (LLC, sole proprietor), status (active only), registration date range, zip code or county, and SIC/NAICS code. After filtering, show the estimated record count and allow export to CSV or direct load into a GHL sequence.

9.4 Add a power dialer integration. Research and recommend the best power dialer that integrates with GHL (options include Mojo Dialer, PhoneBurner, or GHL's native dialer). Document the integration steps and estimated cost in docs/dialer-integration.md.

9.5 Build a call disposition tracker. After each outbound call, the agent logs: outcome (answered/no answer/callback/not interested/wrong number/converted), notes, and next action. Dispositions should update the contact's pipeline stage automatically.

9.6 Build a daily outreach metrics dashboard. Show: dials made today, contacts reached, callbacks scheduled, applications started, applications completed, conversion rate, best performing sequence, and best performing list.

---

**SECTION 10 — SECURITY & COMPLIANCE HARDENING**

10.1 Verify session management is working correctly. Sessions should expire after 8 hours of inactivity. Test by logging in, waiting for expiration, and confirming redirect to login page.

10.2 Add forced 2FA for admin accounts. Any user with admin or manager role who has not enrolled in 2FA should be forced to enroll on next login before accessing the dashboard.

10.3 Verify all API endpoints require authentication. Write a test script that hits every /api/ route without an auth token. Every protected route should return 401. Document any routes that return 200 without auth in docs/qa-report.md as critical security issues.

10.4 Add rate limiting to all public API endpoints. The lead capture form, statement upload, and merchant application endpoints should be rate limited to prevent abuse. Implement per-IP rate limiting with a 429 response when exceeded.

10.5 Verify PCI self-assessment module is complete and accurate. The platform handles payment data references and must not store raw card data anywhere. Audit all database tables and confirm no table stores card numbers, CVVs, or full track data.

10.6 Add a TCPA compliance audit log. Every outbound SMS and call must log: contact phone number (hashed), timestamp, campaign name, consent basis, and opt-out status. This log must be queryable and exportable for compliance purposes.

10.7 Verify GDPR/CCPA data deletion works. Test the "delete my data" flow: submit a deletion request, confirm all records for that contact are purged from the database within 30 days, and confirm a deletion confirmation is sent to the requester.

---

**SECTION 11 — PERFORMANCE & RELIABILITY**

11.1 Add a global health check endpoint at /api/health. It should return: overall status, database status, GHL connection status, OpenAI connection status, NMI connection status, and last deployment timestamp. This endpoint should be publicly accessible (no auth required) for uptime monitoring.

11.2 Verify the job orchestration table shows all background workers checking in with a heartbeat. Any worker that hasn't checked in within 5 minutes should trigger an alert in the operator dashboard.

11.3 Add error boundary components to every major React page. If a page crashes, it should show a user-friendly error message with a "Report this issue" button rather than a white screen.

11.4 Audit all database queries for N+1 problems. Any page that makes more than 10 database queries to render should be flagged and optimized with proper joins or batch queries.

11.5 Verify the platform handles mobile correctly at 390px viewport. Test all public pages, the merchant application wizard, the merchant portal, and the partner portal at 390px. Fix any layout overflow, hidden CTAs, or broken navigation.

11.6 Add automated database backups. Configure daily backups of the production database with a 30-day retention period. Document the backup and restore process in docs/backup-restore.md.

---

**SECTION 12 — DOCUMENTATION & HANDOFF**

12.1 Update replit.md with a complete, accurate description of every feature currently in the platform, every environment variable required, and the correct admin login credentials.

12.2 Create docs/qa-report.md. Document every issue found during this hardening pass with: URL or feature name, description of the issue, severity (Critical/High/Medium/Low), and fix applied or recommended.

12.3 Create docs/go-live-checklist.md. A simple yes/no checklist of everything that must be true before the platform goes live with real merchants. Include: all API keys configured, build passing, email delivering, GHL sending, merchant application chain tested end to end, admin accounts created for team members, and legal pages reviewed.

12.4 Create docs/onboarding-guide.md. A step-by-step guide for a new agent or manager logging into the platform for the first time. Cover: how to log in, how to import a lead list, how to start a GHL sequence, how to review a merchant application, and how to use the AI advisors.

12.5 Create docs/dialer-integration.md. Research and document the best power dialer option for this platform, integration steps, cost, and pros/cons.

---

**EXECUTION INSTRUCTIONS**

Work through each section in order. Sections 1 and 2 are blocking — do not proceed to later sections until all critical fixes and GHL integrations are verified live with real test calls.

After completing each section, write a brief confirmation summary: what was done, what was tested, and any issues found that need a separate fix task.

Save all findings, fixes, and documentation to the docs/ folder.

When complete, run a full build, confirm it deploys successfully to production, and provide a final summary of everything changed, fixed, and built.

---

That's the full prompt. Paste it directly into your Replit agent chat. It's structured so the agent works through it in the right order — critical fixes first, then integration hardening, then conversion optimization, then everything else.

The most important thing to watch: make sure the agent actually tests GHL with real sends in Section 2 before moving on. That's the part most likely to be broken and most likely to get glossed over with a "status card shows connected" instead of a real live test.