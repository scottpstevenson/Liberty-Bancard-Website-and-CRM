Verdict: PREFLIGHT REQUIRED

This is a production backfill over ~154K contacts plus a new background-job control surface. Do not approve fire-and-forget execution until Replit proves the existing job is restart-safe, resumable, and actually bulk-writes scores.

The brief also contradicts itself: “do not re-score within 24 hours,” but the endpoint scope says last_scored_at < 90 days.

Corrections to send back

Run a code preflight before implementation and provide file:line evidence for:
1. ContactScoringJob ownership:
   - current entry points,
   - concurrency guard,
   - restart behavior,
   - batch cursor,
   - retry behavior,
   - whether it already persists progress.
2. Exact scoring schema:
   - lead_score,
   - rev_potential_score,
   - switchability_score,
   - uw_confidence_score,
   - engagement_score,
   - last_scored_at,
   - nullability and defaults.
3. Exact write behavior:
   - prove scoring uses bulk updates/upserts,
   - confirm there is no per-contact DB update loop,
   - identify transaction and partial-failure behavior.
4. Define the target population consistently.
Choose one rule and use it everywhere:
- one-time backfill: score contacts with any missing score component; or
- stale re-score: score contacts older than a defined age.
Do not mix:
- “skip scored in last 24 hours,”
- “NULL or zero,”
- and “older than 90 days.”
Also verify whether lead_score = 0 means genuinely unqualified or historically unscored. Do not overwrite valid zero scores based only on value.
5. Do not use setImmediate fire-and-forget for a 154K-row production job unless the existing architecture explicitly supports restart-safe in-process jobs.
Preferred:
- reuse the canonical BullMQ/background-job path if it exists;
- otherwise implement a persisted resumable backfill with an atomic single-run guard, cursor, heartbeat/progress, and failure state.
6. Define job states:
idle | queued | running | complete | failed | interrupted
Persist:
- runId,
- processed,
- updated,
- skipped,
- failed,
- total,
- cursor,
- startedAt,
- lastProgressAt,
- completedAt,
- lastError.
7. Prevent overlapping runs.
The POST endpoint must atomically reject a second active run and return the current run status. A UI-disabled button is not a concurrency guard.
8. Make the backfill resumable.
Use deterministic keyset pagination, for example contact ID > lastProcessedId, not OFFSET pagination. Persist the cursor after each committed batch.
9. Do not promise that “all contacts” will receive meaningful scores unless the scoring function supports missing input fields.
Track:
- scored,
- skipped with reason,
- failed,
- still missing components.
Completion must not be reported when records remain unprocessed without explanation.
10. Do not calculate the full score distribution on every 3-second poll.
Progress polling should read the persisted progress record only. Compute the final database distribution:
- once at completion,
- on explicit refresh,
- or from an appropriately indexed/cached aggregate.
11. Verify LCC and enrollment ownership.
Provide file:line evidence that:
- LCC Hot/Warm cards read the same canonical score and thresholds;
- new-lead-enrollment-job intentionally consumes backfilled scores;
- scoring does not automatically enroll or send outreach during the backfill.
12. Audit log both start and completion/failure:
- contact_mass_score_started
- contact_mass_score_completed
- contact_mass_score_failed
Include runId, actor, target criteria, processed, updated, skipped, failed, and distribution.
13. Add a dry-run before apply.
The operator must see:
- exact eligible count,
- breakdown by missing/stale reason,
- sample contact IDs,
- estimated batch count,
- confirmation that prospects are excluded.
14. Verify the Activation Panel actually exists and is the canonical owner before adding UI there. Do not couple this task to an unfinished cold-outreach task.

Kill lines

STOP if lead_score = 0 is assumed to mean unscored without code/data proof.
STOP if the 24-hour and 90-day eligibility rules remain contradictory.
STOP if the job uses setImmediate with no persisted cursor or restart recovery.
STOP if two mass-scoring runs can overlap.
STOP if pagination uses OFFSET while rows are being updated.
STOP if each contact causes an individual database update.
STOP if progress is stored only in process memory.
STOP if a failed or restarted process can leave status permanently running.
STOP if the status endpoint runs a full 154K-row distribution query every 3 seconds.
STOP if scoring directly triggers enrollment, sequences, GHL sync, email, SMS, or other outreach without explicit blast-radius approval.
STOP if the job changes calculateLeadScore() or scoring thresholds.
STOP if implementation begins without a dry-run count and sample.

Validation

npx tsx scripts/smoke-role-guards.ts
pnpm typecheck
pnpm lint
pnpm test

Required targeted checks:

- Unauthorized users cannot start or inspect privileged scoring runs.
- Two simultaneous POST requests create only one run.
- A forced crash after batch N resumes from the persisted cursor without double-counting.
- Duplicate retries do not corrupt scores or progress totals.
- Valid zero scores are not incorrectly treated as unscored.
- All five score fields and last_scored_at update consistently.
- Progress totals reconcile: updated + skipped + failed = processed.
- Final eligible count reaches zero or remaining records have explicit reasons.
- Prospects table is untouched.
- Polling performs constant-cost progress reads.
- LCC distribution matches a direct verification query.
- No enrollments or outbound sends are triggered by the backfill itself.

Final status: PREFLIGHT REQUIRED

The business intent is valid, but this must be designed as a resumable production backfill, not a button that launches an untracked Express promise.